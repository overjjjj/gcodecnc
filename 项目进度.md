# CNEXT-CAM 项目进度

> 规则：每次修改代码、修复问题或新增功能，都必须在本文件追加记录。本文档已统一为 UTF-8。

## 2026-05-29

### 已完成：盲槽粗加工余量参数生效
- 复查槽加工链路时发现 `BlindSlotMillingStrategy` 虽然已有 `stockToLeave` 参数，但粗加工范围实际只按刀具半径缩进，没有把用户设置的加工余量计入刀心范围。
- 盲槽粗加工现已按 `toolRadius + stockToLeave` 计算刀心缩进，保证粗加工不会直接扫到成品槽壁/槽底边界。
- 当某一层因为余量、刀具直径或斜面收缩导致没有有效粗加工范围时，现在会跳过该层粗扫并输出调试注释，不再在无效范围内强行下刀；最终清底和轮廓精加工仍按成品尺寸生成。

### 已完成：孔口倒角改为切向斜入斜出
- 根据后续开发计划中的 P1 安全项，修正 `ChamferStrategy` 原先从孔中心在倒角深度横向进给到圆周的问题，避免刀具在已加工孔口/孔底区域留下径向划痕。
- 孔口倒角新增 `leadLength` 参数，默认 1mm，并按实际倒角刀心圆半径自动钳制，避免小孔倒角时引入线过长。
- 新刀路改为：快速定位到圆周切点的切向外侧 R 平面，`G1 X/Y/Z` 斜向切入到倒角深度，完成一圈 `G2` 后再切向斜出到 R 平面，不再回孔中心收刀。

### 已完成：绑定 GitHub 远程仓库
- 根据用户提供的远程仓库地址 `https://github.com/overjjjj/gcodecnc.git`，准备将本地 Git 仓库绑定到 GitHub `origin` 并推送主分支。
- 当前本地默认分支为 `main`，已有首个项目提交；后续代码和项目进度记录将通过 Git 版本管理同步。

### 已完成：GitHub 接入本地仓库准备
- 在项目根目录新增 `.gitignore`，排除 Qt/qmake 构建产物、Debug/Release 目录、本地 `.pro.user` 配置、临时目录、崩溃转储和二进制产物。
- 该准备用于后续初始化 Git 仓库并绑定 GitHub 远程地址，避免把本地编译输出和个人环境配置提交到远程仓库。

### 已完成：盲槽默认改为槽长方向斜线进刀
- 根据实际加工风险复盘，确认盲槽在 `helixRadius=0` 时直接垂直下刀存在折刀风险，尤其是槽宽小于两倍刀径时。
- `BlindSlotMillingStrategy` 新增 `rampAngle` 参数，默认 3 度；粗加工层、底面精加工首刀和轮廓精加工入口现在会沿槽长方向生成 ramp 斜线进刀。
- ramp 进刀先快速定位到斜线起点、下到槽口顶面，再用 `G1 X... Y... Z...` 连续斜降到目标层深；若槽内可用长度不足，则保留垂直下刀兜底，避免生成越界刀路。
- 该修正只处理盲槽进刀安全性，不改开口槽策略，也不改槽识别逻辑。

### 已完成：批量生成 G 代码增加工序分段注释
- 根据用户对“孔口倒角 G 代码看起来像前面精修轨迹”的反馈，确认当前问题主要出在 `生成全部 G 代码` 会把多道孔工序直接串接，文本里缺少清晰的工序边界提示。
- `MainWindow` 的批量生成入口现已为每个输出块追加工序分段注释，包含工序号、策略名、特征摘要和刀具号，便于直接从 G 代码中区分“扩孔/精修/倒角”等相邻工序。
- 该修正只增强批量 G 代码可读性与核对流程，不改 `ChamferStrategy`、扩孔策略或任何孔/槽刀路算法。

### 已完成：工序列表与批量 G 代码定位联动
- 针对用户截图中“选中孔口倒角工序但 G 代码仍停在圆插补精修段”的问题，`OperationListPanel` 现已在选中行变化时发出当前工序号。
- `MainWindow` 会根据批量 G 代码中的 `; ---- Operation N ----` 分段标记自动跳转并高亮对应工序段，方便逐道核对刀路。
- 批量 G 代码分段注释改为纯 ASCII 策略 ID 和 `Operation N`，避免中文注释在不同编码环境中显示成乱码。
- 进一步补齐“生成前已选中某道工序”的场景：批量 G 代码生成完成后会读取当前工序号并主动跳转一次，避免因为选中行没有变化而停留在前一道工序。

### 已完成：设置正面后通孔正反面归类修正
- 根据用户反馈确认：点击“设置正面”后，通孔会整体被归到反面，导致当前单装夹正面加工流程下全部无法生成。
- `MainWindow::reclassifyFeaturesForSetup()` 现已对 `through_hole` / `countersunk_through_hole` 特殊处理：不再只按 `-feature.axis` 推断开口面，而是按孔中心更接近当前模型 `bbMax.z()` 还是 `bbMin.z()` 来归类到正面或反面。
- 该修正只影响“设置正面后的装夹重分类”，不改孔识别本身的半径、深度和子类型。

### 已完成：槽默认策略按开口侧判定并补充策略日志
- `StrategyPanel` 的槽默认策略不再只看 `subType == open_slot`，改为同时识别 `MachiningFeature.openSide`；只要识别结果已经给出开口侧，就默认落到 `mill_slot`，避免“直槽识别正确但默认仍走盲槽铣”。
- `MainWindow` 在槽工序生成入口新增 `strategy-selected` 调试日志，直接打印当前 `strategyId`、槽子类型和 `openSide`，用于区分“界面默认选错策略”和“策略选择正确但生成结果错了”两类问题。
- 下一步：重新编译并让 `槽.stp` 复测，重点确认日志里 `strategy-selected id=` 是否为 `mill_slot`，以及 G 代码头部是否从 `Blind slot geometry` 切换为 `Open slot geometry`。

### 已完成：检测出开口侧后直接升级为开口槽
- 根据用户实测日志确认：`detectOpenSlotSide()` 当前能正常返回开口侧，但 `toContourFeature()` 之前只写 `openSide`，没有把 `subType` 从 `straight_slot` 升级成 `open_slot`，导致后续仍落入盲槽策略。
- `MainWindow::toContourFeature()` 现已改为：只要网格边界检测出有效开口侧，就同步把 `ContourFeature.subType` 设为 `open_slot`，让策略选择、约束校验和 G 代码生成统一走开口槽链路。
- 下一步：重新编译后复测 `槽.stp`，确认日志中的 `requested-after-recognized-slot subtype=` 变成 `open_slot`，且 G 代码头注释切换为 `Open slot geometry`。

### 已完成：开口侧判定增加槽端外侧材料覆盖校验
- 根据最新 `slot-debug` 日志确认，当前问题已经收敛为 `detectOpenSlotSide()` 未能识别出开口侧，导致 `openSide` 一直为 `0.000`，槽继续按盲槽处理。
- `MainWindow::detectOpenSlotSide()` 现已从“仅比较槽两端到模型 XY 包围盒边界的距离”升级为“双判据”：同时统计槽端外侧局部区域是否还有三角网格材料覆盖。
- 现在只要某一端外侧明显缺料，而另一端外侧仍有材料覆盖，就优先把该端判成开口端；仅当边界距离和材料覆盖都显示两端封闭时，才继续返回 `0.0`。
- 下一步：复测 `槽.stp`，确认 `openSide` 不再为 `0.000`，并联动把 `subtype` 升级为 `open_slot`。

### 已完成：槽策略纠正后移到识别回填之后
- 根据用户复测日志确认：`requested-after-recognized-slot` 已经正确变成 `subtype=open_slot openSide=1.000`，但 `strategy-selected` 仍然保持 `mill_blind_slot`，说明原先在入口 `feature` 上做的策略纠正时机太早。
- `MainWindow` 现已把“盲槽铣 -> 开口槽铣”的强制纠正放到识别槽回填 `requestedFeature` 之后执行；只要识别结果已确认是开口槽，就重新绑定 `mill_slot` 策略对象。
- 同时新增 `strategy-initial` 调试日志，区分“UI 初始请求策略”和“识别纠正后的最终执行策略”。

### 已完成：开口槽开口侧符号约定对齐
- 根据最新复测结果确认：开口槽已经正确切到 `mill_slot`，但刀路仍朝错误一端展开，问题已经从“策略选错”收敛为“开口侧符号方向反了”。
- `SlotMachiningGeometry` 现已统一 `openSide` 和 `openSign` 的符号约定：当识别结果给出 `openSide > 0`（+U 端开口）时，内部 `openSign` 反向映射为 `-1`，以匹配现有开口槽粗加工、引入和侧壁精加工代码的方向假设。
- 该修正不改槽长、槽宽、角度和深度，只修正开口槽从哪一端进刀、往哪一端收口。

### 已完成：开口槽粗加工宽度按平底有效宽度收口
- 根据最新仿真截图确认：开口端方向已切换正确，但粗加工扫描仍按矩形总槽宽展开，导致刀路在槽外侧成排扫出。
- `SlotMachiningGeometry` 现已在 `roughHalfWidth` 上增加平底有效宽度约束：当识别到槽宽方向斜面/开口收缩时，粗加工半宽会进一步受 `slopeMinWidth/slopeMaxWidth` 和刀具半径限制。
- 该修正优先保证开口槽粗加工刀心留在识别到的真实槽底宽度内，减少外扩扫刀。

### 已完成：开口槽高风险补丁回退，恢复稳定路径基线
- 根据用户要求先回到“今天最开始 G 代码生成没问题”的状态，已撤回后续叠加在 `mill_slot` 上的高风险行为改动，包括：`openSign` 反向映射、按平底宽度强制收窄 `roughHalfWidth`、以及把粗加工线强行裁到 `bottomU` 范围内。
- 当前保留的仅是已验证必要的两层修正：槽工艺入口不再把槽放大成整块模型；识别为开口槽后可从 `straight_slot` 升级到 `open_slot` 并切到 `mill_slot`。
- 下一步会在这条更稳定的基线之上，单独重做“开口端进入逻辑”，避免继续混入会破坏原始粗加工扫描的补丁。

### 已完成：暂停把直槽强制切到开口槽策略
- 经过多轮复测确认，当前 `mill_slot` 这套开口槽矩形往复粗加工算法本身不适合该槽型；问题不再是识别和策略切换，而是开口槽原始路径核心不对。
- `MainWindow` 现已撤回“识别出 `open_slot` 后强制把 `mill_blind_slot` 改成 `mill_slot`”这一步，先恢复到更可控的盲槽路径生成链路，避免继续把错误刀路作为默认结果输出。
- 后续如果要真正上线开口槽工艺，需要单独重写 `mill_slot` 的粗加工策略，而不是继续在当前矩形行切算法上叠补丁。

### 已完成：槽调试入口日志收口
- 在确认当前阶段先回退到稳定盲槽生成链路后，已移除 `strategy-initial` 这类只用于追踪策略切换时机的临时日志，减少后续测试时的噪声。
- 保留的 `slot-debug` 仅继续服务于几何核对：界面参数覆盖后、匹配到识别槽后、以及最终批量送入策略前的状态。

### 已完成：槽加工入口调试日志
- `MainWindow` 为槽工序生成入口新增 `qDebug` 调试输出，覆盖“界面参数覆盖后”、“匹配到识别槽后”和“最终批量送入策略前”三段关键几何状态。
- 日志内容包含槽子类型、中心点、槽长、槽宽、加工深度、方向角、开口侧以及当前参数表中的 `slotLength/slotWidth/depth/angle`，用于判断问题出在 UI 覆盖、槽特征匹配还是策略入口。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽工艺入口网格细化收紧
- 根据 `slot-debug` 日志确认：槽工序生成前的 `toContourFeature(..., &mesh)` 会在 `refineSlotContourFromMeshData()` 中把“槽附近点”扩展到整块模型网格，导致小槽被放大成外轮廓方向的大槽。
- `SlotMachiningGeometry.cpp` 现已移除该二次扩展采样，只保留槽特征自身 `faceIndices` 对应的网格点来细化槽中心、长宽、方向和斜面范围，避免槽工艺入口再次把整块模型识别进去。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`debug/CNEXT-CAM.exe` 已重新生成。
- 下一步：重新用 `槽.stp` 跑一遍槽生成，确认 `requested-after-recognized-slot` 与识别槽尺寸保持一致，不再被放大到外轮廓。

### 已完成：开口槽开口端判定采样增强
- `MainWindow` 中 `detectOpenSlotSide()` 从“槽长方向单点取样”改为“端面中心 + 宽度方向左右偏移三点采样”，综合最小边界距离和平均边界距离判断开口端。
- 该修正用于减少斜放槽、开口边不完全居中或端部局部缺料时，把开口端误判到反方向的问题。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前开口端判定仍基于模型 XY 包围盒边界距离，不是沿槽轴真实求交；对内部开口、复杂外轮廓或非矩形毛坯仍可能不够精确。

### 已完成：开口槽粗加工范围校验与斜面提取收口
- `SlotMachiningGeometry` 不再要求槽底低位采样同时在长向和宽向都满足阈值才保留斜面信息，改为分别提取长度方向和宽度方向的底部轮廓；这样开口槽在局部采样不足时，长度方向斜面不再整段丢失。
- `SlotMillingStrategy` 的开口槽粗加工层生成前新增有效范围校验，按当前层的 `U` 向有效长度和 `roughHalfWidth` 先检查刀心偏移是否越界，异常时直接中止生成。
- 每层粗加工现在会在 G 代码里输出 `Open slot rough layer` 注释，带出当前层 `Z`、`U` 范围和半宽，便于后续联动仿真和排查槽刀路问题。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前开口槽粗加工的越界校验仍是基于策略几何模型，不是基于真实网格逐段求交；如果识别出的开口方向或槽轴本身有误，仍需要继续在特征识别层修正。

## 2026-05-28

### 已完成：WCS 多坐标系第一期
- `PostProcessorOptions` 新增 `workOffset` 字段，默认 `G54`，用于把当前工件坐标系选择传入后处理器。
- `SiemensPostProcessor` 和 `FanucPostProcessor` 在程序头 `G90/G91` 后输出当前 `G54-G59`，孔、槽、型腔、轮廓等所有策略生成的程序统一受该坐标系选择控制。
- `MainWindow` 工具栏新增 `G54-G59` 下拉框，单次生成、圆铣、铣削批量生成和工序列表“生成全部”都会使用当前下拉框选择。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前只做 WCS 选择输出，不自动计算侧面/反面装夹的坐标旋转矩阵；侧面孔、反面孔仍需要用户在机床侧提前设置对应 WCS 偏置并配合正确装夹。

### 已完成：型腔开粗螺旋下刀
- `PocketRoughingStrategy` 新增 `helixRadius` 和 `helixPitch` 参数，默认在型腔中心附近走 `G2 ... Z...` 螺旋下刀后再进入往复式行切。
- 螺旋半径会按型腔可加工范围自动钳制，型腔太小或参数无效时保留原有垂直下刀兜底，避免小型腔直接生成越界螺旋。
- 估算时间同步把螺旋圆周长度计入总刀路长度，避免新增下刀路径后时间明显偏低。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前螺旋仍按型腔中心圆形轨迹处理，不检测中心岛、复杂边界和真实材料去除状态；复杂型腔后续需要边界裁剪和碰撞层继续兜底。

### 已完成：盲槽宽度方向斜边精加工补齐
- `BlindSlotMillingStrategy` 在最终层已有起端/末端斜面精加工的基础上，继续补入宽度方向左右两侧斜边的 `G1 X/Y/Z` 三轴成形线。
- 左右侧斜边现在按最终槽底有效长度范围排线，刀心从槽口顶面可加工边界走到槽底边界，和底面精加工、端部斜面精加工顺序衔接。
- 新增注释 `Blind slot left side slope finish` / `Blind slot right side slope finish`，便于后续做 G 代码运行联动和仿真问题定位。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前盲槽斜边精加工仍按线性斜坡近似，不包含角部混合过渡、圆角端部裁剪和实体级刀具包络校验。

### 已完成：开口槽侧壁精加工与平底范围对齐
- `SlotMillingStrategy` 的开口槽最终层侧壁精加工不再按 `roughMinU/roughMaxU` 整个槽长范围走刀，而是改为严格使用 `bottomStartU/bottomEndU` 的平底直壁范围。
- 该修正避免把入口/出口斜面段误当成垂直侧壁继续精加工，减少最终层侧壁刀路与前面底面精加工、斜面精加工之间的边界重叠和几何失真。
- 侧壁精加工段新增独立注释 `Open slot side wall finish`，方便后续做 G 代码运行联动和问题定位。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前开口槽侧壁精加工仍按两侧直壁矩形路径处理，不包含侧壁真实倒角/圆角、机床侧补引入引出优化和实体级碰撞包络校验。

### 已完成：盲槽最终层补齐底面精加工
- `BlindSlotMillingStrategy` 最终层新增 `Blind slot bottom finish` 清底段，不再只做分层粗加工后直接收敛到四边轮廓精加工。
- 清底线会按最终成品槽底可加工范围生成更细步距的往复式底面精加工，并根据长边/短边自动选择主扫描方向。
- 盲槽侧壁轮廓精加工范围无效时，当前也会尽量保留底面精加工，不再因为四周轮廓收尾不可用而整段跳过最终清底。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前盲槽底面精加工仍基于矩形化局部范围，不包含圆角端部、复杂封闭槽轮廓裁剪和实体材料去除校验；这部分后续需要和几何边界层进一步统一。

### 已完成：平面加工主入口闭环
- `StrategyPanel` 现在会把 `FlatSurface` 特征直接映射到 `mill_face` 和 `mill_surface_finish`，不再把平面误当成普通开/闭轮廓铣入口。
- 右侧策略面板新增平面特征文案、平面加工策略名称和提示语；平面场景下默认优先选择较大直径立铣刀，避免面铣入口默认落到最小刀具。
- `FaceMillingStrategy` 和 `SurfaceFinishStrategy` 改为直接使用识别到的平面 `length/width` 生成往复式平面刀路，并在边界外补半刀径覆盖，不再退化为基于 `radius` 的正方形占位刀路。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前平面加工仍按识别框的矩形范围生成，不支持复杂岛屿边界、局部避让和任意旋转面域精确裁剪；这部分后续要在平面边界提取层补齐。

### 已完成：快速下刀纳入刀具包络碰撞检查
- `SimulationController::detectCollisionMessage()` 的快速移动检查从“快速 XY”扩展为所有 `G0` 快速移动，包括快速下刀和斜向快速移动。
- 检查沿三维快速段按长度采样，并继续复用刀具半径足迹点与局部三角网格表面高度比较。
- 当快速 Z 向移动进入毛坯包络时给出独立警告，避免只横移报警而漏掉高速下刀扎入毛坯的风险。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前检查仍基于静态毛坯网格，不知道前序工序已去除的材料；后续需要材料状态模型才能减少已加工区域内的误报。

### 已完成：快移碰撞检查纳入刀具半径近似
- `SimulationController::detectCollisionMessage()` 的快速移动检查从刀尖中心点扩展为刀具包络近似采样。
- 每段快速 XY 移动会沿路径采样中心点和 8 个刀具半径方向的足迹点，按局部三角网格表面高度判断是否过近。
- 无有效刀具直径时自动退回中心点检查；有刀具直径时可捕捉刀具边缘贴近台阶、槽壁或凸台的风险。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前仍是圆柱足迹近似，不使用真实 STL 刀具网格做扫掠碰撞，也不考虑材料已被去除后的动态毛坯。

### 已完成：内置仿真刀具模型资源
- 新增 `untitled/tool_models/`，内置 9 个可直接使用的仿真 STL：D3/D6/D10 立铣刀、D3/D6 钻头、D4 90 度中心钻、M6 丝锥、D10 90 度倒角刀、D50 面铣刀。
- 这些模型为项目内生成的占位资源，尺寸单位为 mm，最低 Z 已按刀尖对齐，适配当前仿真视口的刀具模型加载规则。
- `ViewportWidget` 增加相对路径解析，会从程序目录和项目目录查找 `tool_models/...`，保证 Debug 目录运行时也能加载项目内模型。
- `ToolLibrary::loadDefaults()` 会为匹配的默认刀具自动绑定模型路径；现有 `debug/tools.json` 也已同步写入 9 条 `modelPath`。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：模型为视觉仿真用简化模型，不代表具体品牌刀具的真实刃型和夹持结构。

### 已完成：刀具模型支持二进制 STL
- `ViewportWidget` 的刀具模型加载器新增 binary STL 解析，按 STL 80 字节头、三角面数量和 50 字节三角记录读取法向与顶点。
- `.stl` 导入现在先按二进制 STL 判断，文件尺寸不匹配时自动回退到 ASCII STL 解析；`.obj` 支持保持不变。
- 二进制 STL 导入后仍复用原有模型归一化逻辑：XY 居中、最低 Z 对齐刀尖、随仿真刀尖移动。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前 STL 模型仍只用于显示，尚未参与刀具体积碰撞检测；大模型暂未做三角面抽稀或缓存优化。

### 已完成：真实刀具模型导入与仿真显示
- `ToolEntry` 新增 `modelPath` 字段，`ToolLibrary` 保存/加载 `tools.json` 时同步持久化刀具模型路径。
- 刀具编辑对话框新增“刀具模型”选择入口，可为单把刀具绑定 `.obj` 或 `.stl` 文件；未绑定模型时继续使用原有简化圆柱刀具显示。
- `SimulationController` 解析 G 代码中的 `T` 号时同步记录当前刀具模型路径，并通过 `toolModelPathChanged` 信号传给视口。
- `ViewportWidget` 新增轻量模型加载与显示：支持 ASCII OBJ 和 ASCII STL 三角面片，加载后自动 XY 居中、最低 Z 对齐刀尖，并随仿真刀尖位置移动。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前模型仅用于视觉显示；暂不支持二进制 STL、STEP 刀具模型，也未把真实刀具网格纳入碰撞检测。

### 已完成：仿真快移检查升级为网格局部高度采样
- `SimulationController::detectCollisionMessage()` 的快速移动预警从全局包围盒最高点判断，升级为沿快速 XY 段采样并用三角网格 XY 投影求局部最高表面 Z。
- 快速移动只有在采样点低于局部毛坯表面安全余量时才报警，减少台阶、斜面、槽口等模型上因全局最高点导致的误报。
- 仍保留刀路明显低于毛坯底部的检查，作为异常深度/坐标系错误的快速兜底。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前只检查刀心点的局部表面高度，不计算刀具半径扫掠体和材料去除后的动态毛坯。

### 已完成：仿真层基础碰撞警告
- `SimulationController::detectCollisionMessage()` 从占位改为基础检查：加载 G 代码后按模型包围盒检查低位快速 XY 移动，以及刀路明显低于毛坯底部的情况。
- `loadGCode()` 解析刀路后会发出 `collisionChecked` 信号；`MainWindow` 接收后在底部状态栏显示碰撞/越界警告。
- 该检查覆盖所有进入仿真的 G 代码，包括槽加工生成的 `G0/G1` 路径和由圆弧/固定循环展开后的仿真线段。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前仍是包围盒级别的快速预警，不做三角网格求交，也不计算刀补后的完整刀具体积。

### 已完成：开口槽生成后刀路基础校验
- `SlotMillingStrategy` 新增开口槽 G 代码回读校验，在返回结果前解析生成的 `G0/G1` 运动行并按槽本地坐标复核刀心范围。
- 校验覆盖低位快速 XY 移动、刀心 U/V 是否越过允许槽范围、Z 是否低于槽底；开口侧允许引入/引出长度，避免误判正常进退刀。
- 校验失败时直接返回错误信息，不输出可能越界或低位快移的开口槽刀路。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前仍是刀心线级别的基础校验，不替代实体碰撞检测；G2/G3 圆弧和刀补后的真实包络需要后续更完整的仿真/验证层处理。

### 已完成：开口槽斜面精加工成形线
- `SlotMillingStrategy` 新增 `feedXYZ()` 和开口槽斜面精加工辅助函数，在最终层清底后按槽几何中的 `slopeStartLength/slopeEndLength` 输出 `G1 X/Y/Z` 三轴插补成形线。
- 起端/末端斜面分别按 `roughMinU -> bottomStartU`、`bottomEndU -> roughMaxU` 生成，Z 方向从顶面到槽底或从槽底回顶面，宽度方向按刀具半径限制后的多条偏移线覆盖。
- 斜面长度无效或刀具半径导致可加工宽度不足时自动跳过，不影响已有粗加工、底面精加工和侧壁精加工流程。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前斜面精加工仍基于识别到的线性斜面长度，不做实体级刀具包络/碰撞求交；复杂曲面过渡槽仍需要后续验证层兜底。

### 已完成：G 代码行高亮与仿真联动
- `ToolMove` 新增 `sourceLine`，G 代码解析时把每一段 `G0/G1/G2/G3` 运动和 Siemens 固定循环展开运动绑定到原始 G 代码行号。
- `SimulationController` 新增 `currentGCodeLineChanged(int)` 信号和 `seekToGCodeLine(int)` 方法；仿真播放时会同步发出当前运行行，非播放状态可按 G 代码行跳转到对应刀具位置。
- `GCodeEditor` 新增当前执行行高亮、清除高亮和 `lineSelected(int)` 信号；用户点击/移动到某一行时可驱动仿真定位，程序自动设置文本或自动滚动时不会误触发跳转。
- `MainWindow` 接通双向联动：播放时编辑器黄色高亮当前运行行；暂停/停止/未播放状态下，点击 G 代码行会让仿真刀具位置同步到该行对应轨迹。
- 已执行 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前联动按“源 G 代码行 -> 第一段对应运动”定位；一行圆弧或固定循环展开为多段时，点击该行定位到该行第一段/当前播放段，不提供行内分段选择。

### 已完成：G 代码编辑器语法高亮
- 新增 `GCodeHighlighter`，基于 `QSyntaxHighlighter` 对 G 代码编辑器内容做轻量语法高亮。
- 高亮规则包括：`G/M/X/Y/Z/F/S/T` 地址字母蓝色加粗、数字黑色、`;` 注释灰色斜体、`MCALL/CYCLE83/CYCLE84/CYCLE85` Siemens 循环关键字紫色加粗。
- `GCodeEditor` 构造时挂载高亮器，并切换为浅色背景以保证数字和注释可读。
- `untitled.pro` 已加入新增的 `GCodeHighlighter.h/cpp`。
- 已执行 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 补充确认：当前主界面已有“导出 G 代码”入口，导出扩展名由后处理器决定；Siemens 输出 `.mpf`，Fanuc 输出 `.nc`。

### 已完成：开口槽底面精加工与基础几何验证
- `SlotMachiningGeometry` 新增 `validateSlotMachiningGeometry()`，集中校验开口槽加工前的几何、刀具直径、步距、进给、进给起始高度、安全高度、粗加工范围和槽底有效长度。
- `SlotMillingStrategy` 生成最终层时新增“Open slot bottom finish”底面精加工段，在侧壁 U 形精加工前按槽底有效范围生成受限 zigzag 清底线。
- 开口槽策略改为复用几何层验证结果，减少策略内部重复校验，为后续槽/轮廓/平面统一验证层打基础。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前底面精加工仍是刀心线级别清底，不包含实体材料去除校验；斜面真实成形和快速移动穿模检测仍需后续补齐。

## 2026-05-27

### 已完成：槽加工几何调试注释
- 盲槽 G 代码块开头新增几何注释，输出实际进入策略的槽长、槽宽、深度、角度、长向/宽向斜面收缩量和最终精加工范围。
- 开口槽 G 代码块开头新增几何注释，输出实际槽几何、粗加工 U 向范围、半宽、开口方向、斜面长度和平底范围。
- 这些注释不改变刀路执行，只用于后续排查：如果仿真出现空缺或越界，可以直接通过 G 代码判断是识别/几何重算问题还是路径生成问题。

### 已完成：槽策略几何参数二次覆盖修正
- 排查确认：槽口网格重算后的 `length/width/angle/depth` 已写入 `ContourFeature`，但生成 G 代码时策略仍通过 UI 参数里的旧 `slotLength/slotWidth/angle/depth` 再次覆盖，导致刀路仍只按中心旧范围加工。
- 槽加工生成流程现在先把用户修改过的几何参数合并进 `ContourFeature`，再从传给策略的参数中移除几何字段，只保留安全高度、进给、步距、转速等工艺参数。
- 该修正保证网格重算后的完整槽口范围和实际方向能够真正进入盲槽/开口槽策略。

### 已完成：槽口方向网格 PCA 重算
- `refineSlotContourFromMeshData()` 不再直接使用原始识别角度作为槽长方向，而是先收集槽特征面片和附近槽口候选点，再通过 XY 主成分重新计算槽长轴。
- 附近点搜索仍使用原始角度作为粗过滤，实际槽长/槽宽、中心和斜面收缩量改为基于 PCA 后的本地坐标重新投影。
- 该修正用于解决识别角度偏差时，盲槽刀路只覆盖中心区域、长向槽口两侧未被纳入加工范围的问题。

### 已完成：盲槽粗加工长边自适应走刀
- 盲槽粗加工不再固定沿本地 `U` 方向切削、沿 `V` 方向排线，而是根据当前层有效加工范围自动选择长边作为主切削方向。
- 当槽长/槽宽命名或识别角度导致本地轴与实际长边不一致时，每层仍会优先沿较长范围走刀，并沿较短范围密集排线。
- 该修正用于解决斜面盲槽只清理中心区域、长向两端未被覆盖的问题。

## 2026-05-26

### 已完成：槽铣走刀顺序修正
- 开口槽粗加工改为每层连续 zigzag：第一条线下刀，后续相邻扫描线直接进给横移连接，最后统一退刀到安全高度，不再每条扫描线单独 `G0/G1 Z/G0` 进退刀。
- 开口槽最终层新增 U 形精加工路径：从开口侧引入，沿一侧壁走到封闭端，跨过封闭端，再沿另一侧壁退回开口侧退出。
- 盲槽最终精加工前新增安全抬刀和引导点重定位，避免从粗加工最后一条扫描线末端直接在槽底横扫到精加工引导点。

### 已完成：槽斜面网格重算兜底
- `refineSlotContourFromMeshData()` 不再只依赖槽特征自身 `faceIndices` 取样，新增槽中心附近的局部网格搜索窗口。
- 当斜面面片没有被识别进同一个槽特征时，仍可从附近网格顶点推导槽口/槽底范围，用于计算 `slopeStartLength/slopeEndLength/slopeMinWidth/slopeMaxWidth`。
- 该修正用于解决盲槽多层走刀中斜面参数为 0，导致各层扫描范围不随深度递减的问题。

### 已完成：盲槽斜面范围异常保护
- 收窄槽斜面局部网格搜索窗口，避免把槽外大平面误采样进槽口范围。
- 盲槽策略对长向/宽向斜面收缩量增加总量钳制，防止异常斜面参数把刀心加工范围压成负数。
- 当最终精加工范围仍异常时，先跳过精加工而不是中断整条槽粗加工 G 代码生成。

### 已完成：盲槽槽口/槽底几何定义修正
- 放宽槽底区域判定，支持底部较窄、带斜边的槽参与斜面参数计算。
- `refineSlotContourFromMeshData()` 输出的槽长/槽宽改为保持槽口范围，`slope*` 字段单独表达从槽口收缩到槽底的差值。
- 槽中心同步改为槽口包围盒中心，避免用槽底中心生成分层路径导致整体偏移。

### 已完成：盲槽小刀分层与入刀点修正
- 盲槽实际每层切深增加刀具直径限幅，避免 T31 这类小直径刀仍按参数 `stepDown=2` 一刀下到过深位置。
- 每层入刀点改为当前层有效加工范围中心，不再固定使用槽口中心，避免斜面槽在无效区域垂直扎刀。

### 已完成：盲槽横向排线步距限幅
- 盲槽粗加工横向扫描线距不再直接使用 UI 步距，而是按 `min(用户步距, 刀具直径 * 0.45)` 自动限幅。
- 该修正避免 T31 这类小刀仍按 1mm 横向步距排线，导致槽宽方向只切少数几条线、材料清理不足。

### 已完成：盲槽粗加工覆盖边界修正
- 盲槽粗加工刀心边界改为仅按刀具半径内缩，不再额外叠加 `stockToLeave`。
- 该修正避免 T31 小刀在 `余量=0.1` 时每侧少清 `0.15mm`，导致槽宽方向视觉上仍未覆盖完整。

### 已完成：盲槽宽向斜边递减与深度限幅
- `ContourFeature` 新增宽度方向斜边参数 `slopeMinWidth/slopeMaxWidth`，网格重算时从槽口范围和槽底范围的宽度差中提取。
- 盲槽粗加工每层现在同时按长向斜坡和宽向斜边计算本层有效刀心范围，支持侧边斜面随深度逐层递减。
- 盲槽加工深度增加限幅：参数中的加工深度不能超过识别到的槽深，避免有底面槽被人为参数打穿。

### 已完成：盲槽斜面分层范围修正
- 盲槽粗加工不再所有层使用同一个固定槽长范围，而是根据当前层深比例和 `slopeStartLength/slopeEndLength` 动态计算本层有效切削范围。
- 有入口/端部斜面的槽现在会在浅层覆盖更接近槽口的范围，随着层深增加逐步收缩到槽底平底范围，避免斜面区域漏加工或最终层过切。
- 最终精加工轮廓同步改为使用槽底有效范围，避免把槽口斜面长度当作底部成品轮廓直接精修。

### 已完成：盲槽默认取消自动螺旋下刀
- 修正盲槽策略中 `helixRadius=0` 仍会自动生成螺旋下刀的问题。
- 现在螺旋下刀半径为 `0` 时明确不使用螺旋，盲槽每层会直接下到当前层深，再按槽长和槽宽排线清槽。
- 只有用户显式设置 `helixRadius > 0` 时才启用螺旋下刀，避免默认刀路看起来仍是螺旋线而不是分层清槽。

### 已完成：盲槽每层宽度覆盖修正
- 修正盲槽粗加工宽度排线逻辑：不再简单从一侧按 `stepover` 递增到另一侧，避免槽宽小于步距时每层只走一条线。
- 每层粗加工现在强制包含两侧边界刀心线和中心线，并在中间按步距补线，保证每层都覆盖槽长和槽宽。
- 该调整符合“槽加工应一层一层往下，每层都清完整长宽范围”的加工逻辑。

### 已完成：盲槽粗精加工流程修正
- 盲槽策略调整为按层粗加工、最终层再执行轮廓精加工，避免每层都重复走成品轮廓。
- 修正 `G40`/CAM 侧补偿下的盲槽精加工中心线：精加工不再沿“粗加工余量后”的刀心范围空跑，而是按刀具半径偏置到成品槽壁。
- 该修正用于承接有底面槽默认走盲槽铣后的完整加工流程，避免留下余量未清和工序重复。

### 已完成：有底面槽策略路由修正
- 修正策略面板对槽特征的默认选择：只有识别为 `open_slot` 的槽才默认使用开口槽铣，`straight_slot`、`arc_slot`、`blind_slot` 等有底面槽默认使用盲槽铣。
- 取消槽特征切换时沿用上一槽策略的行为，避免刚加工过开口槽后再选有底面槽时仍保留 `mill_slot`，导致按通槽逻辑打穿模型。
- 开口槽策略和生成前校验新增保护：非 `open_slot` 特征不能使用开口槽铣，会提示改用盲槽铣。

### 已完成：开口槽改为按层水平切削
- 根据槽铣粗加工逻辑调整开口槽刀路：粗加工不再用 `G1 X/Y/Z` 从槽口斜穿到当前层深，而是先定位到该层在入口斜面上的合法切入点，再下到当前层 `Z`，随后沿该层水平切削。
- 侧壁精加工同步改为在平底起点下刀并水平精加工侧壁，避免侧壁精加工继续沿三轴斜线穿过模型。
- 清理 `SlotMillingStrategy` 中不再使用的三轴斜线输出辅助函数，后续斜面成形需要单独策略实现，不再混入粗加工主线。

### 已完成：开口槽入口斜坡穿模修正
- 回退入口进刀斜坡的强制拉长逻辑：刀具斜坡长度不再超过识别到的入口斜面范围，避免为了降低下刀角度而让 `G1 X/Y/Z` 直线穿过实体模型。
- 当前先保证刀路不脱离真实槽几何边界；斜坡过陡的问题后续通过减小斜面段层深、碰撞校验或专用斜面成形刀路处理。

### 已完成：开口槽入口进刀斜坡限深修正
- 修正开口槽粗加工入口斜坡过短导致下刀过深的问题：刀具进刀斜坡不再只按识别到的模型斜面长度比例计算，而是增加最小水平进刀距离约束。
- 粗加工每层会按当前层深保证足够的水平斜坡距离，避免 `Z-2.000/Z-4.000/Z-5.000` 在入口处过早到达。
- 侧壁精加工也复用该入口斜坡约束，避免最终层侧壁精加工沿过短斜面扎到槽底。

### 已完成：开口槽侧壁精加工斜坡进刀修正
- 修正侧壁精加工直接在槽底起点垂直下到最终深度的问题：侧壁精加工现在从开口端顶面进入，再沿入口斜面 `G1 X/Y/Z` 下到最终深度，随后沿侧壁直线精加工。
- 这样侧壁精加工与粗加工使用一致的开口斜面进刀逻辑，避免在斜面/平底交界处直接扎到 `Z-5.000`。
- 当前仍只覆盖平底有效长度范围内的两侧直壁，底面精加工和真实斜面成形继续放在后续步骤。

### 已完成：开口槽基础侧壁精加工
- 开口槽在最终层新增基础侧壁精加工，两侧直壁各补一条精加工线，先建立槽宽到尺寸的基本闭环。
- 当前版本只加工平底有效长度范围内的两侧直壁，不包含底面精加工和斜面成形；侧壁精加工暂按 CAM 侧刀心偏置实现，机床侧 `G41/G42` 的开口槽精加工仍保留到后续阶段。
- 这样可以在不扩大当前风险面的前提下，先把开口槽从“只有粗加工”推进到“粗加工 + 侧壁基础精加工”。

### 已完成：开口槽基础几何校验
- 开口槽生成前新增基础几何校验：如果粗加工有效长度无效，或两端斜面已经把平底区域压缩到极短范围，则直接阻止生成 G 代码。
- 该校验用于拦住明显异常的槽几何识别结果，避免继续输出“表面上能跑、实际斜面和平底边界已经失真”的刀路。
- 当前阈值采用最小 `0.10mm` 和 `0.1D` 二者取大值，作为平底有效长度下限。

### 已完成：开口槽斜面识别条件收紧
- 收紧槽底低 Z 区域判定条件：只有当低 Z 区域在槽宽方向覆盖足够完整时，才将其视为真实槽底区域并据此计算两端斜面长度。
- 这样可以减少局部三角面、倒角残片或不完整关联面把槽底误识别成窄条区域，进而把 `slopeStartLength/slopeEndLength` 拉长的问题。
- 当前规则对槽长方向和槽宽方向分别处理，优先保证槽宽识别稳定，再用长度差推导两端斜面范围。

### 已完成：开口槽单向粗加工退刀修正
- 开口槽无出口斜面时，粗加工线不再沿槽底反向退回入口斜面后再抬刀，改为在当前末端位置直接进给抬到槽口顶面，再快速抬到安全高度。
- 这样开口槽粗加工保持单向切削，减少槽底重复回切、多切和视觉上“路径没走完但其实是在回刀”的混淆。
- 本次调整属于 `P1` 阶段的粗加工闭环收口，只修改 `SlotMillingStrategy` 的开口槽单线收刀逻辑。

### 已完成：槽加工专项方案与阶段规划整理
- 在 `项目需求说明书.md` 新增“8.5 槽加工专项方案与阶段规划”，统一整理槽加工的识别层、几何层、粗加工层、精加工层和验证层。
- 文档中明确了当前已完成、进行中和未完成项，并给出后续 `P1/P2/P3/P4` 的推进顺序，后续槽加工开发将以该专项规划作为对照基线。
- 本次仅更新文档，不涉及代码逻辑和可执行文件变更。

### 已完成：开口槽斜面刀心深度补偿
- 修正开口槽入口斜面“刀路走得过深”的问题：槽几何里的斜面长度不再直接作为刀心斜坡长度使用，而是补上 `0.5D` 刀具半径补偿后再生成 `G1 X/Y/Z` 斜坡段。
- 这样最终深度到达点会沿槽长方向后移半个刀径，避免刀具前缘在斜面尚未走完时就提前吃到最终槽底深度。
- 该调整同时作用于起端斜面和末端斜面，当前先保持现有粗加工结构不变，只修正斜面中心轨迹与实际切削包络不一致的问题。

### 已完成：开口槽深度与退刀修正
- 修正槽网格重算把深度压成关联面 `meshDepth` 的问题：识别/参数已有槽深时优先保留该深度，网格深度只作为缺失深度兜底，避免只生成 `Z-1.600` 一层。
- 开口槽无出口斜面时，不再在闭口端原地升 Z；刀路会沿已加工平底退回入口斜面，再沿入口斜面回到顶面后快速抬刀，减少闭口端多切风险。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽粗加工路径 P2 重构
- `SlotMillingStrategy` 新增开口槽粗加工线生成函数，按“开口端进刀、斜面段、平底段、出口斜面/原地进给抬刀”组织单条切削线，避免在策略主流程中散落拼接路径。
- `SlotMachiningGeometry` 开始记录开口方向，开口端长向刀心边界只保留粗加工余量，闭口端仍保留刀具半径与余量，减少槽长加工不到位的问题。
- 斜面段按当前层深动态计算平底起止点，浅层不会直接按最终槽底斜面全长切削，最终层再完整覆盖斜面到槽底。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽网格重算逻辑迁入几何层 P1
- `SlotMachiningGeometry` 新增 `refineSlotContourFromMeshData(...)`，集中完成槽关联面投影、槽底低 Z 区域、实际深度和斜面长度识别。
- `MainWindow` 不再内联维护槽网格重算算法，只调用几何层函数，后续粗加工、精加工、校验和诊断可复用同一槽几何来源。
- 当前迁移保持既有识别结果输出不变，目标是先消除 UI 与策略层各自推导槽几何造成的偏差。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽加工几何层 P0 初版
- 新增 `SlotMachiningGeometry`，集中计算开口槽局部坐标、槽长/槽宽、顶面/深度、刀具半径与余量内缩、粗加工刀心边界、斜面起止段和平底段范围。
- `SlotMillingStrategy` 改为从 `SlotMachiningGeometry` 读取刀心边界和斜面段，不再在策略内部重复推导槽长、槽宽、角度、半径补偿和斜面兜底。
- 该版本先保持现有开口槽粗加工输出行为，目标是为后续真实斜面识别、精加工和过切检测建立统一几何入口。

### 已完成：开口槽斜面端 Z 插补刀路
- `ContourFeature` 新增 `slopeStartLength`、`slopeEndLength`，用于记录槽局部长度方向两端的斜面/倒角过渡长度，并同步工程保存/加载。
- `MainWindow` 在槽轮廓重算时，根据整体槽投影范围与槽底低 Z 区域的差值，自动计算两端斜面长度。
- `SlotMillingStrategy` 的开口槽粗加工线改为“起端斜坡段 + 平底段 + 末端斜坡段”：斜坡段使用 `G1 X/Y/Z` 同步插补，避免只加工平底直线而漏掉斜面区域。
- 针对部分 STEP 识别只关联槽直壁、没有把斜面面片纳入槽特征的问题，增加保底入口斜坡：当两端斜面长度都为 0 时，按槽长比例生成起端 Z 插补斜坡，避免继续垂直下到槽底。
- 修正斜面抬刀和长向刀具半径补偿：开口槽长向刀心边界按刀具半径加余量内缩；无出口斜面时，刀路会沿已加工平底退回入口斜坡并在槽口顶面抬刀，避免在槽内深处直接快速抬刀造成多切。
- 根据仿真反馈取消无出口斜面时的整槽反向退刀，改为在当前末端原地进给抬到顶面后再快速抬高，避免退刀路径重复切削槽底。

### 已完成：开口槽按槽底区域裁剪刀路
- 修正开口槽“线距正确但没有完全加工”的问题：槽轮廓重算不再直接使用所有关联面投影范围，而是优先使用槽底低 Z 点的投影边界作为可加工槽底区域。
- 取消 `SlotMillingStrategy` 里对开口槽两端统一固定缩短的斜面避让，避免非斜面端也被截短导致槽底加工不完整。
- 当前逻辑会把斜面/倒角面从粗加工矩形范围中剔除，策略只负责按刀具半径和余量限制刀心边界。
- 根据仿真反馈继续调整：槽宽仍按槽底低 Z 区域控制，槽长恢复使用整体槽投影范围；开口槽长向刀心边界不再额外扣半个刀径，避免长向加工不到位。

### 已完成：开口槽深度重算与斜面端避让
- 修正槽深被右侧默认参数覆盖的问题：槽几何从网格重算出实际 Z 深度后，只有用户手动修改深度时才覆盖，不再无条件把识别默认 `depth=5` 写回策略。
- `MainWindow` 在槽特征转加工轮廓时，会根据关联三角面的 `Z min/max` 重算当前 setup 下的加工顶面和实际下刀深度，避免把贯通/侧向跨度误当 Z 向槽深。
- `SlotMillingStrategy` 对开口槽长度方向加入斜面端避让，粗加工线两端收缩一个安全量，避免直接切入斜面过渡区。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽粗加工取消槽外长引入
- `SlotMillingStrategy` 的开口槽粗加工取消基于 `openSide` 的槽外长距离引入，避免生成 `Y-5.xxx` 这类明显远离槽体的刀路。
- 粗加工现在从槽内安全边界起刀，到另一侧安全边界结束，所有横向清宽线都限制在槽矩形安全范围内。
- 开口外引入后续需要等开口方向稳定识别后再作为可选进刀策略恢复。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽多线粗加工侧向越界收紧
- `SlotMillingStrategy` 的开口槽多线粗加工增加刀心侧向安全边界，粗加工横向极限至少预留 `0.25D`，避免仿真中侧向刀路贴边/越过槽壁。
- 多线偏移生成改为从中心线向两侧扩展，并按刀具直径限制有效步距，减少一次性跳到槽边导致的越界风险。
- 该调整先保证粗加工安全，侧壁到尺寸后续应通过独立精加工/刀补线处理。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽按槽宽生成多条切削线
- `SlotMillingStrategy` 的开口槽粗加工不再只生成单条中心线；现在会根据槽宽、刀具直径和步距生成多条平行切削线，覆盖槽宽方向。
- 每条切削线仍从开口端外侧进刀，再沿槽长方向切到封闭端，保留开口槽从槽口进刀的加工意图。
- 当用户设置的步距大于槽宽时，会自动按刀具直径限制有效步距，避免又退化成一条中心线。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

## 2026-05-25

### 已完成：开口槽 XY 刀路按关联面投影重算
- 修正开口槽默认刀路仍对不上槽的问题：生成槽加工前不再直接信任识别阶段的槽中心/长宽估算，而是从槽特征 `faceIndices` 对应的网格三角面重新投影到当前 setup 的 XY 平面，按实际投影边界重算 `center`、`length`、`width` 和 `angle`。
- 右侧参数表的默认槽长/槽宽/角度不再覆盖这次重算结果；只有用户把参数改成不同于识别默认值时，才显式覆盖重算几何。
- 批量勾选多个槽时，每个槽都会用各自关联面重算几何，避免套用当前槽的旧中心线估算。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽加工顶面 Z 修正
- 修正识别槽进入加工策略时的 Z 基准：槽识别给出的 `center.z` 是槽几何中心，开槽策略需要的是槽口顶面 Z；现在槽特征转换为 `ContourFeature` 时会使用 `center.z + depth / 2` 作为加工顶面。
- 覆盖右侧策略面板直接生成、主窗口批量/加入工序转换、旧铣削弹窗预填三个入口，避免继续生成 `G0 Z-0.100` 这类贴近/进入工件的进给起始高度。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：设置加工面后按新 setup 重新识别特征
- `StepImporter` 新增带旋转的 STEP 重载入口，设置加工面时先把原始 STEP 形体变换到新 setup 坐标，再重新生成网格并重新跑特征识别，避免继续复用/旋转旧特征导致槽方向、面归属和加工侧判断出错。
- `ProjectManager` 开始保存原始 STEP 路径；`AppController` 新增 setup 重识别流程，重识别成功后会刷新项目网格和特征，并清空旧工序、旧刀路和旧 G 代码。
- `MainWindow` 的“设置正面”确认链路改为 OCC 主路径下重新导入识别，非 OCC 构建才保留旧的旋转网格/旋转特征 fallback。
- `FeatureListPanel` 的特征分组顺序改为正面优先，其次侧面、反面、未知，让设置加工面后的当前加工面特征优先显示和选择。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽按开口端进刀，不再按闭口矩形收刀
- `ContourFeature` 新增 `openSide`，`MainWindow` 会在把槽特征送入策略前，结合当前 setup 下的网格包围边界推断开口槽的槽口方向，保证“设置正面”后仍能识别哪一端是开口端。
- `SlotMillingStrategy` 的 `open_slot` 生成逻辑改为按槽口侧进刀：粗加工每刀都从槽口外侧切入，再沿槽长切到封闭端；精加工改为 `U` 形轮廓，不再像闭口槽那样补出整圈矩形把槽口封死。
- 修正识别为 `straight_slot` 但端部实际贴模型边界的槽：这类槽现在也会通过 `openSide` 进入开口槽刀路分支，避免继续按普通闭口槽生成螺旋下刀。
- `MainWindow` 的槽生成入口增加最近槽匹配兜底：即使左侧高亮行停在平面特征，右侧面板当前槽也会按中心点回找项目槽特征并重新补上 `openSide`。
- `mill_slot` 策略已收紧为固定开口槽刀路，不再保留普通闭口槽的螺旋下刀分支；闭口/盲槽后续统一走 `mill_blind_slot`。
- 开口槽粗加工进一步收敛为中心线开槽：每层只从槽口外侧进到槽中心线，再沿槽长切到封闭端；暂时取消开口槽侧壁轮廓精加工，避免继续生成偏到工件下方的矩形/折线路径。
- 修正 setup 切换后的槽方向角来源：不再用重分类时的面组 PCA 覆盖槽角度，而是旋转原始槽长方向向量，避免槽长方向被误算成 `-90°` 后导致刀路沿错误轴移动。
- 这一轮额外补入了槽策略的 `compensation` 参数，为后续刀具半径补偿继续落地保留接口；`SimulationController` 也预留了撞刀检测信号和刀具几何字段，但这两项还没有形成完整闭环。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽流程几何参数贯通与生成校验收口
- `ContourFeature` 新增 `width`、`length`、`angle` 字段，槽特征从识别结果进入铣削对话框、工序列表、工程保存/加载时不再只剩中心点和半径。
- `MillingOperationDialog` 生成槽工序时会把识别到的槽长、槽宽和方向角写入 `ContourFeature`，`ProjectManager` 同步补齐这些字段的序列化与反序列化。
- `MainWindow` 的普通铣削生成链路新增槽专用前置校验；“生成全部”遇到无效槽工序时会直接阻止继续生成，避免混入错误 G 代码。
- 已通过 `qmake untitled.pro -spec win32-g++ CONFIG+=debug CONFIG+=qml_debug` 与 `mingw32-make -f Makefile.Debug` 编译验证，`untitled/debug/CNEXT-CAM.exe` 成功生成。

### 已完成：开口槽/盲槽策略与工序显示收口
- 重写 `SlotMillingStrategy` 与 `BlindSlotMillingStrategy`，统一按槽长、槽宽、角度和层深生成刀路，槽策略不再依赖缺省的固定 50x10 尺寸。
- 开口槽与盲槽策略都补上了可读的英文显示名和错误提示，避免继续显示历史乱码文本。
- `OperationListPanel` 重写工序显示文案，槽工序改为显示 `L/W/Z` 关键尺寸，封闭/开放轮廓、槽、面铣等类型名称也改为可读文本。

### 已完成：槽批量建工序与槽相关界面文案清理
- `MainWindow` 的铣削生成链路新增“多槽批量”行为：当用户勾选多个槽特征并选择槽策略时，会按同一套刀具和参数为所有勾选槽一起生成 G 代码并批量加入工序列表。
- `FeatureListPanel` 与 `StrategyPanel` 相关槽界面文案改为可读中文/英文，当前选中槽会显示槽长、槽宽和深度，不再保留本轮路径上的乱码提示。
- 这次调整继续通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽专用右侧参数面板与参数覆盖贯通
- `StrategyPanel` 改为按当前特征上下文切换策略集：选中槽时，右侧直接显示开口槽/盲槽策略、刀具和参数表，不再强制走 `MillingOperationDialog` 弹窗。
- 槽右侧参数表补入 `depth`，并把 `depth/slotLength/slotWidth/angle` 真正覆盖到 `ContourFeature`，保证“右侧改参数 -> 生成 G 代码/加入工序”不是只改显示值。
- `MainWindow` 的槽批量生成链路同步套用这些覆盖参数，勾选多个槽时，批量生成、工序加入和后续“生成全部”看到的是同一份槽几何快照。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽点选误切回孔的选择链修正
- `MainWindow` 新增统一的特征面板切换辅助逻辑，避免把非孔特征继续送进 `StrategyPanel::setFeature(...)`，右侧策略栏不再把槽错误显示成钻孔。
- 视口点选命中槽/平面等非孔特征时，当前行为改为优先选中实际命中的特征，不再因为附近有孔就自动吸附回孔特征。
- 勾选多个特征时，若当前高亮特征仍在勾选集合内，右侧策略栏优先保持当前选中对象，不再无条件回退到“第一个孔优先”。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：以选中面作为当前 setup 的正面切换
- `MainWindow` 的“设置正面”不再只是按法向重分类孔/面/槽，而是把当前项目网格和特征整体旋转到新的 setup 坐标，让现有基于 `XY/Z` 的孔加工和 2.5D 槽/轮廓策略可以直接沿所选面法向加工。
- 切换 setup 时会同步重算特征 `region`、平面子类型和槽方向角，并清空旧工序、旧刀路和 G 代码缓存，避免把旧装夹下生成的数据继续用于新装夹。
- `onStepImported()` 现在会把项目网格同时同步给仿真控制器，保证导入模型和后续 setup 切换后，仿真基准网格与视口保持一致。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 当前残留
- `StrategyPanel.cpp`、`FeatureListPanel.cpp`、`MainWindow.cpp` 等文件仍有历史乱码文案，本次只处理了槽闭环直接相关路径，未做整库文本清洗。
- 槽流程目前已完成“识别 -> 右侧改参数 -> 批量建工序 -> 生成全部 -> 保存/加载”的闭环，但还没有对圆弧槽细分专用走刀，也没有按槽类型拆开的更细参数模板。

## 2026-05-22

### 已完成：槽加工流程 UI 闭环第一步
- `StrategyPanel` 新增 `clearFeature()`，切到槽、型腔等非孔特征时会清掉旧的孔状态，避免策略面板继续保留上一次孔加工的上下文。
- `MainWindow` 的特征选择/勾选联动已补齐槽分支：勾选集合里没有孔但有槽时，会优先把槽送入铣削入口，不再只对孔特征生效。
- `OperationListPanel` 的槽工序摘要改为按 `L/W/Z` 展示，`mill_slot` / `mill_blind_slot` 不再沿用通用轮廓的 `R/Z` 摘要，便于区分开槽与普通轮廓铣。
- 待验证：重新编译并用槽特征走一遍“识别 -> 添加工序 -> 生成 G 代码”的 UI 链路。

### 已完成：孔识别朝向阈值回调
- `FeatureRecognizer::findHoles()` 将孔圆柱面的朝向过滤从“必须明显朝内”放宽为“只拦截明显外侧且既不像孔也不像贯通孔的圆柱”，避免因法向/网格噪声把正常孔漏掉。
- 这次调整只影响孔识别的入口筛选，不改孔的孔口/盲孔/螺纹分类逻辑。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：外部槽误识别边界收紧
- `FeatureRecognizer::findSlots()` 新增模型外包络边界判定，槽候选如果贴着整体模型的投影极值，就不再当作槽底或开口槽墙组处理，避免把外表面、背面和侧面拼成一个“外部槽”。
- 该收紧只作用于槽候选的边界判定，不改动已识别的内槽/盲槽的主流程。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：槽底深度回退修正
- `FeatureRecognizer::findSlots()` 的槽底深度计算新增回退：当“平行邻面”法算不出有效深度时，改用侧壁沿槽轴的几何跨度兜底，避免像 `槽.stp` 这类真正的槽因为底面没有对应平行面而被漏识别。
- 这次修正只作用于槽候选的深度判定，不放宽底面/侧壁/长宽比的前置约束，避免把整张平面重新误识别成槽。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：开口槽第二套识别
- `FeatureRecognizer::findSlots()` 在原有“按槽底识别”的流程后，新增一套“按 4 个凹边连通内壁识别开口槽”的补充分支，用于识别没有独立槽底面的贯通/开口槽。
- 新增墙面组几何校验：必须能分成两组相对平行且彼此近似正交的内壁，长宽比满足槽特征，并且不存在可作为底面的共用平面邻面，避免把普通台阶侧壁误判成槽。
- `MillingOperationDialog` 对 `open_slot` 改为默认选择 `mill_slot`，不再一律按盲槽策略处理。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：孔外圆柱误判过滤与槽侧壁收敛
- `FeatureRecognizer::findHoles()` 增加圆柱法向朝向校验，只保留法向朝向轴心的内孔圆柱面，外圆角/外圆柱面不再直接进入孔识别。
- 孔识别不再把与孔轴垂直的大开口平面并入 `feature.faceIndices`，避免点选孔特征时把整张外表面一起高亮成孔。
- `FeatureRecognizer::findSlots()` 新增槽侧壁筛选，只保留贴近槽底两侧边的侧壁参与槽判定；同时把 `processedBottoms` 延后到槽真正识别成功后再消耗，避免候选过早被跳过。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：槽大平面误识别过滤与中文主界面恢复
- `FeatureRecognizer` 的平面包围盒改为按主方向重新计算长宽和方向，并新增槽侧壁贴边校验；只有侧壁真正贴近槽底两侧边时才保留槽候选，避免“大平面 + 局部凹边”被整面识别成槽。
- `MainWindow::retranslateUi()` 恢复按当前语言输出主菜单、工具栏、仿真按钮和停靠面板标题，中文模式不再统一回退成英文。
- `SlotMillingStrategy.cpp` 与 `BlindSlotMillingStrategy.cpp` 的槽工序错误提示恢复为中文可读文本。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：槽识别按共面底面组消耗
- `FeatureRecognizer::recognize()` 里槽特征不再只把 `slot.faceIndices.first()` 记入 `consumedBottomFaces`，改为按 `collectCoplanarFaces(...)` 一次消耗整组共面底面，避免同一槽被后续流程拆开。
- 清理了 `FeatureRecognizer.cpp` 中遗留的重复 `findSlots()` 声明，并修复了 `reclassifyRegions()` 里被乱码注释吞掉的控制流，保证槽识别模块可继续编译。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：反面孔生成禁用与批量按参数分批
- `MainWindow` 新增孔位装夹限制校验，当前单一正面 Z 向流程下，侧面孔和反面孔都会在生成前被阻止，避免直接输出错误刀路。
- `generateRequested` 和 `generateAllRequested` 统一复用同一套孔位校验逻辑，单孔生成和“生成全部”不再出现校验口径不一致。
- `generateAllRequested` 的孔工序分批条件从“同策略 + 同刀具”收紧为“同策略 + 同刀具 + 同参数”，避免不同参数的孔被错误合并为同一批次输出。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：工程状态检查与后续开发排期梳理
- 确认实际 Qt/C++ 工程位于 `untitled/`，根目录为文档与工作区总目录，当前根目录不是 Git 仓库。
- 确认项目进度规则：每次功能新增、功能删除、修复问题、行为调整或构建/部署规则变更，都必须在 `项目进度.md` 当日节追加记录；后续以根目录 `项目进度.md` 作为总账。
- 检查主要模块：STEP/OCC 导入、AAG 特征识别、孔/铣削策略、Siemens/Fanuc 后处理、仿真、刀具库、工序列表、CNC 串口接口均已有源码入口。
- 编译验证通过：已执行 `qmake` 与 `mingw32-make -f Makefile.Debug`，生成 `untitled/debug/CNEXT-CAM.exe`；当前警告为 OpenCAMLib vendored source 缺失，OCL 支持禁用。
- 风险记录：`untitled/设计方案.md` 和部分源码用户提示文本仍存在历史乱码，需要作为近期质量任务处理。

### 后续开发进度安排
- P0：进度记录规范化。收敛根目录与 `untitled/项目进度.md` 两份进度记录，明确唯一总账；新增/删除功能时同步写明改动文件、行为变化、验证命令和残留风险。
- P1：编码与中文提示清理。修复 `设计方案.md`、`CncCommInterface.cpp`、`SurfaceFinishStrategy.cpp` 等乱码文本，统一文档和源码编码为 UTF-8。
- P2：高频铣削闭环。优先完善封闭/开放轮廓、直槽/盲槽/圆弧槽、铣平面从“识别特征 -> 自动带入参数 -> 生成 G 代码 -> 仿真显示”的完整链路。
- P3：坐标系与多加工面。实现正面/反面/侧面装夹坐标变换或明确禁用生成逻辑，避免反面/侧面特征直接输出错误 Z 向刀路。
- P4：工程文件与刀具库持久化。补齐 `.cnext` 保存模型路径、特征、工序、策略参数、刀具、后处理器和 G 代码；刀具库独立 JSON 持久化。
- P5：后处理与仿真增强。完善 Fanuc 输出选择、G 代码高亮、工序手动排序、仿真圆弧/循环覆盖验证和基础碰撞/过切检查。
- P6：高级策略。按需求补齐铣螺纹、圆锥、清角、动态铣、3D 边线铣、3D 选面铣、毛坯/库存模型和加工边界。

### 已完成：圆插补铣孔改为连续螺旋下刀
- `HoleCircularMillingStrategy` 从“每层下到底后走整圆”的台阶式刀路改为连续螺旋刀路，`G2` 现在携带 `Z` 轴连续下降。
- 螺旋节距继续复用 `stepDown` 参数，含义调整为“每圈下刀量”；到底后仍保留底部精修圈 `springPasses`。
- `HoleCircularMillingStrategy` 的显示名和错误提示改为可读中文。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：槽开槽流程接入与编译收口
- `MainWindow.cpp` 接入槽工序校验，槽工序在生成前会检查槽长、槽宽、刀具直径和切深，批量生成时也会继续按槽工序类型拦截无效参数。
- `SlotMillingStrategy.cpp` 与 `BlindSlotMillingStrategy.cpp` 补齐槽参数校验，去掉了会导致编译失败的历史乱码提示文本。
- 删除了孔生成路径里误加的槽校验分支，圆插补孔流程不再误走槽限制判断。
- `MainWindow.cpp` 的 `retranslateUi()` 残留断行已清理，相关提示文本统一改为可编译的 ASCII 文本。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证，`debug/CNEXT-CAM.exe` 成功生成。

## 2026-05-20

### 已完成：圆插补铣孔改为连续螺旋下刀
- `HoleCircularMillingStrategy` 从“每层下到底后走整圆”的台阶式刀路改为连续螺旋刀路，`G2` 现在携带 `Z` 轴连续下降。
- 螺旋节距继续复用 `stepDown` 参数，含义调整为“每圈下刀量”；到底后仍保留底部精修圈 `springPasses`。
- `HoleCircularMillingStrategy` 的显示名和错误提示改为可读中文。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：孔倒角圆弧刀路与点钻刀具类型
- `ChamferStrategy` 改为孔口圆弧倒角刀路：倒角刀下到倒角深度后沿孔口半径 `G2` 绕孔一圈，不再只在孔中心下压。
- `ToolLibrary` 将默认中心钻归类为 `spot_drill`，避免孔定点默认选择麻花钻。
- `StrategyPanel` 的孔定点策略优先筛选中心钻，排屑钻/深孔钻避免选择大于目标孔径的钻头。
- `ToolLibraryPanel` 修复刀具类型和标题中文乱码。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：孔收尾倒角刀具约束与中文修复
- `ToolLibrary` 默认刀具库改为正常中文名称，并新增默认倒角刀 `D6/D10/D12 90°`。
- `StrategyPanel` 修复当前加工策略区域的中文乱码，孔倒角策略会自动过滤为倒角刀，不再默认显示麻花钻。
- `ChamferStrategy` 增加刀具类型校验，孔倒角/去毛刺必须使用 `chamfer_mill`，避免生成用麻花钻下压的错误收尾 G 代码。
- `StrategyBase` 默认错误提示改为中文可读文本。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：仿真圆弧插补与刀具模型
- `SimulationController` 的 G 代码解析补齐 `G2/G3` 圆弧插补离散，圆弧铣孔不再在仿真中退化为直线或漏画完整圆。
- 仿真解析会跟踪 `T` 刀具号，并从刀具库读取当前刀具直径，随每段运动传递给视口。
- `ViewportWidget` 新增简化刀具模型显示，按当前刀具直径绘制刀具圆柱和刀柄，替代单一黄色十字光标。
- `MainWindow` 接入仿真刀具直径信号，播放/停止时刀具模型与当前位置、刀具号保持同步。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

## 2026-05-18

### 已完成：孔加工最近邻排序
- `HoleStrategyUtils.h` 新增 `sortHolesByNearestNeighbor()`，贪心最近邻启发式在 XY 平面对孔位排序，减少空程移动距离。
- `MainWindow.cpp` 引入 `HoleStrategyUtils.h`，批量孔生成前调用排序，避免按特征列表索引顺序加工导致走刀迂回。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证（全部 .o 目标编译通过）。

### 已完成：孔定点需求替换与视频功能审查
- 审查 `D:\桌面\仰高科技` 目录下 26 个教学视频缩略画面，覆盖孔加工、铣削、特种加工、3D 加工和绘图流程。
- 重写 `项目需求说明书.md` 中 `3.3.1 孔定点加工`，将其明确为中心定位/点钻工序。
- 新增 `7. 视频审查补充需求`，补充默认参数、排屑钻、深孔钻、攻牙、铰孔、铣边、铣槽、铣平面、圆锥、铣螺纹、清角、动态铣、3D 边线铣、3D 选面铣、画图、中心线等需求。

### 已完成：项目进度文件编码清理
- 根目录 `项目进度.md` 清理为 UTF-8 中文文本。
- 保留当前关键进展记录，避免继续追加乱码。

### 已完成：视频功能与当前实现查漏补缺
- 对照视频、当前需求说明书和 `untitled/src` 实际实现，梳理出未实现或未闭环功能清单。
- 在 `项目需求说明书.md` 新增 `8. 视频功能与当前实现查漏补缺清单（2026-05-18）`。
- 清单覆盖孔加工批量闭环、封闭/开放轮廓铣、槽加工、铣平面、圆锥、铣螺纹、清角、动态铣、3D 边线铣、3D 选面铣、画图、中心线、默认参数、工程保存、后处理、仿真校验、毛坯库存模型、探测找正等功能缺口。

### 已完成：孔定点批量加工第一版
- `SpotDrillingStrategy` 新增批量孔生成接口，一次输出多个孔位的中心钻刀路，避免每个孔重复换刀和主轴启动块。
- `MainWindow` 生成 G 代码时会读取特征栏已勾选孔位；未勾选时回退到当前选中的孔特征。
- 排屑钻、深孔钻、攻牙、铰孔、孔倒角也接入同一勾选孔集合，先形成批量生成闭环。
- `HoleStrategyUtils` 修正孔口 Z 计算，孔定点深度从真实孔口向下计算，不再从孔圆柱中心直接下刀。
- `StrategyBase` 默认批量接口和默认错误提示改为中文，减少界面英文残留。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：铣削策略 UI 入口第一版
- 新增 `MillingOperationDialog`，可从界面选择铣平面、型腔开粗、轮廓精加工、曲面精加工 4 种已注册铣削策略。
- `StrategyPanel` 的铣削分组新增“添加铣削工序”入口，支持填写中心、顶面 Z、半径/半宽、加工深度和策略参数。
- `MainWindow` 接入铣削工序信号，生成铣削 G 代码并同步加入 `OperationListPanel`。
- `OperationListPanel` 的铣削工序标签改为显示策略名、尺寸和深度，开粗/精加工策略按工序类型归类。
- `ToolLibrary` 新增默认立铣刀/面铣刀，避免铣削入口只能选到钻孔刀具。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：开孔 G 代码固定循环化
- `SpotDrillingStrategy` 改为输出 `G81/G82` 孔定点固定循环，支持批量孔位排序后连续输出 XY。
- `PeckDrillingStrategy` 改为输出 `G83` 排屑钻固定循环，单孔和多孔不再展开为多段 `G0/G1` 手写下刀。
- `DeepHoleDrillingStrategy` 改为输出 `G83` 深孔钻固定循环，并移除 UI 中不再生效的局部退刀参数。
- `StrategyPanel` 增加策略参数缓存，切换开孔策略后保留用户改过的参数。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：Siemens 840D 孔循环后处理
- 开孔策略内部改为输出 `CNEXT_HOLE_CYCLE` 元数据，保留孔口参考平面、返回平面、安全距离、孔底、步距和进给。
- `SiemensPostProcessor` 读取孔循环元数据并转换为 `MCALL CYCLE81/CYCLE82/CYCLE83`，不再在 Siemens 840D 后处理下直接输出 Fanuc 风格 `G81/G83`。
- 多孔且循环参数一致时，后处理器只输出一次 `MCALL CYCLE...`，后续连续输出孔位 `X/Y`，最后用 `MCALL` 取消循环。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：攻丝/铰孔批量与仿真展开
- `TappingStrategy` 新增 `QVector<HoleFeature>` 批量接口，批量孔位会按最近邻排序并输出攻丝循环元数据。
- `ReamingStrategy` 新增 `QVector<HoleFeature>` 批量接口，批量孔位会按最近邻排序并输出铰孔循环元数据。
- `SiemensPostProcessor` 新增 `CYCLE84` 攻丝和 `CYCLE85` 铰孔输出，不再直接输出通用 `G84/G85`。
- `SimulationController` 的 Siemens 循环解析扩展到 `CYCLE84/CYCLE85`，攻丝/铰孔在仿真路径里会展开进刀和退刀动作。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：阶段一 UI 补齐
- `BottomBar` 增加仿真速度下拉框，支持 0.25x、0.5x、1x、2x、5x、10x。
- `MainWindow` 将速度下拉框接到 `SimulationController::setSpeed()`，仿真速度 UI 形成闭环。
- `StrategyPanel` 增加“编辑开孔参数”按钮，弹窗编辑当前孔策略参数并同步回参数表。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：型腔开粗行切路径
- `PocketRoughingStrategy` 从占位式方形同心框线改为分层往复行切清腔。
- 矩形型腔按半宽生成完整行切；圆形型腔按圆边界裁剪每条扫描线，刀路不会越过有效圆边界。
- 同步修复该策略文件内的中文显示乱码，错误提示和策略名称恢复为中文。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：轮廓精加工引入/引出
- `ContourFinishStrategy` 增加直线引入和引出，刀补 `G41/G42` 在引入线前开启，离开轮廓后再 `G40` 取消。
- 圆形轮廓支持每层切深后整圆精加工；点列轮廓支持开放/封闭点列加工。
- 新增 `leadLength` 参数用于控制引入长度，并将策略名称、错误提示恢复为中文。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：工序列表刀具同步修复
- `OperationListPanel` 新增“用当前刀具”按钮，可将右侧策略面板当前刀具应用到选中工序；未选中时应用到全部工序。
- 批量孔生成后，工序列表会把本次参与生成的每一个孔都加入列表，不再只记录当前单个孔。
- `StrategyPanel::currentTool()` 调整为公开只读接口，供工序列表同步当前刀具使用。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：孔径与刀具匹配校验
- 新增孔径/刀具直径辅助校验，排屑钻和深孔钻在刀具明显小于目标孔径时输出底孔加工提示注释。
- 铰孔策略增加硬校验：铰刀直径必须接近目标孔径，避免用错误刀具生成成品孔代码。
- 攻丝策略增加螺距和刀具直径匹配校验，避免错误丝锥加工非匹配螺纹孔。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：圆插补铣孔策略
- 新增 `HoleCircularMillingStrategy`，作为 `hole_circular_mill` 开孔策略注册到策略工厂。
- 可直接对识别到的孔特征生成圆插补铣孔 G 代码，自动按目标孔径和刀具直径计算刀心圆半径。
- 支持分层切深、进给/下刀速度、余量和光刀圈数参数，适合将钻后的底孔铣到成品孔径。
- `StrategyPanel` 选择该策略时自动筛选立铣刀。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：圆插补铣孔下刀修正
- 圆插补铣孔不再在最终刀心圆位置垂直下刀，避免小立铣刀直接扎入未加工环形材料。
- 每层改为先回孔中心，在已钻底孔区域下刀，再径向进给到刀心圆并执行整圆插补。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：换刀与圆插补深度修正
- `SiemensPostProcessor` 在第二次及之后的 `T... M6` 换刀前自动插入 `M5`，避免主轴未停直接换刀。
- 圆插补铣孔不再默认沿用钻孔 1mm 穿透量，默认加工到识别孔深；新增 `bottomAllowance` 参数，需要穿透时再显式设置。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：五步工艺流程阶段化
- `MachiningOperation` 新增 `OperationStage`，覆盖对刀/准备、粗切、精修、深孔、收尾五个阶段。
- `OperationListPanel` 新增“阶段”列，并提供“按流程排序”按钮，可将工序按五步流程自动整理。
- 新增工序时会按策略自动推断阶段：点钻归入准备，型腔开粗归入粗切，圆插补/铰孔归入精修，排屑/深孔归入深孔，倒角归入收尾。
- `ProjectManager` 保存/加载工序时同步序列化阶段信息。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：加工策略提示与孔流程修正
- `StrategyPanel` 在加工策略选择区域新增动态工艺提示，说明当前策略所属阶段、用途和注意事项。
- 孔定点策略不再自动套用特征孔深，保持浅定位深度，避免生成 `CYCLE81` 钻穿孔。
- “按流程排序”顺序调整为准备、粗切、深孔/钻孔、精修、收尾，圆插补精修不再排到钻孔前。
- 生成全部 G 代码时，同一孔若同时存在排屑钻和深孔钻，会跳过排屑钻，保留深孔钻，避免重复钻孔。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

## 2026-05-15

### 阶段一：开孔加工最小闭环推进
- STEP 导入、孔识别、特征列表、策略面板、G 代码编辑器、刀路仿真已形成基础链路。
- 特征列表支持勾选确认，模型点选和列表选择可联动高亮。
- 生成 G 代码前增加孔特征、刀具、几何有效性校验。

### 阶段一：识别与显示修复
- 修复通孔被识别为凸台的问题。
- 修复孔口点选后策略面板记录为平面导致无法生成开孔 G 代码的问题。
- 修复孔特征高亮包含整张大平面的问题。
- 正面、反面、侧面分组和同类特征折叠已接入特征列表。

## 2026-05-14

### STEP 特征识别基础架构
- 将 `HoleFeature` 扩展为通用 `MachiningFeature`。
- 新增 `FeatureKind`，覆盖孔、槽、型腔、凸台、倒角、圆角、平面、螺纹孔。
- 拆出 `TopoAnalyzer`，构建 AAG 邻接属性图。
- `FeatureRecognizer` 从 `StepImporter` 中拆出，开始基于 AAG 做模式匹配。
- `FeatureClassifier` 负责孔类型细分。

### 运行环境与部署
- 补齐 Debug 目录运行所需的 OpenCascade / Qt / MSYS2 DLL。
- 修复双击 EXE 缺失 DLL 的问题。
- 启用软件 OpenGL 以规避部分 OpenGL 启动异常。
