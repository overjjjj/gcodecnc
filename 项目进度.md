# CNEXT-CAM 项目进度

> 规则：每次修改代码、修复问题或新增功能，都必须在本文件追加记录。本文档已统一为 UTF-8。

## 2026-08-12

### 已完成：验收模型孔识别到 CQ8 接口 G 代码闭环
- 修复 OpenCASCADE `REVERSED` 圆柱面的法向判断，并将同轴、同半径、同轴向范围的分片圆柱面合并计算圆周角；只有合并后接近完整圆周的内向圆柱才进入孔候选，避免单个半圆曲面被误判为孔或凸台。
- 验收模型现识别出 176 个孔候选和 62 个螺纹孔候选；其中位于正面 `X-60 Y305` 的 `D21` 通孔可被稳定定位。该数量仍属于几何候选结果，必须由操作员核对孔型、尺寸及工艺后确认正式工序。
- 新增真实 STEP 端到端回归：从验收模型识别上述 `D21` 通孔，显式创建已确认的深孔钻工序，经 `ProgramGenerationService` 和 CQ8 后处理接口生成 `G98 G83 ... X-60.000 Y305.000`，并校验 `G80`、主轴/冷却关闭及 `M30`。
- CQ8 在本阶段仅作为程序输出接口使用，没有增加网口通讯、实时控制或自动下发逻辑；操作员人工确认工艺的边界保持不变。
- 完整自动测试集和 Release 构建均已通过。下一步优先收紧螺纹孔、沉头孔的启发式分类，减少误分类后再扩展同类孔批量程序生成。

### 已完成：验收模型侧向槽不再混入正面加工
- 将 `WH250852-模板紅色面加工（零件3）.STEP` 纳入仓库验收夹具，并以 SHA-256 固定模型版本；模型可由 OpenCASCADE 稳定导入，包围范围约为 `1390 × 1320 × 50 mm`。
- 修复槽区域判定：当槽加工轴与当前 Setup 正面法向夹角不满足正面加工条件时，直接归入 `Side`，不再因为关联面触及正面边界而错误归入 `Front`。
- 验收模型中 4 个已知水平轴槽继续保留为侧向特征，不会进入当前正面工艺选择及 G 代码生成；避免把 `120.906/659.718 mm` 等侧向长度误当成正面切深。
- 新增 `acceptance_step_model_test` 并加入完整测试脚本，校验模型指纹、厚度、侧向槽保留及正面槽深度边界；完整自动测试与 Release 构建均通过。
- 下一阻塞项是验收模型圆柱凹面目前被大量归为 `Boss`，需建立孔/凸台拓扑方向回归后修复，再选择首批孔生成正式 G 代码。

### 已完成：模型到 G 代码链路测试基线恢复
- 修复 `program_generation_service_test` 的链接源清单，补入服务直接依赖的 CQ8 后处理接口实现、Fanuc 包装基类、宏程序构建器和模态优化器；不包含 CQ8 实时控制或网络通讯开发。
- 将型腔侧壁和底面精加工测试正式加入完整测试脚本，避免旋转、独立余量和安全下刀回归只在手工单测中运行。
- 增加构建管线契约，若程序生成服务再次漏链依赖或型腔精加工测试从全量脚本脱落，会立即失败。
- 完整自动测试集现已全部通过，包括真实 STEP 孔特征夹具、工序确认、程序生成、安全检查、快照、项目保存和重新加载。
- 已实测验收模型可导入，但当前识别出 2039 个候选；正面 4 个槽候选中存在深度超过 50 mm 模型厚度的异常，孔形圆柱被归入凸台候选。下一步优先修复圆柱凹凸分类和槽深度边界，再建立该模型的正式端到端 G 代码验收。

### 已完成：型腔侧壁余量与底面余量独立确认
- 型腔侧壁、底面精加工默认参数由单一 `stockToLeave` 拆分为 `sideStockToLeave` 和 `floorStockToLeave`，操作员可分别控制 XY 刀心边界和最终 Z 深度。
- 侧壁精加工按底面余量限制最深分层；底面精加工按底面余量确定最终切削 Z，两项策略均只用侧壁余量缩小矩形或圆形刀心范围。
- 底面余量达到或超过型腔深度时阻止生成，负余量继续被安全门拒绝。
- 参数编辑器和中英文工艺提示已显示“侧壁余量/底面余量”；旧项目只有 `stockToLeave` 时自动回退为两方向相同余量，保持历史工序语义。
- 新增分离余量、超深拒绝和旧参数兼容回归，生成结果继续通过 G 代码安全门。

### 已完成：旋转矩形型腔精加工统一使用局部坐标
- 矩形型腔侧壁精加工现按 `feature.angle` 将局部 U/V 刀心轮廓转换到世界 XY，四条侧壁不再错误地保持轴对齐。
- 矩形型腔底面精加工的往复扫描线使用同一角度转换；圆形型腔行为保持不变。
- 继续采用已粗加工型腔中心作为安全下刀点，进给连接到首个切削点；旋转前后的刀心范围均扣除了刀具半径和人工确认余量。
- 新增 90° 旋转侧壁与底面 G 代码回归，并通过独立策略测试和 G 代码安全门。

## 2026-08-10

### 已完成：孔分类组、组级批量确认与孔心原点
- 识别特征树中的孔不再只归入一个“孔”节点，现按孔型、直径、深度、沉头大径和螺距形成稳定分类组，例如“通孔 D10 深12”和“螺纹孔 D8.5 深16 螺距1.5”。
- 孔分类组增加组级复选框，可一次勾选同型同尺寸孔，并允许在组内逐个取消；现有人工工艺方案、刀具和参数确认链路继续负责生成正式工序。
- 增加混合批次安全门：定点钻允许跨孔组；其他孔工艺若同时勾选不同孔型或尺寸，会被阻止并要求逐组确认，避免把同一套深度、刀具或螺距错误应用到异类孔。
- 工具栏新增“孔心原点”：必须选中一个属于当前 Setup 的具体孔并二次确认，只用孔中心更新 X/Y，Z 保持当前原点高度，避免误用孔圆柱几何中心的 Z。
- 孔心原点变更继续进入 Setup 指纹，已有程序会标记过期但不删除。
- 新增 `hole_feature_grouping_test`，扩展独立 PC 界面契约；已通过完整 20 项自动测试、OCC Debug 构建、静态界面规则检查和孔分组独立渲染验收。

### 已完成：零件属性与矩形毛坯六向余量闭环
- 新增 `StockDefinition`，首期支持矩形毛坯、X−/X+/Y−/Y+/Z−/Z+ 六向独立毫米余量、非负归一化、最终包围范围和长宽高计算。
- 新增“零件/毛坯”工具栏入口和人工确认窗口：直接显示真实 STEP 零件范围及长宽高，编辑余量时实时预览最终毛坯范围和尺寸。
- 工艺流程状态带扩展为“设置正面 → 设置原点 → 设置毛坯 → Setup 已确认”；毛坯未确认时阻止工艺确认、程序生成、导出和 CQ8 发送。
- 毛坯定义已纳入项目保存恢复和 Setup 指纹；任一方向余量改变后旧程序会标记过期并阻断输出，但保留原代码供追溯。
- 旧项目缺少毛坯字段时安全回退为零余量、未确认，要求操作员按实际下料重新核对。
- 新增 `stock_definition_test`，扩展项目序列化和独立 PC 界面契约回归；已通过完整 19 项自动测试、OCC Debug 构建、静态界面规则检查和毛坯窗口独立渲染验收。

### 已完成：Setup 九点原点、偏移与程序失效闭环
- 新增正式 `SetupOrigin` 数据模型，支持顶面九点定位、自定义测量点/孔中心、XYZ 毫米偏移和人工确认状态，并根据定位方式、偏移与 G54-G59 生成稳定 Setup 指纹。
- 新增“设置原点”工具栏入口和 Setup 原点对话框；只有确认加工正面后才能进入，窗口实时显示最终 X/Y/Z 原点，自定义坐标只在选择“自定义点/孔中心”时开放编辑。
- 工艺流程状态带现在区分“设置正面”“设置原点”“原点已确认”三个真实阶段；未确认原点时禁止确认工艺方案和生成程序。
- Setup 原点、偏移、确认状态和 WCS 已纳入项目保存恢复；旧项目安全回退为 G54、顶面中心、原点未确认。
- 程序快照新增 Setup 指纹。原点、偏移或 G54-G59 改变后，已有程序自动显示为过期并阻断导出与发送，原程序内容不会被删除。
- 新增 `setup_origin_test`，扩展程序快照状态和项目序列化回归；已通过完整 18 项自动测试、OCC Debug 构建、静态界面规则检查和原点对话框独立渲染验收。

### 已完成：设计功能复刻范围与第一批加工对象筛选
- 依据《加工中心控制系统用户使用手册》和《ARMaster 源胜免编程数控系统 CAM 使用说明》，建立 `docs/设计功能复刻矩阵.md`，明确文件、Setup、孔槽轮廓、刀具、模板、测量、工序和仿真等能力的现状、缺口与 P0-P2 顺序。
- “复刻”统一定义为实现等价的设计与编程能力，不复制品牌资源或控制器专有逻辑；特征识别仍只产生候选对象，必须由操作员人工选择工艺并确认正式工序。
- 识别特征区新增“全部、孔、槽/腔、平面、轮廓”五类真实筛选入口，并显示各类别数量；筛选只改变当前显示对象，不会自动创建或确认工序。
- 分类规则独立为可测试的 `FeatureDisplayFilter`：孔包含孔和螺纹孔，槽/腔包含槽和型腔，平面对应平面，其余凸台、倒角和圆角归入轮廓/其他。
- 新增 `feature_list_filter_test` 覆盖五类筛选映射；排查并移除测试对缺失 offscreen Qt 平台插件的依赖，避免测试启动时被平台错误对话框阻塞。
- 已通过完整 17 项自动测试、OCC Debug 构建、差异检查和空项目界面目视验收；下一批按矩阵推进 Setup 原点、毛坯与零件属性。

## 2026-08-05

### 已完成：参考两份手册补齐工艺设计流程状态带
- 重新核对《加工中心控制系统用户使用手册》和《ARMaster 源胜免编程数控系统 CAM 使用说明》的加工界面、设计界面、刀具、工序和加工对象组织方式。
- 保留现有“特征/工序 + 三维图形 + 工艺/刀具”的稳定工作区，在顶部新增模型、Setup、特征、工序、程序五阶段工艺流程状态带。
- 五个阶段直接读取真实项目数据：模型是否加载、加工正面是否确认、识别特征数量、正式工序数量和程序快照数量，不以静态演示数据伪装完成状态。
- 各阶段可作为快捷入口：导入模型、设置正面、聚焦特征、聚焦工序或进入程序验证；前置条件不满足时禁用，并通过提示说明恢复条件。
- 继续保留人工工艺决策边界，识别结果不会自动转为正式工序，工序也不会自动下发 CQ8。
- 新增流程状态带 UI 契约回归；已通过完整 16 项自动测试、OCC Debug 构建和空项目界面目视验收。

### 已完成：程序验证工作区状态链优化
- “程序验证”页重构为程序快照、最终代码仿真和最终 CQ8 程序三个明确区域，并增加程序快照、安全校验、仿真状态、CQ8 输出四段状态条。
- 状态条已连接真实程序内容、工序快照指纹、`GCodeSafetyValidator` 与仿真生命周期；程序编辑后仿真自动回到待运行状态。
- 快照过期或安全校验失败会同时阻断导出与发送；手工程序会明确标识，不伪装成自动生成快照。
- 无程序时显示完整空状态和下一步提示，导出、发送及仿真操作保持禁用。
- 继续严格以 `untitled/` 独立 PC 软件为唯一实现基准，未引入或修改 `CNC_SYSTEM`。
- 已通过完整 16 项自动测试、OCC Debug 构建和两轮独立 `CNEXT-CAM.exe` 界面目视检查。

## 2026-08-04

### 已确认：独立 PC 软件架构与主界面第一轮优化
- 唯一实现基准固定为 `untitled/`，不引入或依赖 `CNC_SYSTEM` 项目。
- `CNEXT-CAM.exe` 内部组织为工艺设计、程序验证和机床运行三个工作区，并由自身的在线控制模块连接 CQ8。
- 主界面增加 CQ8 连接、机床模式、当前任务和安全状态常驻头部。
- “机床控制（预留）”改为自有“机床运行”工作区，删除接入外部控制系统的占位语义。
- 在 CQ8、安全链和实体面板联调完成前，运动请求按钮保持明确禁用。
- 新增 `standalone_ui_contract_test`，并通过完整 16 项自动测试。

## 2026-08-03

### 已完成：真实贯通孔 OCC Setup 正面归类修复
- 扩展 `hole_step_fixture_test`，要求 `测试模型/孔.stp` 的三个贯通孔在当前默认正面 Setup 下全部归类为 `FaceRegion::Front`。
- 回归确认 OCC 主路径此前仍按 `-feature.axis` 推断孔口侧，贯通孔轴向的正负方向会导致三个双侧开口孔整体落入反面；旧的孔中心修复仅存在于无 OCC 回退路径。
- `FeatureRecognizer::machiningRegionForFeature()` 现在对 `through_hole` 单独处理：先保留侧向孔轴为 `Side`，再沿当前 Setup 法向计算模型前后投影范围，并按孔中心到前后两侧的距离选择开口侧；距离相同时选择当前正面。
- 盲孔、沉孔和攻丝孔仍按明确的单侧孔轴判断开口方向；侧面孔不会被投影为正面孔，因此当前单 Setup 安全门仍会阻止其生成。
- 该修复只改变特征的 Setup 区域归类，不自动选择孔工艺、刀具或参数，也不自动建立正式工序。
- 已通过 15 项 C++ 自动化测试、真实 OCC STEP fixture、`build_pipeline_test`、完整 OCC Debug 构建、差异检查和 CNEXT-CAM 5 秒启动冒烟验证。
- 下一步将三个真实孔转换为人工确认的孔工序测试输入，固定穿透余量、`CYCLE83` 参数、最终主 MPF 和最终 G-code 仿真解析。

### 已完成：基础孔 STEP 黄金识别与外圆角误识别修复
- 新增 `hole_step_fixture_test`，直接导入仓库中的 `测试模型/孔.stp`，并锁定 SHA-256 `8285E06022B3A6FA447AB6060811F647E3ED35FE4D858A125C7CF1E1E33896A6`。
- 黄金期望固定当前加工坐标语义：模型顶面为 `Z0`、底面为 `Z-10`，XY 包围盒为 `[-44,-35]` 到 `[68,27]`。
- 测试锁定三个真实通孔的中心、半径、10 mm 深度、Z 轴方向与 STEP 面关联，并要求普通圆柱孔不得被推断为螺纹孔。
- 真实模型回归发现 `center=(63,22,-5), R5` 的外轮廓圆角被误识别为第四个 `tapped_hole`；该面是局部圆柱外角，不是闭合孔壁。
- `FeatureRecognizer::findHoles()` 现在使用受面边界约束的圆柱参数域检查周向覆盖，只允许接近完整 `2π` 的闭合圆柱进入孔候选；无限底层曲面的参数范围不再把局部圆角伪装成完整孔。
- 标准 `run_tests.ps1` 新增 `-OccPrefix` 和 OCC fixture 构建入口，真实 STEP 导入/识别已成为默认阻塞回归，不再只依赖合成结构测试。
- 测试总数增至 15 项；全部 C++ 测试、`build_pipeline_test`、完整 OCC Debug 构建、差异检查和 CNEXT-CAM 5 秒启动冒烟验证均通过。
- 下一步继续固定三个孔在确认 Setup 后的正面归类、人工选择工艺、循环参数、最终 MPF 和最终 G-code 仿真路径。

### 已完成：重复孔 Siemens 循环黄金回归与批量工序追溯
- 使用真实 `PeckDrillingStrategy`、`ProgramGenerationService` 和 `SiemensPostProcessor` 建立双孔批量黄金回归，不再只用固定字符串假策略验证服务边界。
- 已确认兼容重复孔采用 Siemens 函数式循环：同参数双孔只输出一次 `MCALL CYCLE83(...)` 定义，随后分别输出两个 `X/Y` 位置调用并用 `MCALL` 结束，没有重复展开钻孔逻辑。
- 修复批量孔工序块只写第一道 `[op:id]` 的追溯缺口；现在同一批次内所有正式工序 ID 都写入最终 G-code 头部，程序选择、快照审计和后续 Execution Preview 均可找到完整来源集合。
- 主 MPF 仍与通过安全校验的最终 G-code 完全一致；孔循环已经消除重复时不会额外生成无收益的 SPF 包装。
- 参数化 SPF 将只用于确实重复的槽/轮廓局部刀路，并要求策略提供结构化局部坐标和调用参数；不会从任意最终 G-code 文本猜测函数边界。
- 已通过 14 项 C++ 自动化测试、`build_pipeline_test`、完整 OCC Debug 构建、差异检查和 CNEXT-CAM 5 秒启动冒烟验证。

### 已完成：Program Snapshot 与 Siemens 主 MPF 接入生成服务
- `ProgramGenerationService` 的成功结果现在直接返回完整 `ProgramEntry`，包含快照 UUID、创建时间、名称、实际后处理器、来源工序 ID 与指纹、Machine Profile ID/版本、实际安全起始块、最终 G-code 和行数。
- Siemens 正常生成流程会自动创建确定性命名的主 `.MPF` 包文件及 SHA-256；包文件内容与刚通过 `GCodeSafetyValidator` 的最终控制器 G-code 完全一致，避免编辑器、仿真与导出使用不同程序副本。
- 新增 `SiemensProgramPackageBuilder::fromValidatedMainProgram()`，只封装已经验证的最终主程序，不添加未经再次验证的运动或机床状态。
- `MainWindow` 不再自行拼装批量 Program Snapshot，只负责把服务返回的完整快照写入 `ProjectManager` 并加载展示。
- 生成失败时返回的快照保持为空，不会留下部分 G-code、部分包文件或虚假的来源工序记录。
- 扩展 `program_generation_service_test`，覆盖快照身份、元数据、来源指纹、机床配置追溯、确定性 MPF 名称、文件哈希以及 MPF 与最终 G-code 字节一致性。
- 当前只自动生成一个主 MPF；重复孔槽逻辑自动抽取为参数化 SPF 尚未接入，下一步将以操作级 Program Section 数据建模和黄金输出测试单独实现，不从任意 G-code 文本猜测函数边界。
- 已通过 14 项 C++ 自动化测试、`build_pipeline_test`、完整 OCC Debug 构建、差异检查和 CNEXT-CAM 5 秒启动冒烟验证。

### 已完成：ProgramGenerationService 第一阶段提取
- 新增无界面依赖的 `ProgramGenerationService`，从 `MainWindow` 接管正式工序的策略执行、连续同类孔批处理、刀具解析、控制器后处理和最终 `GCodeSafetyValidator` 校验。
- 服务只接受带正式工序 ID 的已确认工序；空工序 ID、缺失策略、无效刀具、策略失败、空 G-code 或最终安全校验失败均采用原子失败，不返回部分程序或来源工序列表。
- 保留现有孔工序顺序与批处理规则，并继续在槽策略执行前移除参数表中可能覆盖正式槽几何的 `depth/slotLength/slotWidth/angle` 字段。
- `MainWindow` 现在只保留当前单 Setup 的正面可达性预检、后处理器选择、结果展示和 Program Snapshot 落库，不再直接编排每道策略的生成循环。
- 新增 `program_generation_service_test`，覆盖有效工序生成、后处理包装、工序追踪标记、来源工序 ID、任一道刀具无效时整体失败，以及未确认工序不能绕过界面生成。
- 已通过 14 项 C++ 自动化测试、`build_pipeline_test`、完整 OCC Debug 构建、差异检查和 CNEXT-CAM 5 秒启动冒烟验证。
- 下一步继续把 Program Snapshot/MPF-SPF 程序包创建从 `MainWindow` 移入服务，再以孔、槽两个基础 STEP 模型建立黄金输出和最终 G-code 仿真回归。

### 已完成：Operation Proposal 人工确认、Setup 安全门与程序生成分层
- 新增瞬态 `OperationProposal` 和显式 `OperationConfirmationIntent::ExplicitUser`；系统建议不能直接成为可执行工序，只有用户点击“确认并加入工序”后才转换为带独立 UUID 的正式 `MachiningOperation`。
- 工艺规划页只负责编辑和确认候选方案，不再隐式生成 G-code 或创建 Program Snapshot；已确认工序页成为唯一程序生成入口，加工页只负责最终程序的仿真、导出和后续发送。
- 第一阶段单 Setup 采用 fail-closed：活动 Setup 未定义、特征方向未知或特征不属于当前 Setup 时均阻止确认，不提供未定义权限与审计规则的工程师绕过入口。
- 工序批量生成改为原子失败：缺少策略、刀具无效、任一策略生成失败、空 G-code 或最终 `GCodeSafetyValidator` 校验失败时均停止，不创建部分 Program Snapshot。
- Program Snapshot 新增 `Incomplete` 状态；快照未覆盖全部已确认工序时阻止输出，工序增删、排序、换刀或顺序变化后旧快照会被标记为过期并要求重新生成。
- 工序列表按用户当前可见顺序生成程序；新增批量加入正式工序、编辑后同步工程、删除二次确认及明确的中英文状态提示。
- 按 Impeccable 评审统一操作层级：工艺页主操作为“确认并加入工序”，程序页主操作为“从已确认工序生成程序”，加工页主操作为“导出 G-code”；候选方案与已确认工序使用可见标题和状态区分。
- 已通过 13 项 C++ 自动化测试、`build_pipeline_test`、完整 OCC Debug 构建、差异检查及 CNEXT-CAM 5 秒启动冒烟验证；本阶段没有接入或部署 CQ8 实时控制。

## 2026-07-31

### 已完成：项目级 Impeccable skill 安装
- 通过官方 `impeccable` npm 安装器将 Codex skill 安装到 `.agents/skills/impeccable`，包含完整 `SKILL.md`、参考资料、脚本和四个内嵌设计代理。
- 生成项目级 `.codex/hooks.json`；skill 目录共 148 个文件，约 3.0 MB，关键文件完整性检查通过。
- 官方安装器同时更新了检测到的项目级 Claude harness；Impeccable 将在重新载入 skills 后用于后续界面设计审查，不改变当前 CAM 功能范围。

## 2026-07-30

### 已确认：第一阶段 CAM 验收范围冻结
- 第一阶段固定为单 Setup、三轴 STEP 输入、孔与槽稳定闭环，以及 WH250852 最终验收模型实际需要的平面和轮廓加工。
- 在上述闭环通过前，暂不扩展 DXF 编程、3+2、多 Setup、动态铣、刻字、完整 CAD 编辑和与验收模型无关的广泛新策略。
- 阶段完成标准是识别、加工几何、工序、最终 G-code、仿真与安全校验能够对生产代表模型稳定重复，而不是以功能按钮数量衡量。

### 已确认：工艺方案采用人工确认闭环
- 系统可以自动识别、分类和分组特征，也可以推荐兼容的工艺、刀具和参数，但不得静默生成可执行工序。
- 新增领域概念 `Operation Proposal`：系统建议在用户确认前不可执行；用户明确选择工艺、刀具和方案后才转为正式 `Machining Operation`。
- 相同且兼容的孔槽允许批量应用一次人工选择，但所有正式 G-code 只能来源于已经确认的工序。
- 新增 ADR-0005，后续 UI、持久化和生成安全门均按“建议与正式工序分离”设计。
- 交互规则进一步确认：候选方案先过滤几何、Setup 和刀具尺寸上不可能的组合；可标记并预填推荐方案，但必须由用户点击“确认并加入工序”；高级参数可修改，但不可绕过硬安全校验。

### 已确认：第一阶段加入人工选择的工艺模板
- 新增领域概念 `Process Template`，按用户选择的材料、加工策略和兼容刀具范围提供转速、进给、下刀、切深、步距、冷却及安全高度等建议值。
- 模板不自动识别材料、不自动建立工序；应用模板只把参数复制到 `Operation Proposal`，用户仍可修改并必须明确确认。
- 正式 `Machining Operation` 和 Program Snapshot 保存确认后的最终参数及模板版本信息，后续修改模板不得静默改变旧工序或旧程序。
- 新增 ADR-0006，作为后续材料库、配速表和孔槽参数复用的设计基线。

### 已确认：第一阶段仿真以最终 G-code 为唯一权威
- 新增领域概念 `Execution Preview`，必须从最终控制器目标输出解析，不允许用策略内部另一份临时刀路替代正式程序预览。
- 第一阶段要求展开支持的孔循环、宏和子程序，联动 G-code 行号、工序与刀具位置，并检查特征边界、深度、低位快移、刀具包络和模型底部越界。
- 上述硬校验失败时阻止正式输出；真实材料去除、余料模型和完整刀柄/夹具扫掠碰撞延后到基础模型与最终验收模型通过之后。
- 新增 ADR-0007，固化最终输出与显示/安全检查一致的设计原则。

## 2026-07-29

### 已确认：CQ8 一体化产品采用 CAM 与实时控制分进程架构
- 最终生产目标由 Siemens 840D 程序包调整为自有 CQ8 驱控一体平台；Siemens/Fanuc 保留为可选兼容输出和回归目标。
- CNEXT-CAM 继续负责 STEP、特征、工艺、刀路、安全验证和不可变程序快照；CQ8 Control Runtime 独占控制器通信、机床状态、联锁、程序加载和实时运动控制。
- 两者对用户表现为一体化产品，但通过 `CQ8 Machining Job Package` 建立经过验证、可追溯的进程间交付边界，避免 CAM 崩溃或复杂几何处理影响实时控制。
- 新增 ADR-0004，并将原“Siemens 为首个生产验收控制器”的 ADR-0002 标记为已被取代；CQ8 任务包的具体载荷格式仍待下一项架构决策确认。
- 当前不急于部署 CQ8：近期只在 CAM 内保留版本化 `CQ8 Integration Port`，不实现真实连接、程序下载、启动加工或任何实机运动控制，开发资源继续集中在 CAM 闭环。

### 已完成：设置正面确认与重复 Setup 旋转修复
- 修复 STEP 同一可见平面被拆分为多个共面面片时，第二次点击因 `faceIndex` 不同而无法确认“设置正面”的问题；现在相同面或法向一致的共面面片均可完成确认。
- 修复重复设置正面时只使用本次法向旋转、覆盖既有 Setup 方向的问题；现在将本次增量旋转与工程中已有的 Setup 四元数正确组合。
- “设置正面”动作改为使用 QAction 的实际勾选状态，并在进入、取消和完成时统一清理待确认面、法向与高亮，避免残留选择状态干扰下一次操作。
- 新增 `SetupOrientation` 核心辅助类和 `setup_orientation_test`，覆盖同面确认、共面分片确认、不同法向拒绝以及连续两次设置正面的旋转组合。
- 已通过 12 个 C++ 测试和完整 OCC Debug 构建验证；新程序已生成，当前运行中的旧实例关闭后即可替换其可执行文件。

### 已完成：MPF/SPF 程序包目录导出
- 新增 `ProgramPackageExporter`，可将 Program Snapshot 中的一个 MPF 和多个 SPF 一次写入用户选择的目录。
- 写盘前统一检查：程序包非空、主 MPF 存在、文件名不含目录或路径穿越、Windows 大小写语义下文件名不重复、每个文件内容与保存的 SHA-256 一致。
- 文件写入使用 `QSaveFile` 提交，单个文件不会以半写入状态替换目标文件；导出报告返回实际写出的全部路径。
- 主界面“导出 G-code”已支持双模式：含 `packageFiles` 的快照选择目录批量导出，旧单文件快照继续使用原来的保存文件对话框。
- 用户手工修改当前 G-code 后，快照中的旧 `mainProgramFileName/packageFiles` 会立即清空，避免编辑后的 MPF 与旧 SPF 混合导出。
- 新增 `program_package_exporter_test`，覆盖 MPF/SPF 内容写盘、文件数量、哈希不匹配预写入阻断和 `../` 路径穿越阻断。
- 已通过 11 个 C++ 测试和完整 OCC Debug 构建验证。
- 当前仍需把程序生成入口自动转换为 `SiemensProgramPackage` 并填充快照，之后才能从正常孔/槽生成流程直接得到多文件程序包。

### 已完成：Program Snapshot 持久化 MPF/SPF 文件包
- `.cnext` 工程格式由 `1.9` 提升到 `2.0`，`ProgramEntry` 新增主程序文件名和完整程序包文件列表。
- 每个快照文件记录角色（主程序/子程序）、文件名、完整内容和 SHA-256，可同时保存一个 MPF 与多个 SPF，不再只能依赖单一 `gcodeText`。
- 项目 JSON 在程序项下新增 `programPackage`，包含 `mainProgramFileName` 和 `files` 数组；保存、关闭、重新加载后文件顺序、名称、内容和哈希保持一致。
- 保留现有 `gcodeText/macroText` 字段，旧版 `.cnext` 项目缺少 `programPackage` 时仍可正常加载，新增包字段保持为空。
- 扩展 `project_manager_serialization_test`，覆盖 MPF/SPF 两文件写入、JSON 结构、完整读回、工程版本 2.0 以及旧单文件工程兼容。
- 已通过 10 个 C++ 测试和完整 OCC Debug 构建验证。
- 当前 Program Snapshot 已具备存储能力，但生成入口尚未自动填充程序包；下一步将把 `SiemensProgramPackageBuilder` 的结果接入快照创建与目录导出。

### 已完成：Siemens MPF/SPF 确定性程序包核心
- 新增 `SiemensProgramPackageBuilder` 及 MPF/SPF 文件数据结构，程序包包含主程序文件、零到多个子程序文件、每个文件的内容和 SHA-256 指纹。
- Siemens 文件名统一转换为大写 ASCII 字母、数字和下划线，限制为 24 个字符，并生成确定性的 `.MPF` / `.SPF` 文件名。
- 同一 `repeatKey` 的加工逻辑只有出现至少两次时才提取为 SPF；只出现一次的逻辑继续内联在 MPF 中，避免为了单次调用增加程序复杂度。
- SPF 使用官方 `PROC NAME(REAL ...)` 参数定义、主程序使用 `NAME(actuals)` 调用，并以 `RET` 返回；相同输入重复构建时文件内容与哈希保持完全一致。
- SPF 构建阶段拒绝工具、主轴、冷却液、WCS 和安全模态指令，确保 `T/M6`、`S/M3`、`M7/M8/M9`、`G54-G59`、`G40/G49/G80/G90/G94` 等受控状态保留在 MPF。
- 新增 `siemens_program_package_test`，覆盖重复阈值、单次逻辑内联、程序命名、参数调用、`PROC/RET`、文件指纹、重复构建确定性及 SPF 非法机床状态阻断。
- 已通过 10 个 C++ 测试和完整 OCC Debug 构建验证。
- 当前尚未把程序包接入 Program Snapshot、目录导出和孔/槽工序自动抽取；这些作为下一步继续实现。

### 已完成：显式安全 Z 与多刀换刀状态闭环
- G-code 安全校验器现在拒绝在尚未建立明确 Z 状态时执行 `G0 X/Y`，避免程序依赖机床启动前未知的实际 Z 位置进行首次横向快移。
- 新增主轴、冷却液和换刀次数的逐行状态跟踪；第二把及后续刀具换刀前必须已经 `M5` 停主轴、`M9` 关冷却液，并位于已知的正安全 Z。
- `M30` 不再只检查程序中是否曾出现 `M5/M9`；若主轴在收尾前重新启动或冷却液重新开启但未再次关闭，程序结束位置会被阻止。
- Siemens 与 Fanuc 后处理器在第二次及后续 `T... M6` 前统一输出 `M5`、`M9`；策略原有的结束 `G0 Zsafe` 继续负责换刀前抬刀。
- 扩展 `gcode_safety_validator_test`，覆盖未知 Z 横向快移、合法双刀程序、主轴运行换刀、冷却液开启换刀、低 Z 换刀和主轴运行状态结束程序。
- 扩展 `postprocessor_header_test`，覆盖 Siemens/Fanuc 双刀程序必须输出 `G0 Zsafe → M5 → M9 → T2 M6` 顺序。
- 已通过 9 个 C++ 测试和完整 OCC Debug 构建验证。

### 已完成：轮廓与槽策略刀补引入/引出闭环
- `ContourFinishStrategy`、开放轮廓、封闭轮廓、开口槽和盲槽策略不再独立输出机床刀补代码；`G41/G42` 现在与原有第一条 `G1 X/Y` 引入运动同段，`G40` 与最后一条 `G1 X/Y` 引出运动同段。
- `CircleMillingStrategy` 原先直接在圆周点启用刀补且没有直线引入/引出；现在从圆周外侧按至少一个刀具直径建立径向引入段，整圆加工后沿同一路径引出并取消刀补。
- CAM 已计算刀心偏置的 `compensation=0` 分支保持 `G40` 复位，不启用机床刀补，也不改变既有刀心几何。
- 新增 `cutter_compensation_strategy_test`，直接生成圆铣、通用轮廓精加工、开放轮廓、封闭轮廓、开口槽和盲槽 G-code，检查刀补转换程序段，并在补齐受控程序头/收尾后通过统一 `GCodeSafetyValidator`。
- 全仓扫描确认机床刀补策略不再输出独立 `G41/G42`；剩余独立 `G40` 仅位于未启用刀补的 CAM 偏置复位分支。
- 已通过 9 个 C++ 测试和完整 OCC Debug 构建验证。

### 已完成：运动模态继承与刀补引出校验
- G-code 安全校验器开始跟踪 `G0/G1/G2/G3` 运动模态；未重复书写 `G0` 的后续 X/Y 程序段也会按快速移动检查，堵住低位模态快移漏检。
- `G41/G42` 启用和 `G40` 取消均要求处于直线运动模态并包含 X/Y 位移，避免无实际引入/引出运动的刀补状态切换。
- 支持 `G40 X... Y...` 继承上一条 `G1` 模态，保持标准模态程序写法可用。
- 扩展 `gcode_safety_validator_test`，覆盖模态低位快速移动、缺少刀补引出运动以及继承 `G1` 的合法 `G40` 引出。
- 核查发现现有部分轮廓和槽策略仍将 `G41/G42/G40` 独立成行；新安全门会阻止这些程序导出，下一步需将对应策略统一为刀补代码与实际 XY 引入/引出运动同段输出。
- 已通过 8 个 C++ 测试和完整 OCC Debug 构建验证。

### 已完成：停机顺序与刀具半径补偿安全状态校验
- G-code 安全校验器新增停机顺序检查，要求 `M5` 和 `M9` 位于 `M30` 之前；在 `M30` 之后或同一程序段才停主轴/关冷却液时拒绝导出和发送。
- 新增 `G40/G41/G42` 模态跟踪：`G41/G42` 必须在包含 `G1/G01` 和 X/Y 位移的直线引入段上启用。
- 刀补启用后，换刀和程序结束前必须先执行 `G40`；未取消刀补会报告具体行或结束状态错误。
- 扩展 `gcode_safety_validator_test`，覆盖错误停机顺序、合法刀补闭环、无效刀补引入、换刀前未取消刀补及结束前未取消刀补。
- 已通过 8 个 C++ 测试和完整 OCC Debug 构建验证。

### 已完成：Machine Profile 安全程序头与 G90 强制
- 新增 Machine Profile 编辑器，可在工具栏中维护配置 ID、版本、控制器和逐行安全程序头，包括 `G17 G40 G49 G80` 等模态代码。
- 新增 `MachineProfileValidator`；缺少 `G17/G40/G49/G80/G21/G90/G94`、缺少 `G54-G59`、出现 `G91` 或配置标识为空时拒绝保存。
- Siemens/Fanuc 后处理器改为使用 Machine Profile 的安全程序头；当前 WCS 会替换模板中的 `G54-G59`，并始终强制输出 `G90`，旧 `useAbsoluteCoords=false` 不再产生 `G91`。
- G-code 安全校验器新增 `G91` 硬阻断，确保手工编辑后的增量坐标程序不能导出或发送。
- 修复组合快速移动的 Z 判断：`G0 X... Y... Z...` 现在使用本行目标 Z，避免安全抬升误报和低位快速移动漏报。
- 新增/扩展 `machine_profile_validator_test`、`postprocessor_header_test` 和 `gcode_safety_validator_test`，覆盖缺少 `G80`、禁止 `G91`、WCS 模板替换及组合 XYZ 快移。
- 已通过 8 个 C++ 测试和完整 OCC Debug 构建验证。

## 2026-07-20

### 已完成：程序快照过期检测
- 新增 `ProgramSnapshotFingerprint`，为程序快照记录生成时的源工序状态指纹，覆盖工序 ID、类型/阶段、策略、刀具、参数和关联特征几何。
- `ProgramEntry` 增加 `sourceOperationFingerprint` 并写入 `.cnext` 项目文件；项目保存版本提升到 `1.6`，旧项目缺少该字段时仍可加载。
- `MainWindow::appendProgramSnapshot()` 在生成 G-code 快照时保存源工序指纹；导出和发送到机床前会重新计算当前工序指纹，若工序已被修改或丢失则阻止操作并提示重新生成程序。
- 新增 `ProgramSnapshotStatus`，把快照状态判断集中为 `Fresh / Changed / MissingSource / Unchecked`，并让导出/发送阻断提示能区分“源工序已变化”和“源工序已丢失”。
- 程序列表现在会提前标记异常快照：源工序变化显示 `[stale]`，源工序丢失显示 `[missing source]`，tooltip 同步追加具体原因，便于用户在导出/发送前发现问题。
- 新增 `tests/program_snapshot_fingerprint_test.cpp`，覆盖指纹稳定性以及刀具、参数、特征几何、缺失源工序变化场景。
- 新增 `tests/program_snapshot_status_test.cpp`，覆盖快照新鲜、源工序变化、源工序缺失、旧快照未检查四种状态。
- 新增 `tests/project_manager_serialization_test.cpp`，覆盖 `.cnext` 保存版本、程序快照指纹写入/读回、当前程序 ID 读回、工序 ID 读回，以及旧 `1.5` 项目缺少指纹字段的兼容加载。
- 新增 `scripts/run_tests.ps1` 统一测试入口，可一键编译并运行 G-code 安全、快照指纹、快照状态、项目序列化四个测试。
- 已通过 `scripts/run_tests.ps1`、`git diff --check` 和 `mingw32-make -f Makefile.Debug` 验证。

### 已完成：导出/发送前 G-code 安全校验
- 新增 `GCodeSafetyValidator`，用于在导出和发送到机床前检查当前 G-code 是否具备基础安全结构。
- 当前校验覆盖：空程序、`G17 G40 G49 G80` 安全模态复位、`G21/G20` 单位模式、`G90` 绝对坐标、`G54-G59` 工件坐标系、`T... M6` 换刀、主轴启动、`M5/M9/M30` 收尾，以及低于或等于切削 Z 时的 `G0 X/Y` 快速移动。
- `MainWindow::onExportGCode()` 和 `MainWindow::onSendToMachine()` 已接入该校验；校验失败时阻止导出/发送，并显示具体失败项。
- Siemens 与 Fanuc 后处理器程序头已补充 `G21`，确保本软件生成的 G-code 能通过新增单位模式校验。
- 新增 `tests/gcode_safety_validator_test.cpp`，覆盖有效程序、缺少 `M30`、缺少 `G21/G20`、低 Z 快速 XY 四个核心场景。
- 新增 `tests/postprocessor_header_test.cpp`，覆盖 Siemens/Fanuc 程序头必须输出 `G21`。
- 已通过 `scripts/run_tests.ps1` 和 `mingw32-make -f Makefile.Debug` 构建验证。

## 2026-07-17

### 已完成：安全威胁模型草案与导入/发送提示修复
- 新增 `gcodecnc-threat-model.md`，按本地 Qt/C++ CAM 桌面软件建模，覆盖 STEP 导入、`.cnext` 工程加载、策略参数、G-code 后处理/仿真、导出和串口发送边界。
- 威胁模型明确当前最高风险为：不可信 STEP/工程文件导致错误特征或参数、G-code 编辑绕过策略校验、程序快照与工序状态不一致、串口发送缺少发送前校验。
- 修复 `StepImporter` 的 STEP 导入错误提示乱码，恢复文件不存在、读取失败、几何转换失败、无可用几何体、OpenCascade 异常等中文信息。
- 修复 `CncSendDialog` 和 `CncCommInterface` 中机床发送、端口、波特率、连接状态、发送日志和串口错误提示的中文乱码。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：参数弹窗与斜底槽策略稳定化
- 修复 `ParameterEditorDialog` 新增文件内的中文乱码，参数提示、表头、错误提示和恢复默认按钮均恢复为可读文本。
- 清理 `StrategyPanel::onEditHoleParams()` 中 `return` 后残留的旧参数编辑死代码，统一走新的 `ParameterEditorDialog` 参数编辑流程。
- 修复加工面不匹配警告的中文乱码，并清理 `MainWindow.cpp` 中两行损坏注释，避免后续维护继续被乱码干扰。
- `TaperedSlotMillingStrategy` 增加硬校验：进给高度必须大于 0，斜面精修余量必须小于槽深，避免生成退化层或无效深度刀路。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

## 2026-07-01

## 2026-07-02

### 已完成：策略参数改为弹窗编辑，并对三页面工作台做第一轮美化
- `StrategyPanel` 不再把策略参数表直接铺在主界面上，改为“参数摘要 + 参数配置弹窗”模式；保留原有 `StrategyParams` 数据流，孔加工和选中特征的铣削策略都可复用同一套参数编辑入口。
- 主界面新增一轮保守样式整理：三页面导航、工具栏、加工页侧栏、程序列表、分割条和策略面板的视觉层级统一，界面从“表格堆叠”调整为更接近工作台的布局。
- 本次改动未触碰刀路生成协议、后处理协议和仿真信号链，重点只在 UI 展示层和参数编辑交互层。
- 主要改动文件：`untitled/src/ui/StrategyPanel.h`、`untitled/src/ui/StrategyPanel.cpp`、`untitled/src/ui/MainWindow.cpp`。
- 已通过 `mingw32-make -f Makefile.Debug` 全量编译验证。

### 已完成：新增通用参数编辑弹窗，补齐参数分组与基础校验
- 新增 `ParameterEditorDialog` 通用组件，用于承接策略参数编辑；支持“基本参数 / 高级参数”分组显示、恢复默认值，以及确认前的数值校验。
- `StrategyPanel` 已切换到新弹窗组件，当前工艺参数会先进入摘要区，再通过统一弹窗编辑，避免主界面再次回到内嵌参数表模式。
- 参数校验规则先按最小可用方案落地：大多数工艺参数要求大于 0，余量类参数要求不小于 0，角度/补偿/斜面方向等特殊项允许按既有语义输入。
- 主要改动文件：`untitled/src/ui/ParameterEditorDialog.h`、`untitled/src/ui/ParameterEditorDialog.cpp`、`untitled/src/ui/StrategyPanel.h`、`untitled/src/ui/StrategyPanel.cpp`、`untitled/untitled.pro`。
- 已重新执行 `qmake untitled.pro` 并通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：开口槽取消伪造斜坡回退，改为真实开口侧 ramp 进刀
- 针对 `SlotMachiningGeometry` 在未识别到槽端斜面时，仍自动回填 `slopeStartLength/slopeEndLength` 的问题，移除了这条几何层兜底逻辑，避免把普通开口槽误当成“带底部斜面”的槽去生成刀路。
- `SlotMillingStrategy` 新增开口侧 `XYZ` ramp 进刀：粗加工首刀、底面精加工首刀和侧壁精加工首刀都改为从开口侧外延位置进入，再沿 `G1 X/Y/Z` 联动下到目标深度，不再直接在槽内首刀位置垂直下插。
- 开口槽策略同步补充 `M8` 冷却液开启；既有的底面精加工、斜面精加工、侧壁精加工和越界校验逻辑保持不变，但现在不再依赖伪造斜坡来解释入口路径。
- 主要改动文件：`untitled/src/strategies/mill/SlotMachiningGeometry.cpp`、`untitled/src/strategies/mill/SlotMillingStrategy.cpp`。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

## 2026-06-17

### 已完成：斜底槽粗加工改为带 Z 插补的分层扫描
- 针对用户反馈“没有一层层铣”的问题，修正 `TaperedSlotMillingStrategy` 的线性斜底粗加工路径。
- 线性斜底模式下，粗加工每层不再只按固定 Z 截取深端局部区域，而是按当前层深在完整槽长/槽宽范围内输出 `G1 X/Y/Z` 往复扫描，让每一层都形成对应深度的斜面。
- 保留最终底面精加工的完整斜底扫描，槽长、槽宽和加工深度仍使用已稳定的前视投影几何与识别深度。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：密集刀路仿真碰撞预检增加预算保护
- 针对生成斜底槽密集分层刀路后程序可能在加载仿真路径时被强制结束的问题，给 `SimulationController::detectCollisionMessage()` 增加同步碰撞检测预算。
- 当快速移动采样点、刀具足迹点和网格三角面组合过大时，跳过本次碰撞预检，避免 UI 线程长时间卡住；G 代码生成、刀路解析和刀路显示不受影响。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证，并完成 12 秒启动烟测。

### 已完成：斜底槽粗加工恢复恒定 Z 分层
- 针对 2026-06-17 11:14 的 G 代码中粗加工每条线都从 `Z=0` 插补到当前层深、视觉上仍像竖帘的问题，调整 `TaperedSlotMillingStrategy`。
- 粗加工阶段恢复为恒定 Z 层切：每层先下到固定 `Z`，再按该层与斜底面的交线裁剪可切 `U` 范围，并在该范围内输出 `G1 X/Y` 往复扫描。
- 斜底面的 `G1 X/Y/Z` 插补只保留在底面精加工阶段，避免粗加工阶段反复从浅端顶面斜插到深端。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：斜底槽按宽度方向识别斜面并逐层收窄
- 针对 2026-06-17 11:18 的 G 代码仍按长度 `U` 裁剪、第二层只剩很短 X 段的问题，修正斜底槽的坡面语义。
- 前视投影槽不再清零 `slopeMinWidth/slopeMaxWidth`，保留底部样本计算出的宽度方向斜面参数，使日志和 G 代码头能反映 `minV/maxV` 收缩。
- `TaperedSlotMillingStrategy` 新增宽度斜面模式：当存在 `slopeMinWidth/slopeMaxWidth` 时，每层保持长度方向范围，按层深收缩 `V` 向加工宽度，实现“越深越窄”的层切。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：前视投影槽按识别槽宽回填缺失斜面宽度
- 针对 2026-06-17 11:23 的 G 代码头仍显示 `minV=0/maxV=0`，导致策略继续按长度方向缩短刀路的问题，补齐前视投影槽的宽度斜面来源。
- 当前槽底部样本宽度约 `1.150`，识别槽宽约 `1.949`；当两者存在明显差值时，直接把差值作为单侧 `slopeMaxWidth` 回填，使策略进入 `width-profile`，按层深逐步缩小宽度方向可切范围。
- `TaperedSlotMillingStrategy` 的注释模式同步输出 `width-profile`，便于从生成 G 代码头快速判断是否走到了宽度收窄逻辑。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：宽度斜面槽底面精加工限制在最终底部窄带
- 针对 2026-06-17 14:12 的 G 代码在 `width-profile` 下仍出现超范围斜切的问题，修正 `TaperedSlotMillingStrategy` 的底面精加工边界。
- 粗加工继续按层深逐步收窄宽度；底面精加工不再扫描完整槽宽并按 `V` 向插补斜深，而是只在最终深度对应的底部宽度范围内恒定 Z 精修，避免刀路切出橙色槽区域。
- 已移除不再使用的 `wedgeZAtV()` 宽度斜插补逻辑，防止后续误用。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

## 2026-06-02

## 2026-06-04

### 已完成：斜底槽线性坡面不再裁剪平面走刀范围
- 针对 2026-06-16 日志中 `L=5.000 W=1.949 D=1.949 A=180` 已正确，但 G 代码仍形成扇形短线的问题，修正 `TaperedSlotMillingStrategy` 的线性斜底策略。
- `roughReachForLayer()` 在线性斜底模式下不再根据 `v` 方向和当前层深裁剪 `U` 范围，粗加工恢复为覆盖完整槽矩形，避免每条刀路被缩短成三角/扇形。
- 底面精加工恢复为完整宽度扫描，并通过 `G1 X/Y/Z` 在每条线上按 `wedgeZAtU()` 输出斜底高度；侧向槽转正面加工时同步清零宽度方向坡面参数，避免 `maxV` 再次影响路径。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：斜底槽粗加工按长度方向层切，避免浅端过切
- 重新排查生成思路后确认：线性斜底槽粗加工不能始终覆盖完整长度，否则浅端会被当前层深过切；也不能按宽度 `V` 裁剪，否则会形成扇形短线。
- `roughReachForLayer()` 现改为只根据当前 Z 层与斜底面的关系裁剪长度 `U` 范围，宽度方向保持完整槽宽。普通线性斜底使用层深比例计算可切长度段，长度坡面模式则按 `wedgeZAtU()` 采样判断。
- 移除侧向槽正面加工中残留的宽度坡面自动推断，避免 `slopeMaxWidth=0.974` 再次影响策略或日志判断。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：斜底槽生成前保留识别几何与工艺参数
- 针对日志中识别树已得到 `L=2.777 W=1.949 A=90`，但生成前被 `front-local-slot-applied` 改写成 `L=5.000 W=1.150 A=180` 的问题，调整正面局部样本修正逻辑。
- `refineFrontStraightSlotFromLocalSamples()` 现在只根据局部样本补充深度和坡面余量，不再覆盖已识别的 `center/length/width/angle`，避免把槽加工区域扩展到整张前侧面。
- `applySlotOverridesFromParams()` 对槽长、槽宽、角度参数改为显式写回最终特征，保证工艺面板参数能覆盖下游污染后的几何值。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：斜底槽坡面样本限制在槽边界内
- 针对新日志中 `requested-after-recognized-slot` 已恢复为 `L=2.777 W=1.949 A=90`，但 `slope-candidate` 仍出现 `bottomV[-5.000,0.000]` 的问题，增加坡面底部样本边界校验。
- `refineSlotSlopesFromMesh()` 现在要求底部样本的 `U/V` 范围必须落在当前槽的 `length/width` 边界容差内；超出边界的样本会被拒绝，不再写入 `slopeStartLength/slopeEndLength/slopeMinWidth/slopeMaxWidth`。
- 该修复避免大侧面或前脸样本继续把斜底槽坡面参数拉歪，后续策略会回退到按槽长方向的线性斜底加工。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧向槽正面投影长度与下刀深度分离
- 根据既有成功截图，侧向槽正面加工时刀路平面范围需要按前视投影展开，但 `5.000` 这类贯穿跨度不能直接作为 Z 向加工深度。
- `refineFrontStraightSlotFromLocalSamples()` 对侧向槽转正面加工时恢复投影中心、投影长度和投影方向修正，同时保留识别槽宽作为加工宽度，避免把前视窄边 `1.150` 当成槽宽。
- `applySlotOverridesFromParams()` 对侧向槽正面加工保留默认投影后的长度/角度；只有当用户参数确实不同于识别值时才覆盖，Z 向深度仍受 `sideSlotFrontDepthLimit()` 限制。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧向槽正面投影中心保留槽宽方向原点
- 针对 2026-06-08 12:17 日志中 `front-local-slot-applied center=(2.500,0.625)` 导致刀路整体偏向槽宽一侧的问题，调整前视投影中心计算。
- 侧向槽转正面加工时，中心现在只沿投影长度方向移动到贯穿跨度中点，槽宽方向仍保留识别中心，不再把前视窄边中点叠加到 `Y` 方向。
- 同时侧向槽正面加工禁用宽度方向坡面修正，避免 `slopeMinWidth/slopeMaxWidth` 把完整槽宽错误收缩。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：斜底槽粗加工按层裁剪为楔形范围
- 针对 `TaperedSlotMillingStrategy` 仍按整块 `L x W` 矩形蛇形扫描的问题，粗加工层内扫描范围改为按当前 `Z` 层深度对应的可切 `U` 范围截断。
- 新逻辑使用 `wedgeZAtU()` 反推每一层的有效长度范围，避免上层还未到达的区域提前参与粗加工，刀路形状从整矩形通扫收敛为与斜底一致的楔形区域。
- 该改动只影响 `mill_tapered_slot` 的粗加工层路径，不改变识别出的槽宽、槽深和中心。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：斜底槽底面精加工改为分层往复切削
- 针对底面精加工仍然生成大量从 `Z=0` 到 `Z=-1.949` 的整条斜线插补、视觉上像一排竖线下插的问题，`floor finish` 改为与粗加工一致的按层往复切削。
- 每一层 `finish layer` 先下到该层固定 `Z`，再在该层允许的楔形边界内做横向扫刀，不再跨越整深度做单条 `XYZ` 斜线切削。
- 这次修改让斜底槽整体路径组织回到“从上到下，一层层切”的工艺顺序，和用户期望的往复式分层切削一致。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：盲槽正面映射角度只保留一次
- 修复 `adaptSideTaggedSlotForBlindFrontMilling()` 在槽生成链路中被重复调用的问题，之前会出现 `A=90 -> 180 -> 270` 的连续旋转，导致同一槽被多次补偿角度后整体偏移。
- 现在该类槽只在 `toContourFeature()` 生成正面加工几何时做一次角度映射；生成前匹配槽特征、批量生成 `contourBatch` 等环节不再二次改角度。
- 本次修复直接对应日志中 `requested-after-recognized-slot` 与 `strategy-selected` 角度不一致的问题。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧向槽转正面加工时同步重映射角度
- 修复侧向识别槽在转为正面加工几何后，只映射了 `width/depth`，但保留了原始 `angle`，导致局部长度轴和宽度轴仍按侧面识别方向工作，刀路整体沿错误轴展开。
- `adaptSideTaggedSlotForBlindFrontMilling()` 现在会对这类 `region=Front` 且 `axis.z()` 不接近 Z 的槽同步旋转角度 90 度，使 `L/W/A` 三者保持一致。
- 本次修复针对日志中 `center=(0,0.05)` 仍然生成 `X[-2.318,2.350]` 这类“中心正确但轴向错误”的偏移。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：盲槽局部坐标到世界坐标转换统一
- 修复 `BlindSlotMillingStrategy` 仍使用旧版 `rotateAround()` 变换局部 `u/v` 坐标的问题，该写法在 `A=90°` 时会把刀路整体映射到错误的 X/Y 位置，造成生成后整体偏移。
- 盲槽策略的 `slotToWorld()` 已统一为与 `SlotMachiningGeometry` 一致的公式：`x = cx + u*cosA - v*sinA`，`y = cy + u*sinA + v*cosA`。
- 本次修复直接作用于粗加工、底面精加工、斜面精加工和壁面精加工的全部坐标输出，属于当前截图中“刀路整体偏到特征外”的主因修复。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：盲槽错误开口判定与中心漂移抑制
- 针对正面加工的侧向几何直槽，关闭 `detectOpenSlotSide()` 的自动开口槽改写，避免 `straight_slot` 在生成前被错误改成 `open_slot`，导致策略语义和刀路边界一起漂移。
- 禁止 `refineSideTaggedSlotProjection()` 的 `center-only` 回退分支对直槽生效，避免在长宽投影不成立时仅平移中心点，把刀路整体从 `x=0` 推到 `x=2.5`。
- 本次改动优先保证盲槽仍按盲槽处理，且刀路保持在原识别特征中心附近，不再接受“只改中心不改尺寸”的投影修正。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：盲槽宽度方向斜面采样增强
- 在槽斜面参数回填逻辑中，底部轮廓采样优先过滤纯垂直侧壁三角面，避免侧壁底边顶点把底部 V 向范围撑满，导致 `slopeMinWidth/slopeMaxWidth` 被误判为 0。
- 新增 `[slot-debug] slope-bottom-source=non-vertical/all-faces` 日志，用于确认当前斜面参数来源；非垂直面采样不足时自动回退到原有全部面片采样。
- 本次只调整斜面识别采样，不改变已确认铺满的槽长、槽宽、深度映射和盲槽基础扫描刀路。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

## 2026-06-03

### 已完成：盲槽斜面参数从网格回填到刀路生成
- 针对槽已经铺满但斜面仍按长方体加工的问题，新增槽网格底部轮廓采样逻辑，只回填 `slopeStartLength`、`slopeEndLength`、`slopeMinWidth`、`slopeMaxWidth`，不改已确认正确的槽中心、长宽和深度映射。
- `MainWindow` 在将识别特征转换为 `ContourFeature` 后，使用当前槽角度和中心把关联面片投影到局部 U/V 坐标，根据最低 Z 附近的底部采样轮廓计算斜面长度。
- 新增 `[slot-debug] slope-candidate...` 日志，便于测试时确认底部采样范围、斜面参数是否被应用。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：正面归类侧向槽的加工尺寸映射修正
- 修复槽被正确归类到 `Front` 后，原先只对 `region=Side` 生效的侧向槽正面加工映射失效的问题。
- 新增“侧向几何槽”判定：槽虽然显示为正面特征，但如果槽轴 `axis.z()` 不接近 Z 方向，仍按侧向识别转正面加工处理。
- `StrategyPanel` 默认参数现在会对这类槽使用正面加工尺寸：槽宽取原识别深度，Z 加工深度取原识别槽宽，避免 G 代码继续输出 `W=1.949 D=5.000` 导致未铺满。
- `MainWindow` 的加入工序、生成全部 G 代码和面片 footprint 修正同步使用该判定，避免 UI 参数与实际生成不一致。
- 槽刀具自动选择也改用映射后的加工槽宽，避免按原识别窄宽误选刀。

### 已完成：新增 CNC G-code Review 项目 Skill 草案
- 在 `.codex/skills/cnc-gcode-review` 新增项目专用 Codex skill，用于后续审查 G 代码安全性、刀路边界、后处理器输出和仿真一致性。
- `SKILL.md` 定义触发场景和工作流，要求在修改 G 代码生成、槽/孔策略、后处理器和刀路显示问题时先按清单排查。
- 新增 `references/gcode-review-checklist.md`，覆盖程序头、快速移动、切削边界、槽加工、孔加工和后处理检查项。
- 新增 `references/open-source-notes.md`，记录 LinuxCNC、GRBL、`dillonhuff/gpr`、`xyz-tools/gcode-preview` 对本项目的参考用途，先作为设计参考，不引入外部依赖。
- 已手动检查 skill 目录结构、frontmatter 和 `agents/openai.yaml`；官方 `quick_validate.py` 因当前 Python 环境缺少 `yaml` 模块未能运行完成。

### 已完成：盲槽 ramp 回退到中心线并限制有效层深
- 撤销上一轮将粗加工 ramp 入口放到扫描边界的做法，恢复为槽中心线下刀，避免刀路贴槽边生成长竖线/斜线。
- 盲槽粗加工在无螺旋下刀时，会根据槽中心线可用长度和 `rampAngle` 自动限制有效每层切深，避免短槽里用 3° ramp 强行下 2mm 造成不可行陡斜坡。
- 当有效层深被限制时，G 代码头部输出 `Blind slot stepdown limited...` 注释，便于测试时确认层深为何变小、层数为何变多。
- 若槽太短导致有效 ramp 层深低于 0.005mm，则直接返回错误，不再输出危险刀路。

### 已完成：盲槽 G 代码流程补完
- 盲槽策略在主轴启动后补充 `M8` 冷却液开启，收尾继续由后处理输出 `M9`。
- 粗加工每层新增层号、Z 深度和 U/V 刀具中心边界注释，便于核对刀路是否被限制在槽内部。
- 斜坡下刀改为直接落到每层第一条扫描线起点，避免先落到槽中心再横向斜切到边界造成视觉偏移。
- `appendRampPlunge()` 统一先抬到进给平面再快速 XY，清理底面精加工和侧壁精加工前重复的 `G0 Z`。
- 螺旋下刀分支同样改为先到进给平面再快速定位，避免低 Z 快速横移风险。

### 已完成：正面开口槽优先归入当前加工面
- 修正槽分组仍被识别为侧面的情况：槽 `region` 判定现在优先检查关联面片是否在当前加工面最高 Z 处开口。
- `FeatureRecognizer` 在 STEP 重新识别阶段新增槽面片投影范围判断，若槽关联面片最高投影接近当前模型正面最高投影，则归为 `Front`。
- `MainWindow` 的设置加工面后重分类兜底同步使用面片 Z 范围，避免侧壁数量或槽轴方向把正面可加工槽带到 `Side`。
- 该修改只调整特征分组/加工面归类，不改变槽刀路生成算法。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：当前加工面优先加工排序
- 勾选多个特征时，策略面板默认优先选取当前激活加工面内的孔/槽作为加工入口。
- 批量孔加工按当前激活面优先分组，同组内继续使用最近邻排序，减少空程且保证当前面先加工。
- 批量槽加工按当前激活面优先生成 `contourBatch`，G 代码块和加入工序顺序与优先级一致。
- 工序列表接收当前激活面状态，“按流程排序”和“生成全部 G 代码”会将当前面工序排在其它面工序之前；同一面内部仍按原有阶段/用户顺序稳定排序。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：当前加工面状态与前置提示
- `MainWindow` 新增当前激活加工面状态，设置加工面成功后将选中面作为 `Front` 激活面并广播。
- `FeatureListPanel` 接收激活面变化后自动展开当前加工面分组、折叠其它分组并滚动到当前组，保留非当前面特征可查看。
- `StrategyPanel` 接收激活面状态，选中非当前面分组特征生成前给出确认提示，但不硬阻断，避免误挡可从当前面加工的槽。
- 撤销上一版对所有侧面槽的统一阻断，恢复为仅 `mill_slot` 开口槽侧面流程阻断，盲槽仍可继续测试。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证；首次链接因 EXE 正在运行失败，停止 `CNEXT-CAM.exe` 后重试通过。

### 已完成：撤销刀路穿透显示
- 撤销 `ViewportWidget::drawToolPath()` 中临时关闭 OpenGL 深度测试的改动，恢复刀路按模型遮挡关系显示。
- 这次修改只收敛显示层，避免模型内部或背面刀路被强制叠加后造成“路径更歪”的误判；槽加工坐标生成问题仍需单独修复。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：暂停侧面槽强制正面盲槽生成
- 停止将 `region=Side` 的槽自动改写为 `region=Front` 并套用 `mill_blind_slot` 的临时转换逻辑。
- 槽生成前对侧面槽统一拦截，避免坐标系未稳定时继续输出偏移或超深的危险刀路。
- 后续需要以单一坐标转换函数重做“侧面标签槽从正面加工”的专用流程，再恢复生成。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：刀路显示不完整修复
- 修复仿真刀路在模型内部或被模型表面遮挡时显示不完整的问题。
- `ViewportWidget::drawToolPath()` 绘制刀路线时临时关闭 OpenGL 深度测试，使 G0/G1 路径作为叠加层完整显示；绘制完成后恢复原深度测试状态。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧面槽正面加工 Z 顶面基准修正
- 修复侧面槽进入正面盲槽流程时仍用侧向识别深度计算 `zTop` 的问题；之前 `zTop=feature.center.z + depth/2` 会把槽口顶面抬到空中，导致长距离空中 ramp 后才接近材料。
- 侧面标签槽现在用竖向开口厚度计算正面加工顶面：`zTop=feature.center.z + width/2`，与 Z 加工深度上限保持一致。
- `MainWindow` 和 `StrategyPanel` 的槽轮廓转换均同步该 Z 基准，避免显示参数与生成参数不一致。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：盲槽切深与层间退刀优化
- 修复盲槽策略把每层切深强制限制为刀具直径的问题；现在 `stepDown` 直接按用户参数生效，只限制到目标总深度，避免 0.1mm 小刀导致每层只下 0.1mm。
- 盲槽层间和精加工内部退刀从全局 `safeHeight` 改为 `zTop + feedHeight`，大幅减少槽加工过程中反复抬到 50mm 的空跑。
- 工序开始和最终结束仍保留安全高度，避免跨工序移动风险。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧面槽正面加工的槽宽与 Z 深度分离
- 针对侧面槽正面加工只覆盖部分橙色槽口的问题，明确区分识别坐标系和加工坐标系：正面加工槽宽默认取侧面识别深度，Z 加工深度默认取侧面槽竖向开口厚度。
- `StrategyPanel` 选中侧面槽时，默认参数现在会传递加工坐标系下的 `slotWidth` 和 `depth`，用户修改 Z 加工深度不会影响槽宽。
- `MainWindow` 在匹配识别特征后同样按加工坐标系修正槽宽，避免生成时又恢复为侧面识别宽度。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧面标签槽正面加工只修正 XY 中心
- 针对 Z 深度已正确限制但刀路仍未落在橙色槽口内部的问题，新增侧面标签槽的 `footprint-center-only` 修正。
- 当关联面投影长度能匹配槽长、但投影宽度明显大于槽宽时，只采用投影中心来修正 `ContourFeature.center`，保留原槽长、槽宽和 Z 加工深度不变。
- 新增 `[slot-debug] footprint-center-candidate / footprint-center-applied` 日志，用于确认是否仅应用中心修正而未覆盖槽尺寸。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧面标签槽正面加工的 Z 深度上限
- 针对侧面标签槽在正面盲槽流程中默认 `D=5.000` 导致 Z 轴远超槽实际可切深度的问题，新增侧面槽正面加工深度上限：以槽竖向开口厚度 `width` 作为 Z 深度上限。
- 策略面板选中侧面槽时，槽加工默认“加工深度”改为该上限；用户输入更小深度时保留输入，输入更大深度时生成前自动限制到上限。
- 该修正不再改变槽长和槽宽，避免“加工深度影响加工宽度”的问题回归。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：撤销槽深影响槽宽的错误映射
- 撤销上一版将侧面槽 `depth` 映射到正面槽 `width`、再将侧面槽 `width` 映射到 Z 深度的逻辑；该逻辑会导致用户调整“加工深度”时间接影响刀路宽度，违反槽长/槽宽/Z 深度三者独立的原则。
- 槽从侧面标签转正面生成时现在只修正加工区域和误判的开口槽类型，不再交换或推导几何尺寸。
- “生成全部 G 代码”时，槽工序会移除 `params` 中残留的 `depth/slotLength/slotWidth/angle` 几何参数，统一使用工序保存的 `ContourFeature`，避免旧参数覆盖工序几何。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧面槽正面加工参数维度映射修正
- 根据最新日志，侧面槽保持 `A=90` 后方向正确，但仍把侧面槽的 `D=5.000` 当成 Z 加工深度，导致正面刀路 footprint 只按 `W=1.949` 展开，没有覆盖侧向 5mm 槽深范围。
- `mill_blind_slot` 对侧面标签槽转正面加工时，现在将侧面槽深度映射为正面槽宽，将侧面槽宽映射为 Z 加工深度，槽长度和方向角保持不变。
- footprint 候选校验改用转换后的 `ContourFeature` 期望长宽，便于确认 `all-feature-faces` 是否命中 `L≈2.777 / W≈5.000` 的正面加工范围。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：撤销侧面标签盲槽的错误角度旋转
- 根据 `footprint-candidate` 日志，当前槽在正面 XY 投影下主要沿原识别角 `A=90` 方向展开，之前将侧面标签盲槽强制 `angle - 90` 会把刀路转到 X 方向，造成刀路偏离选中槽范围。
- `mill_blind_slot` 对侧面标签槽转正面加工时现在只修正 `region` 和误判的 `open_slot`，不再修改槽方向角，保留用户/识别参数中的 `A=90`。
- 清理了本次修改产生的未使用角度归一化函数，已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：盲槽偏移排查增加底面 footprint 候选日志
- 为排查这两天改动导致槽刀路偏移的问题，侧面标签盲槽转正面加工时不再只看全部关联面投影，而是优先尝试 `faceIndices.first()` 作为槽底面 footprint 候选。
- 新增 `[slot-debug] footprint-candidate / footprint-applied / footprint-not-applied` 日志，输出 U/V 投影范围、候选长宽、期望长宽和是否采纳，用于判断偏移来自识别几何、面片集合还是刀路策略。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：侧面标签盲槽按选中面片投影修正中心
- 根据新日志，槽角度已成功转为正面 `A=0`，但刀路仍相对橙色选中槽偏移，说明问题转为中心点/投影范围仍沿用侧面识别结果。
- 对 `region=Side` 的直槽/弧槽，在转为正面盲槽加工前，会从选中特征的 `faceIndices` 收集网格顶点，并按正面加工角度重新计算 XY 投影中心、槽长和槽宽。
- 投影结果增加尺寸上限保护，避免误把整块侧面或无关大面纳入槽范围；该修正只影响侧面标签转正面盲槽的加工轮廓。

### 已完成：侧面标签盲槽转正面加工角度
- 根据 G 代码复核，当前槽以 `region=side` 进入盲槽策略时仍携带侧面识别角 `A=90`，导致刀路沿世界 Y 方向展开，未落在从正面看到的槽长方向内。
- 对 `mill_blind_slot` 且 `region=side` 的槽，在生成前转换为正面盲槽加工：加工角度减 90 度、加工区域标记为 `Front`，并把误判出的 `open_slot` 还原为 `straight_slot`。
- 单槽生成和勾选批量槽生成都应用同一转换；`strategy-selected` 调试日志新增最终 `region` 与角度，便于确认进入刀路时是否已经变为正面角度。

### 已完成：盲槽策略不再因侧面标签被阻断
- 根据实际测试日志，当前槽在识别和特征列表中仍保留 `region=side`，但用户选择的是 `mill_blind_slot` 正面盲槽分层加工流程。
- 生成前校验已收窄：只有 `mill_slot` 开口槽策略继续阻断侧面槽，`mill_blind_slot` 不再仅凭侧面标签阻断，允许按正面 Z 方向分层开槽。
- 该改动用于先打通当前槽的 G 代码生成闭环，后续仍需继续检查生成刀路是否完全约束在槽内部。

### 已完成：生成前按槽口可达性修正槽面区域
- 针对特征列表仍显示“侧面槽”导致生成前被阻断的问题，槽特征转换为加工轮廓时会用当前模型最高 Z 与槽深重新判断是否可从正面下刀。
- 当槽口位于当前正面最高面附近时，生成用的 `ContourFeature.region` 会修正为 `Front`，允许继续使用盲槽从上往下分层开槽；特征列表原始分组暂不强制改动。
- `[slot-debug]` 日志新增 `region` 字段，后续可直接确认校验前进入刀路生成的槽面区域。

### 已完成：槽装夹面改为按槽口位置判定
- 修正上一版仍依赖槽底面法向正反的问题：STEP 面法向可能朝上也可能朝下，单看底面 region 会把正面可下刀的槽误判成反面或侧面。
- 槽识别现在按当前正面方向计算模型投影范围，并用槽底位置 + 槽深估算槽口位置；槽口接近正面最高投影时判为正面槽，槽口接近最低投影时才判为反面槽。
- 真正轴向为侧向的槽仍判为侧面槽并由生成前校验阻断，避免侧面槽被投影到正面 XY 生成错误刀路。

### 已完成：正面可达槽按底面方向判定装夹面
- 修正槽装夹面识别逻辑：槽特征不再因为包含多个竖直侧壁就被判为侧面槽，而是优先使用槽底面/槽轴方向判断加工方向。
- 当前装夹重分类增加槽口 Z 可达性判断：槽口接近当前正面最高 Z 时按正面槽处理，允许从上往下一层层开槽；真正侧面轴向槽仍保留侧面阻断。
- 该改动只修正槽的加工方向判定，不扩大槽长宽刀路范围，也不恢复旧的开口槽强制切换逻辑。

### 已完成：侧面/反面槽禁止按正面 Z 槽铣生成
- 修复槽特征从识别结果进入策略面板和工序保存时丢失 `region` 的问题，`ContourFeature` 现在会保留正面、侧面、反面信息。
- 槽铣生成前新增装夹面校验：当前盲槽/开口槽流程只允许正面 Z 装夹槽，侧面槽和反面槽会直接提示并阻断生成，避免把侧面槽投影成正面 XY 刀路。
- 同步保存/加载槽工序的 `region` 字段，避免项目重新打开后槽面信息丢失。

## 2026-05-29

### 已完成：盲槽粗加工余量参数生效
- 复查槽加工链路时发现 `BlindSlotMillingStrategy` 虽然已有 `stockToLeave` 参数，但粗加工范围实际只按刀具半径缩进，没有把用户设置的加工余量计入刀心范围。
- 盲槽粗加工现已按 `toolRadius + stockToLeave` 计算刀心缩进，保证粗加工不会直接扫到成品槽壁/槽底边界。
- 当某一层因为余量、刀具直径或斜面收缩导致没有有效粗加工范围时，现在会跳过该层粗扫并输出调试注释，不再在无效范围内强行下刀；最终清底和轮廓精加工仍按成品尺寸生成。

### 已完成：孔口倒角改为切向斜入斜出
- 根据后续开发计划中的 P1 安全项，修正 `ChamferStrategy` 原先从孔中心在倒角深度横向进给到圆周的问题，避免刀具在已加工孔口/孔底区域留下径向划痕。
- 孔口倒角新增 `leadLength` 参数，默认 1mm，并按实际倒角刀心圆半径自动钳制，避免小孔倒角时引入线过长。
- 新刀路改为：快速定位到圆周切点的切向外侧 R 平面，`G1 X/Y/Z` 斜向切入到倒角深度，完成一圈 `G2` 后再切向斜出到 R 平面，不再回孔中心收刀。

### 已完成：绑定 GitHub 远程仓库
- 根据用户提供的远程仓库地址 `https://github.com/overjjjj/gcodecnc.git`，准备将本地 Git 仓库绑定到 GitHub `origin` 并推送主分支。
- 当前本地默认分支为 `main`，已有首个项目提交；后续代码和项目进度记录将通过 Git 版本管理同步。

### 已完成：GitHub 接入本地仓库准备
- 在项目根目录新增 `.gitignore`，排除 Qt/qmake 构建产物、Debug/Release 目录、本地 `.pro.user` 配置、临时目录、崩溃转储和二进制产物。
- 该准备用于后续初始化 Git 仓库并绑定 GitHub 远程地址，避免把本地编译输出和个人环境配置提交到远程仓库。

### 已完成：盲槽默认改为槽长方向斜线进刀
- 根据实际加工风险复盘，确认盲槽在 `helixRadius=0` 时直接垂直下刀存在折刀风险，尤其是槽宽小于两倍刀径时。
- `BlindSlotMillingStrategy` 新增 `rampAngle` 参数，默认 3 度；粗加工层、底面精加工首刀和轮廓精加工入口现在会沿槽长方向生成 ramp 斜线进刀。
- ramp 进刀先快速定位到斜线起点、下到槽口顶面，再用 `G1 X... Y... Z...` 连续斜降到目标层深；若槽内可用长度不足，则保留垂直下刀兜底，避免生成越界刀路。
- 该修正只处理盲槽进刀安全性，不改开口槽策略，也不改槽识别逻辑。

### 已完成：批量生成 G 代码增加工序分段注释
- 根据用户对“孔口倒角 G 代码看起来像前面精修轨迹”的反馈，确认当前问题主要出在 `生成全部 G 代码` 会把多道孔工序直接串接，文本里缺少清晰的工序边界提示。
- `MainWindow` 的批量生成入口现已为每个输出块追加工序分段注释，包含工序号、策略名、特征摘要和刀具号，便于直接从 G 代码中区分“扩孔/精修/倒角”等相邻工序。
- 该修正只增强批量 G 代码可读性与核对流程，不改 `ChamferStrategy`、扩孔策略或任何孔/槽刀路算法。

### 已完成：工序列表与批量 G 代码定位联动
- 针对用户截图中“选中孔口倒角工序但 G 代码仍停在圆插补精修段”的问题，`OperationListPanel` 现已在选中行变化时发出当前工序号。
- `MainWindow` 会根据批量 G 代码中的 `; ---- Operation N ----` 分段标记自动跳转并高亮对应工序段，方便逐道核对刀路。
- 批量 G 代码分段注释改为纯 ASCII 策略 ID 和 `Operation N`，避免中文注释在不同编码环境中显示成乱码。
- 进一步补齐“生成前已选中某道工序”的场景：批量 G 代码生成完成后会读取当前工序号并主动跳转一次，避免因为选中行没有变化而停留在前一道工序。

### 已完成：设置正面后通孔正反面归类修正
- 根据用户反馈确认：点击“设置正面”后，通孔会整体被归到反面，导致当前单装夹正面加工流程下全部无法生成。
- `MainWindow::reclassifyFeaturesForSetup()` 现已对 `through_hole` / `countersunk_through_hole` 特殊处理：不再只按 `-feature.axis` 推断开口面，而是按孔中心更接近当前模型 `bbMax.z()` 还是 `bbMin.z()` 来归类到正面或反面。
- 该修正只影响“设置正面后的装夹重分类”，不改孔识别本身的半径、深度和子类型。

### 已完成：槽默认策略按开口侧判定并补充策略日志
- `StrategyPanel` 的槽默认策略不再只看 `subType == open_slot`，改为同时识别 `MachiningFeature.openSide`；只要识别结果已经给出开口侧，就默认落到 `mill_slot`，避免“直槽识别正确但默认仍走盲槽铣”。
- `MainWindow` 在槽工序生成入口新增 `strategy-selected` 调试日志，直接打印当前 `strategyId`、槽子类型和 `openSide`，用于区分“界面默认选错策略”和“策略选择正确但生成结果错了”两类问题。
- 下一步：重新编译并让 `槽.stp` 复测，重点确认日志里 `strategy-selected id=` 是否为 `mill_slot`，以及 G 代码头部是否从 `Blind slot geometry` 切换为 `Open slot geometry`。

### 已完成：检测出开口侧后直接升级为开口槽
- 根据用户实测日志确认：`detectOpenSlotSide()` 当前能正常返回开口侧，但 `toContourFeature()` 之前只写 `openSide`，没有把 `subType` 从 `straight_slot` 升级成 `open_slot`，导致后续仍落入盲槽策略。
- `MainWindow::toContourFeature()` 现已改为：只要网格边界检测出有效开口侧，就同步把 `ContourFeature.subType` 设为 `open_slot`，让策略选择、约束校验和 G 代码生成统一走开口槽链路。
- 下一步：重新编译后复测 `槽.stp`，确认日志中的 `requested-after-recognized-slot subtype=` 变成 `open_slot`，且 G 代码头注释切换为 `Open slot geometry`。

### 已完成：开口侧判定增加槽端外侧材料覆盖校验
- 根据最新 `slot-debug` 日志确认，当前问题已经收敛为 `detectOpenSlotSide()` 未能识别出开口侧，导致 `openSide` 一直为 `0.000`，槽继续按盲槽处理。
- `MainWindow::detectOpenSlotSide()` 现已从“仅比较槽两端到模型 XY 包围盒边界的距离”升级为“双判据”：同时统计槽端外侧局部区域是否还有三角网格材料覆盖。
- 现在只要某一端外侧明显缺料，而另一端外侧仍有材料覆盖，就优先把该端判成开口端；仅当边界距离和材料覆盖都显示两端封闭时，才继续返回 `0.0`。
- 下一步：复测 `槽.stp`，确认 `openSide` 不再为 `0.000`，并联动把 `subtype` 升级为 `open_slot`。

### 已完成：槽策略纠正后移到识别回填之后
- 根据用户复测日志确认：`requested-after-recognized-slot` 已经正确变成 `subtype=open_slot openSide=1.000`，但 `strategy-selected` 仍然保持 `mill_blind_slot`，说明原先在入口 `feature` 上做的策略纠正时机太早。
- `MainWindow` 现已把“盲槽铣 -> 开口槽铣”的强制纠正放到识别槽回填 `requestedFeature` 之后执行；只要识别结果已确认是开口槽，就重新绑定 `mill_slot` 策略对象。
- 同时新增 `strategy-initial` 调试日志，区分“UI 初始请求策略”和“识别纠正后的最终执行策略”。

### 已完成：开口槽开口侧符号约定对齐
- 根据最新复测结果确认：开口槽已经正确切到 `mill_slot`，但刀路仍朝错误一端展开，问题已经从“策略选错”收敛为“开口侧符号方向反了”。
- `SlotMachiningGeometry` 现已统一 `openSide` 和 `openSign` 的符号约定：当识别结果给出 `openSide > 0`（+U 端开口）时，内部 `openSign` 反向映射为 `-1`，以匹配现有开口槽粗加工、引入和侧壁精加工代码的方向假设。
- 该修正不改槽长、槽宽、角度和深度，只修正开口槽从哪一端进刀、往哪一端收口。

### 已完成：开口槽粗加工宽度按平底有效宽度收口
- 根据最新仿真截图确认：开口端方向已切换正确，但粗加工扫描仍按矩形总槽宽展开，导致刀路在槽外侧成排扫出。
- `SlotMachiningGeometry` 现已在 `roughHalfWidth` 上增加平底有效宽度约束：当识别到槽宽方向斜面/开口收缩时，粗加工半宽会进一步受 `slopeMinWidth/slopeMaxWidth` 和刀具半径限制。
- 该修正优先保证开口槽粗加工刀心留在识别到的真实槽底宽度内，减少外扩扫刀。

### 已完成：开口槽高风险补丁回退，恢复稳定路径基线
- 根据用户要求先回到“今天最开始 G 代码生成没问题”的状态，已撤回后续叠加在 `mill_slot` 上的高风险行为改动，包括：`openSign` 反向映射、按平底宽度强制收窄 `roughHalfWidth`、以及把粗加工线强行裁到 `bottomU` 范围内。
- 当前保留的仅是已验证必要的两层修正：槽工艺入口不再把槽放大成整块模型；识别为开口槽后可从 `straight_slot` 升级到 `open_slot` 并切到 `mill_slot`。
- 下一步会在这条更稳定的基线之上，单独重做“开口端进入逻辑”，避免继续混入会破坏原始粗加工扫描的补丁。

### 已完成：暂停把直槽强制切到开口槽策略
- 经过多轮复测确认，当前 `mill_slot` 这套开口槽矩形往复粗加工算法本身不适合该槽型；问题不再是识别和策略切换，而是开口槽原始路径核心不对。
- `MainWindow` 现已撤回“识别出 `open_slot` 后强制把 `mill_blind_slot` 改成 `mill_slot`”这一步，先恢复到更可控的盲槽路径生成链路，避免继续把错误刀路作为默认结果输出。
- 后续如果要真正上线开口槽工艺，需要单独重写 `mill_slot` 的粗加工策略，而不是继续在当前矩形行切算法上叠补丁。

### 已完成：槽调试入口日志收口
- 在确认当前阶段先回退到稳定盲槽生成链路后，已移除 `strategy-initial` 这类只用于追踪策略切换时机的临时日志，减少后续测试时的噪声。
- 保留的 `slot-debug` 仅继续服务于几何核对：界面参数覆盖后、匹配到识别槽后、以及最终批量送入策略前的状态。

### 已完成：槽加工入口调试日志
- `MainWindow` 为槽工序生成入口新增 `qDebug` 调试输出，覆盖“界面参数覆盖后”、“匹配到识别槽后”和“最终批量送入策略前”三段关键几何状态。
- 日志内容包含槽子类型、中心点、槽长、槽宽、加工深度、方向角、开口侧以及当前参数表中的 `slotLength/slotWidth/depth/angle`，用于判断问题出在 UI 覆盖、槽特征匹配还是策略入口。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽工艺入口网格细化收紧
- 根据 `slot-debug` 日志确认：槽工序生成前的 `toContourFeature(..., &mesh)` 会在 `refineSlotContourFromMeshData()` 中把“槽附近点”扩展到整块模型网格，导致小槽被放大成外轮廓方向的大槽。
- `SlotMachiningGeometry.cpp` 现已移除该二次扩展采样，只保留槽特征自身 `faceIndices` 对应的网格点来细化槽中心、长宽、方向和斜面范围，避免槽工艺入口再次把整块模型识别进去。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`debug/CNEXT-CAM.exe` 已重新生成。
- 下一步：重新用 `槽.stp` 跑一遍槽生成，确认 `requested-after-recognized-slot` 与识别槽尺寸保持一致，不再被放大到外轮廓。

### 已完成：开口槽开口端判定采样增强
- `MainWindow` 中 `detectOpenSlotSide()` 从“槽长方向单点取样”改为“端面中心 + 宽度方向左右偏移三点采样”，综合最小边界距离和平均边界距离判断开口端。
- 该修正用于减少斜放槽、开口边不完全居中或端部局部缺料时，把开口端误判到反方向的问题。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前开口端判定仍基于模型 XY 包围盒边界距离，不是沿槽轴真实求交；对内部开口、复杂外轮廓或非矩形毛坯仍可能不够精确。

### 已完成：开口槽粗加工范围校验与斜面提取收口
- `SlotMachiningGeometry` 不再要求槽底低位采样同时在长向和宽向都满足阈值才保留斜面信息，改为分别提取长度方向和宽度方向的底部轮廓；这样开口槽在局部采样不足时，长度方向斜面不再整段丢失。
- `SlotMillingStrategy` 的开口槽粗加工层生成前新增有效范围校验，按当前层的 `U` 向有效长度和 `roughHalfWidth` 先检查刀心偏移是否越界，异常时直接中止生成。
- 每层粗加工现在会在 G 代码里输出 `Open slot rough layer` 注释，带出当前层 `Z`、`U` 范围和半宽，便于后续联动仿真和排查槽刀路问题。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前开口槽粗加工的越界校验仍是基于策略几何模型，不是基于真实网格逐段求交；如果识别出的开口方向或槽轴本身有误，仍需要继续在特征识别层修正。

## 2026-05-28

### 已完成：WCS 多坐标系第一期
- `PostProcessorOptions` 新增 `workOffset` 字段，默认 `G54`，用于把当前工件坐标系选择传入后处理器。
- `SiemensPostProcessor` 和 `FanucPostProcessor` 在程序头 `G90/G91` 后输出当前 `G54-G59`，孔、槽、型腔、轮廓等所有策略生成的程序统一受该坐标系选择控制。
- `MainWindow` 工具栏新增 `G54-G59` 下拉框，单次生成、圆铣、铣削批量生成和工序列表“生成全部”都会使用当前下拉框选择。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前只做 WCS 选择输出，不自动计算侧面/反面装夹的坐标旋转矩阵；侧面孔、反面孔仍需要用户在机床侧提前设置对应 WCS 偏置并配合正确装夹。

### 已完成：型腔开粗螺旋下刀
- `PocketRoughingStrategy` 新增 `helixRadius` 和 `helixPitch` 参数，默认在型腔中心附近走 `G2 ... Z...` 螺旋下刀后再进入往复式行切。
- 螺旋半径会按型腔可加工范围自动钳制，型腔太小或参数无效时保留原有垂直下刀兜底，避免小型腔直接生成越界螺旋。
- 估算时间同步把螺旋圆周长度计入总刀路长度，避免新增下刀路径后时间明显偏低。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前螺旋仍按型腔中心圆形轨迹处理，不检测中心岛、复杂边界和真实材料去除状态；复杂型腔后续需要边界裁剪和碰撞层继续兜底。

### 已完成：盲槽宽度方向斜边精加工补齐
- `BlindSlotMillingStrategy` 在最终层已有起端/末端斜面精加工的基础上，继续补入宽度方向左右两侧斜边的 `G1 X/Y/Z` 三轴成形线。
- 左右侧斜边现在按最终槽底有效长度范围排线，刀心从槽口顶面可加工边界走到槽底边界，和底面精加工、端部斜面精加工顺序衔接。
- 新增注释 `Blind slot left side slope finish` / `Blind slot right side slope finish`，便于后续做 G 代码运行联动和仿真问题定位。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前盲槽斜边精加工仍按线性斜坡近似，不包含角部混合过渡、圆角端部裁剪和实体级刀具包络校验。

### 已完成：开口槽侧壁精加工与平底范围对齐
- `SlotMillingStrategy` 的开口槽最终层侧壁精加工不再按 `roughMinU/roughMaxU` 整个槽长范围走刀，而是改为严格使用 `bottomStartU/bottomEndU` 的平底直壁范围。
- 该修正避免把入口/出口斜面段误当成垂直侧壁继续精加工，减少最终层侧壁刀路与前面底面精加工、斜面精加工之间的边界重叠和几何失真。
- 侧壁精加工段新增独立注释 `Open slot side wall finish`，方便后续做 G 代码运行联动和问题定位。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前开口槽侧壁精加工仍按两侧直壁矩形路径处理，不包含侧壁真实倒角/圆角、机床侧补引入引出优化和实体级碰撞包络校验。

### 已完成：盲槽最终层补齐底面精加工
- `BlindSlotMillingStrategy` 最终层新增 `Blind slot bottom finish` 清底段，不再只做分层粗加工后直接收敛到四边轮廓精加工。
- 清底线会按最终成品槽底可加工范围生成更细步距的往复式底面精加工，并根据长边/短边自动选择主扫描方向。
- 盲槽侧壁轮廓精加工范围无效时，当前也会尽量保留底面精加工，不再因为四周轮廓收尾不可用而整段跳过最终清底。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前盲槽底面精加工仍基于矩形化局部范围，不包含圆角端部、复杂封闭槽轮廓裁剪和实体材料去除校验；这部分后续需要和几何边界层进一步统一。

### 已完成：平面加工主入口闭环
- `StrategyPanel` 现在会把 `FlatSurface` 特征直接映射到 `mill_face` 和 `mill_surface_finish`，不再把平面误当成普通开/闭轮廓铣入口。
- 右侧策略面板新增平面特征文案、平面加工策略名称和提示语；平面场景下默认优先选择较大直径立铣刀，避免面铣入口默认落到最小刀具。
- `FaceMillingStrategy` 和 `SurfaceFinishStrategy` 改为直接使用识别到的平面 `length/width` 生成往复式平面刀路，并在边界外补半刀径覆盖，不再退化为基于 `radius` 的正方形占位刀路。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前平面加工仍按识别框的矩形范围生成，不支持复杂岛屿边界、局部避让和任意旋转面域精确裁剪；这部分后续要在平面边界提取层补齐。

### 已完成：快速下刀纳入刀具包络碰撞检查
- `SimulationController::detectCollisionMessage()` 的快速移动检查从“快速 XY”扩展为所有 `G0` 快速移动，包括快速下刀和斜向快速移动。
- 检查沿三维快速段按长度采样，并继续复用刀具半径足迹点与局部三角网格表面高度比较。
- 当快速 Z 向移动进入毛坯包络时给出独立警告，避免只横移报警而漏掉高速下刀扎入毛坯的风险。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前检查仍基于静态毛坯网格，不知道前序工序已去除的材料；后续需要材料状态模型才能减少已加工区域内的误报。

### 已完成：快移碰撞检查纳入刀具半径近似
- `SimulationController::detectCollisionMessage()` 的快速移动检查从刀尖中心点扩展为刀具包络近似采样。
- 每段快速 XY 移动会沿路径采样中心点和 8 个刀具半径方向的足迹点，按局部三角网格表面高度判断是否过近。
- 无有效刀具直径时自动退回中心点检查；有刀具直径时可捕捉刀具边缘贴近台阶、槽壁或凸台的风险。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前仍是圆柱足迹近似，不使用真实 STL 刀具网格做扫掠碰撞，也不考虑材料已被去除后的动态毛坯。

### 已完成：内置仿真刀具模型资源
- 新增 `untitled/tool_models/`，内置 9 个可直接使用的仿真 STL：D3/D6/D10 立铣刀、D3/D6 钻头、D4 90 度中心钻、M6 丝锥、D10 90 度倒角刀、D50 面铣刀。
- 这些模型为项目内生成的占位资源，尺寸单位为 mm，最低 Z 已按刀尖对齐，适配当前仿真视口的刀具模型加载规则。
- `ViewportWidget` 增加相对路径解析，会从程序目录和项目目录查找 `tool_models/...`，保证 Debug 目录运行时也能加载项目内模型。
- `ToolLibrary::loadDefaults()` 会为匹配的默认刀具自动绑定模型路径；现有 `debug/tools.json` 也已同步写入 9 条 `modelPath`。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：模型为视觉仿真用简化模型，不代表具体品牌刀具的真实刃型和夹持结构。

### 已完成：刀具模型支持二进制 STL
- `ViewportWidget` 的刀具模型加载器新增 binary STL 解析，按 STL 80 字节头、三角面数量和 50 字节三角记录读取法向与顶点。
- `.stl` 导入现在先按二进制 STL 判断，文件尺寸不匹配时自动回退到 ASCII STL 解析；`.obj` 支持保持不变。
- 二进制 STL 导入后仍复用原有模型归一化逻辑：XY 居中、最低 Z 对齐刀尖、随仿真刀尖移动。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前 STL 模型仍只用于显示，尚未参与刀具体积碰撞检测；大模型暂未做三角面抽稀或缓存优化。

### 已完成：真实刀具模型导入与仿真显示
- `ToolEntry` 新增 `modelPath` 字段，`ToolLibrary` 保存/加载 `tools.json` 时同步持久化刀具模型路径。
- 刀具编辑对话框新增“刀具模型”选择入口，可为单把刀具绑定 `.obj` 或 `.stl` 文件；未绑定模型时继续使用原有简化圆柱刀具显示。
- `SimulationController` 解析 G 代码中的 `T` 号时同步记录当前刀具模型路径，并通过 `toolModelPathChanged` 信号传给视口。
- `ViewportWidget` 新增轻量模型加载与显示：支持 ASCII OBJ 和 ASCII STL 三角面片，加载后自动 XY 居中、最低 Z 对齐刀尖，并随仿真刀尖位置移动。
- 已通过 `mingw32-make -f Makefile.Debug` 编译链接验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前模型仅用于视觉显示；暂不支持二进制 STL、STEP 刀具模型，也未把真实刀具网格纳入碰撞检测。

### 已完成：仿真快移检查升级为网格局部高度采样
- `SimulationController::detectCollisionMessage()` 的快速移动预警从全局包围盒最高点判断，升级为沿快速 XY 段采样并用三角网格 XY 投影求局部最高表面 Z。
- 快速移动只有在采样点低于局部毛坯表面安全余量时才报警，减少台阶、斜面、槽口等模型上因全局最高点导致的误报。
- 仍保留刀路明显低于毛坯底部的检查，作为异常深度/坐标系错误的快速兜底。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前只检查刀心点的局部表面高度，不计算刀具半径扫掠体和材料去除后的动态毛坯。

### 已完成：仿真层基础碰撞警告
- `SimulationController::detectCollisionMessage()` 从占位改为基础检查：加载 G 代码后按模型包围盒检查低位快速 XY 移动，以及刀路明显低于毛坯底部的情况。
- `loadGCode()` 解析刀路后会发出 `collisionChecked` 信号；`MainWindow` 接收后在底部状态栏显示碰撞/越界警告。
- 该检查覆盖所有进入仿真的 G 代码，包括槽加工生成的 `G0/G1` 路径和由圆弧/固定循环展开后的仿真线段。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前仍是包围盒级别的快速预警，不做三角网格求交，也不计算刀补后的完整刀具体积。

### 已完成：开口槽生成后刀路基础校验
- `SlotMillingStrategy` 新增开口槽 G 代码回读校验，在返回结果前解析生成的 `G0/G1` 运动行并按槽本地坐标复核刀心范围。
- 校验覆盖低位快速 XY 移动、刀心 U/V 是否越过允许槽范围、Z 是否低于槽底；开口侧允许引入/引出长度，避免误判正常进退刀。
- 校验失败时直接返回错误信息，不输出可能越界或低位快移的开口槽刀路。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前仍是刀心线级别的基础校验，不替代实体碰撞检测；G2/G3 圆弧和刀补后的真实包络需要后续更完整的仿真/验证层处理。

### 已完成：开口槽斜面精加工成形线
- `SlotMillingStrategy` 新增 `feedXYZ()` 和开口槽斜面精加工辅助函数，在最终层清底后按槽几何中的 `slopeStartLength/slopeEndLength` 输出 `G1 X/Y/Z` 三轴插补成形线。
- 起端/末端斜面分别按 `roughMinU -> bottomStartU`、`bottomEndU -> roughMaxU` 生成，Z 方向从顶面到槽底或从槽底回顶面，宽度方向按刀具半径限制后的多条偏移线覆盖。
- 斜面长度无效或刀具半径导致可加工宽度不足时自动跳过，不影响已有粗加工、底面精加工和侧壁精加工流程。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前斜面精加工仍基于识别到的线性斜面长度，不做实体级刀具包络/碰撞求交；复杂曲面过渡槽仍需要后续验证层兜底。

### 已完成：G 代码行高亮与仿真联动
- `ToolMove` 新增 `sourceLine`，G 代码解析时把每一段 `G0/G1/G2/G3` 运动和 Siemens 固定循环展开运动绑定到原始 G 代码行号。
- `SimulationController` 新增 `currentGCodeLineChanged(int)` 信号和 `seekToGCodeLine(int)` 方法；仿真播放时会同步发出当前运行行，非播放状态可按 G 代码行跳转到对应刀具位置。
- `GCodeEditor` 新增当前执行行高亮、清除高亮和 `lineSelected(int)` 信号；用户点击/移动到某一行时可驱动仿真定位，程序自动设置文本或自动滚动时不会误触发跳转。
- `MainWindow` 接通双向联动：播放时编辑器黄色高亮当前运行行；暂停/停止/未播放状态下，点击 G 代码行会让仿真刀具位置同步到该行对应轨迹。
- 已执行 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前联动按“源 G 代码行 -> 第一段对应运动”定位；一行圆弧或固定循环展开为多段时，点击该行定位到该行第一段/当前播放段，不提供行内分段选择。

### 已完成：G 代码编辑器语法高亮
- 新增 `GCodeHighlighter`，基于 `QSyntaxHighlighter` 对 G 代码编辑器内容做轻量语法高亮。
- 高亮规则包括：`G/M/X/Y/Z/F/S/T` 地址字母蓝色加粗、数字黑色、`;` 注释灰色斜体、`MCALL/CYCLE83/CYCLE84/CYCLE85` Siemens 循环关键字紫色加粗。
- `GCodeEditor` 构造时挂载高亮器，并切换为浅色背景以保证数字和注释可读。
- `untitled.pro` 已加入新增的 `GCodeHighlighter.h/cpp`。
- 已执行 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 补充确认：当前主界面已有“导出 G 代码”入口，导出扩展名由后处理器决定；Siemens 输出 `.mpf`，Fanuc 输出 `.nc`。

### 已完成：开口槽底面精加工与基础几何验证
- `SlotMachiningGeometry` 新增 `validateSlotMachiningGeometry()`，集中校验开口槽加工前的几何、刀具直径、步距、进给、进给起始高度、安全高度、粗加工范围和槽底有效长度。
- `SlotMillingStrategy` 生成最终层时新增“Open slot bottom finish”底面精加工段，在侧壁 U 形精加工前按槽底有效范围生成受限 zigzag 清底线。
- 开口槽策略改为复用几何层验证结果，减少策略内部重复校验，为后续槽/轮廓/平面统一验证层打基础。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。
- 残留风险：当前底面精加工仍是刀心线级别清底，不包含实体材料去除校验；斜面真实成形和快速移动穿模检测仍需后续补齐。

## 2026-05-27

### 已完成：槽加工几何调试注释
- 盲槽 G 代码块开头新增几何注释，输出实际进入策略的槽长、槽宽、深度、角度、长向/宽向斜面收缩量和最终精加工范围。
- 开口槽 G 代码块开头新增几何注释，输出实际槽几何、粗加工 U 向范围、半宽、开口方向、斜面长度和平底范围。
- 这些注释不改变刀路执行，只用于后续排查：如果仿真出现空缺或越界，可以直接通过 G 代码判断是识别/几何重算问题还是路径生成问题。

### 已完成：槽策略几何参数二次覆盖修正
- 排查确认：槽口网格重算后的 `length/width/angle/depth` 已写入 `ContourFeature`，但生成 G 代码时策略仍通过 UI 参数里的旧 `slotLength/slotWidth/angle/depth` 再次覆盖，导致刀路仍只按中心旧范围加工。
- 槽加工生成流程现在先把用户修改过的几何参数合并进 `ContourFeature`，再从传给策略的参数中移除几何字段，只保留安全高度、进给、步距、转速等工艺参数。
- 该修正保证网格重算后的完整槽口范围和实际方向能够真正进入盲槽/开口槽策略。

### 已完成：槽口方向网格 PCA 重算
- `refineSlotContourFromMeshData()` 不再直接使用原始识别角度作为槽长方向，而是先收集槽特征面片和附近槽口候选点，再通过 XY 主成分重新计算槽长轴。
- 附近点搜索仍使用原始角度作为粗过滤，实际槽长/槽宽、中心和斜面收缩量改为基于 PCA 后的本地坐标重新投影。
- 该修正用于解决识别角度偏差时，盲槽刀路只覆盖中心区域、长向槽口两侧未被纳入加工范围的问题。

### 已完成：盲槽粗加工长边自适应走刀
- 盲槽粗加工不再固定沿本地 `U` 方向切削、沿 `V` 方向排线，而是根据当前层有效加工范围自动选择长边作为主切削方向。
- 当槽长/槽宽命名或识别角度导致本地轴与实际长边不一致时，每层仍会优先沿较长范围走刀，并沿较短范围密集排线。
- 该修正用于解决斜面盲槽只清理中心区域、长向两端未被覆盖的问题。

## 2026-05-26

### 已完成：槽铣走刀顺序修正
- 开口槽粗加工改为每层连续 zigzag：第一条线下刀，后续相邻扫描线直接进给横移连接，最后统一退刀到安全高度，不再每条扫描线单独 `G0/G1 Z/G0` 进退刀。
- 开口槽最终层新增 U 形精加工路径：从开口侧引入，沿一侧壁走到封闭端，跨过封闭端，再沿另一侧壁退回开口侧退出。
- 盲槽最终精加工前新增安全抬刀和引导点重定位，避免从粗加工最后一条扫描线末端直接在槽底横扫到精加工引导点。

### 已完成：槽斜面网格重算兜底
- `refineSlotContourFromMeshData()` 不再只依赖槽特征自身 `faceIndices` 取样，新增槽中心附近的局部网格搜索窗口。
- 当斜面面片没有被识别进同一个槽特征时，仍可从附近网格顶点推导槽口/槽底范围，用于计算 `slopeStartLength/slopeEndLength/slopeMinWidth/slopeMaxWidth`。
- 该修正用于解决盲槽多层走刀中斜面参数为 0，导致各层扫描范围不随深度递减的问题。

### 已完成：盲槽斜面范围异常保护
- 收窄槽斜面局部网格搜索窗口，避免把槽外大平面误采样进槽口范围。
- 盲槽策略对长向/宽向斜面收缩量增加总量钳制，防止异常斜面参数把刀心加工范围压成负数。
- 当最终精加工范围仍异常时，先跳过精加工而不是中断整条槽粗加工 G 代码生成。

### 已完成：盲槽槽口/槽底几何定义修正
- 放宽槽底区域判定，支持底部较窄、带斜边的槽参与斜面参数计算。
- `refineSlotContourFromMeshData()` 输出的槽长/槽宽改为保持槽口范围，`slope*` 字段单独表达从槽口收缩到槽底的差值。
- 槽中心同步改为槽口包围盒中心，避免用槽底中心生成分层路径导致整体偏移。

### 已完成：盲槽小刀分层与入刀点修正
- 盲槽实际每层切深增加刀具直径限幅，避免 T31 这类小直径刀仍按参数 `stepDown=2` 一刀下到过深位置。
- 每层入刀点改为当前层有效加工范围中心，不再固定使用槽口中心，避免斜面槽在无效区域垂直扎刀。

### 已完成：盲槽横向排线步距限幅
- 盲槽粗加工横向扫描线距不再直接使用 UI 步距，而是按 `min(用户步距, 刀具直径 * 0.45)` 自动限幅。
- 该修正避免 T31 这类小刀仍按 1mm 横向步距排线，导致槽宽方向只切少数几条线、材料清理不足。

### 已完成：盲槽粗加工覆盖边界修正
- 盲槽粗加工刀心边界改为仅按刀具半径内缩，不再额外叠加 `stockToLeave`。
- 该修正避免 T31 小刀在 `余量=0.1` 时每侧少清 `0.15mm`，导致槽宽方向视觉上仍未覆盖完整。

### 已完成：盲槽宽向斜边递减与深度限幅
- `ContourFeature` 新增宽度方向斜边参数 `slopeMinWidth/slopeMaxWidth`，网格重算时从槽口范围和槽底范围的宽度差中提取。
- 盲槽粗加工每层现在同时按长向斜坡和宽向斜边计算本层有效刀心范围，支持侧边斜面随深度逐层递减。
- 盲槽加工深度增加限幅：参数中的加工深度不能超过识别到的槽深，避免有底面槽被人为参数打穿。

### 已完成：盲槽斜面分层范围修正
- 盲槽粗加工不再所有层使用同一个固定槽长范围，而是根据当前层深比例和 `slopeStartLength/slopeEndLength` 动态计算本层有效切削范围。
- 有入口/端部斜面的槽现在会在浅层覆盖更接近槽口的范围，随着层深增加逐步收缩到槽底平底范围，避免斜面区域漏加工或最终层过切。
- 最终精加工轮廓同步改为使用槽底有效范围，避免把槽口斜面长度当作底部成品轮廓直接精修。

### 已完成：盲槽默认取消自动螺旋下刀
- 修正盲槽策略中 `helixRadius=0` 仍会自动生成螺旋下刀的问题。
- 现在螺旋下刀半径为 `0` 时明确不使用螺旋，盲槽每层会直接下到当前层深，再按槽长和槽宽排线清槽。
- 只有用户显式设置 `helixRadius > 0` 时才启用螺旋下刀，避免默认刀路看起来仍是螺旋线而不是分层清槽。

### 已完成：盲槽每层宽度覆盖修正
- 修正盲槽粗加工宽度排线逻辑：不再简单从一侧按 `stepover` 递增到另一侧，避免槽宽小于步距时每层只走一条线。
- 每层粗加工现在强制包含两侧边界刀心线和中心线，并在中间按步距补线，保证每层都覆盖槽长和槽宽。
- 该调整符合“槽加工应一层一层往下，每层都清完整长宽范围”的加工逻辑。

### 已完成：盲槽粗精加工流程修正
- 盲槽策略调整为按层粗加工、最终层再执行轮廓精加工，避免每层都重复走成品轮廓。
- 修正 `G40`/CAM 侧补偿下的盲槽精加工中心线：精加工不再沿“粗加工余量后”的刀心范围空跑，而是按刀具半径偏置到成品槽壁。
- 该修正用于承接有底面槽默认走盲槽铣后的完整加工流程，避免留下余量未清和工序重复。

### 已完成：有底面槽策略路由修正
- 修正策略面板对槽特征的默认选择：只有识别为 `open_slot` 的槽才默认使用开口槽铣，`straight_slot`、`arc_slot`、`blind_slot` 等有底面槽默认使用盲槽铣。
- 取消槽特征切换时沿用上一槽策略的行为，避免刚加工过开口槽后再选有底面槽时仍保留 `mill_slot`，导致按通槽逻辑打穿模型。
- 开口槽策略和生成前校验新增保护：非 `open_slot` 特征不能使用开口槽铣，会提示改用盲槽铣。

### 已完成：开口槽改为按层水平切削
- 根据槽铣粗加工逻辑调整开口槽刀路：粗加工不再用 `G1 X/Y/Z` 从槽口斜穿到当前层深，而是先定位到该层在入口斜面上的合法切入点，再下到当前层 `Z`，随后沿该层水平切削。
- 侧壁精加工同步改为在平底起点下刀并水平精加工侧壁，避免侧壁精加工继续沿三轴斜线穿过模型。
- 清理 `SlotMillingStrategy` 中不再使用的三轴斜线输出辅助函数，后续斜面成形需要单独策略实现，不再混入粗加工主线。

### 已完成：开口槽入口斜坡穿模修正
- 回退入口进刀斜坡的强制拉长逻辑：刀具斜坡长度不再超过识别到的入口斜面范围，避免为了降低下刀角度而让 `G1 X/Y/Z` 直线穿过实体模型。
- 当前先保证刀路不脱离真实槽几何边界；斜坡过陡的问题后续通过减小斜面段层深、碰撞校验或专用斜面成形刀路处理。

### 已完成：开口槽入口进刀斜坡限深修正
- 修正开口槽粗加工入口斜坡过短导致下刀过深的问题：刀具进刀斜坡不再只按识别到的模型斜面长度比例计算，而是增加最小水平进刀距离约束。
- 粗加工每层会按当前层深保证足够的水平斜坡距离，避免 `Z-2.000/Z-4.000/Z-5.000` 在入口处过早到达。
- 侧壁精加工也复用该入口斜坡约束，避免最终层侧壁精加工沿过短斜面扎到槽底。

### 已完成：开口槽侧壁精加工斜坡进刀修正
- 修正侧壁精加工直接在槽底起点垂直下到最终深度的问题：侧壁精加工现在从开口端顶面进入，再沿入口斜面 `G1 X/Y/Z` 下到最终深度，随后沿侧壁直线精加工。
- 这样侧壁精加工与粗加工使用一致的开口斜面进刀逻辑，避免在斜面/平底交界处直接扎到 `Z-5.000`。
- 当前仍只覆盖平底有效长度范围内的两侧直壁，底面精加工和真实斜面成形继续放在后续步骤。

### 已完成：开口槽基础侧壁精加工
- 开口槽在最终层新增基础侧壁精加工，两侧直壁各补一条精加工线，先建立槽宽到尺寸的基本闭环。
- 当前版本只加工平底有效长度范围内的两侧直壁，不包含底面精加工和斜面成形；侧壁精加工暂按 CAM 侧刀心偏置实现，机床侧 `G41/G42` 的开口槽精加工仍保留到后续阶段。
- 这样可以在不扩大当前风险面的前提下，先把开口槽从“只有粗加工”推进到“粗加工 + 侧壁基础精加工”。

### 已完成：开口槽基础几何校验
- 开口槽生成前新增基础几何校验：如果粗加工有效长度无效，或两端斜面已经把平底区域压缩到极短范围，则直接阻止生成 G 代码。
- 该校验用于拦住明显异常的槽几何识别结果，避免继续输出“表面上能跑、实际斜面和平底边界已经失真”的刀路。
- 当前阈值采用最小 `0.10mm` 和 `0.1D` 二者取大值，作为平底有效长度下限。

### 已完成：开口槽斜面识别条件收紧
- 收紧槽底低 Z 区域判定条件：只有当低 Z 区域在槽宽方向覆盖足够完整时，才将其视为真实槽底区域并据此计算两端斜面长度。
- 这样可以减少局部三角面、倒角残片或不完整关联面把槽底误识别成窄条区域，进而把 `slopeStartLength/slopeEndLength` 拉长的问题。
- 当前规则对槽长方向和槽宽方向分别处理，优先保证槽宽识别稳定，再用长度差推导两端斜面范围。

### 已完成：开口槽单向粗加工退刀修正
- 开口槽无出口斜面时，粗加工线不再沿槽底反向退回入口斜面后再抬刀，改为在当前末端位置直接进给抬到槽口顶面，再快速抬到安全高度。
- 这样开口槽粗加工保持单向切削，减少槽底重复回切、多切和视觉上“路径没走完但其实是在回刀”的混淆。
- 本次调整属于 `P1` 阶段的粗加工闭环收口，只修改 `SlotMillingStrategy` 的开口槽单线收刀逻辑。

### 已完成：槽加工专项方案与阶段规划整理
- 在 `项目需求说明书.md` 新增“8.5 槽加工专项方案与阶段规划”，统一整理槽加工的识别层、几何层、粗加工层、精加工层和验证层。
- 文档中明确了当前已完成、进行中和未完成项，并给出后续 `P1/P2/P3/P4` 的推进顺序，后续槽加工开发将以该专项规划作为对照基线。
- 本次仅更新文档，不涉及代码逻辑和可执行文件变更。

### 已完成：开口槽斜面刀心深度补偿
- 修正开口槽入口斜面“刀路走得过深”的问题：槽几何里的斜面长度不再直接作为刀心斜坡长度使用，而是补上 `0.5D` 刀具半径补偿后再生成 `G1 X/Y/Z` 斜坡段。
- 这样最终深度到达点会沿槽长方向后移半个刀径，避免刀具前缘在斜面尚未走完时就提前吃到最终槽底深度。
- 该调整同时作用于起端斜面和末端斜面，当前先保持现有粗加工结构不变，只修正斜面中心轨迹与实际切削包络不一致的问题。

### 已完成：开口槽深度与退刀修正
- 修正槽网格重算把深度压成关联面 `meshDepth` 的问题：识别/参数已有槽深时优先保留该深度，网格深度只作为缺失深度兜底，避免只生成 `Z-1.600` 一层。
- 开口槽无出口斜面时，不再在闭口端原地升 Z；刀路会沿已加工平底退回入口斜面，再沿入口斜面回到顶面后快速抬刀，减少闭口端多切风险。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽粗加工路径 P2 重构
- `SlotMillingStrategy` 新增开口槽粗加工线生成函数，按“开口端进刀、斜面段、平底段、出口斜面/原地进给抬刀”组织单条切削线，避免在策略主流程中散落拼接路径。
- `SlotMachiningGeometry` 开始记录开口方向，开口端长向刀心边界只保留粗加工余量，闭口端仍保留刀具半径与余量，减少槽长加工不到位的问题。
- 斜面段按当前层深动态计算平底起止点，浅层不会直接按最终槽底斜面全长切削，最终层再完整覆盖斜面到槽底。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽网格重算逻辑迁入几何层 P1
- `SlotMachiningGeometry` 新增 `refineSlotContourFromMeshData(...)`，集中完成槽关联面投影、槽底低 Z 区域、实际深度和斜面长度识别。
- `MainWindow` 不再内联维护槽网格重算算法，只调用几何层函数，后续粗加工、精加工、校验和诊断可复用同一槽几何来源。
- 当前迁移保持既有识别结果输出不变，目标是先消除 UI 与策略层各自推导槽几何造成的偏差。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽加工几何层 P0 初版
- 新增 `SlotMachiningGeometry`，集中计算开口槽局部坐标、槽长/槽宽、顶面/深度、刀具半径与余量内缩、粗加工刀心边界、斜面起止段和平底段范围。
- `SlotMillingStrategy` 改为从 `SlotMachiningGeometry` 读取刀心边界和斜面段，不再在策略内部重复推导槽长、槽宽、角度、半径补偿和斜面兜底。
- 该版本先保持现有开口槽粗加工输出行为，目标是为后续真实斜面识别、精加工和过切检测建立统一几何入口。

### 已完成：开口槽斜面端 Z 插补刀路
- `ContourFeature` 新增 `slopeStartLength`、`slopeEndLength`，用于记录槽局部长度方向两端的斜面/倒角过渡长度，并同步工程保存/加载。
- `MainWindow` 在槽轮廓重算时，根据整体槽投影范围与槽底低 Z 区域的差值，自动计算两端斜面长度。
- `SlotMillingStrategy` 的开口槽粗加工线改为“起端斜坡段 + 平底段 + 末端斜坡段”：斜坡段使用 `G1 X/Y/Z` 同步插补，避免只加工平底直线而漏掉斜面区域。
- 针对部分 STEP 识别只关联槽直壁、没有把斜面面片纳入槽特征的问题，增加保底入口斜坡：当两端斜面长度都为 0 时，按槽长比例生成起端 Z 插补斜坡，避免继续垂直下到槽底。
- 修正斜面抬刀和长向刀具半径补偿：开口槽长向刀心边界按刀具半径加余量内缩；无出口斜面时，刀路会沿已加工平底退回入口斜坡并在槽口顶面抬刀，避免在槽内深处直接快速抬刀造成多切。
- 根据仿真反馈取消无出口斜面时的整槽反向退刀，改为在当前末端原地进给抬到顶面后再快速抬高，避免退刀路径重复切削槽底。

### 已完成：开口槽按槽底区域裁剪刀路
- 修正开口槽“线距正确但没有完全加工”的问题：槽轮廓重算不再直接使用所有关联面投影范围，而是优先使用槽底低 Z 点的投影边界作为可加工槽底区域。
- 取消 `SlotMillingStrategy` 里对开口槽两端统一固定缩短的斜面避让，避免非斜面端也被截短导致槽底加工不完整。
- 当前逻辑会把斜面/倒角面从粗加工矩形范围中剔除，策略只负责按刀具半径和余量限制刀心边界。
- 根据仿真反馈继续调整：槽宽仍按槽底低 Z 区域控制，槽长恢复使用整体槽投影范围；开口槽长向刀心边界不再额外扣半个刀径，避免长向加工不到位。

### 已完成：开口槽深度重算与斜面端避让
- 修正槽深被右侧默认参数覆盖的问题：槽几何从网格重算出实际 Z 深度后，只有用户手动修改深度时才覆盖，不再无条件把识别默认 `depth=5` 写回策略。
- `MainWindow` 在槽特征转加工轮廓时，会根据关联三角面的 `Z min/max` 重算当前 setup 下的加工顶面和实际下刀深度，避免把贯通/侧向跨度误当 Z 向槽深。
- `SlotMillingStrategy` 对开口槽长度方向加入斜面端避让，粗加工线两端收缩一个安全量，避免直接切入斜面过渡区。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽粗加工取消槽外长引入
- `SlotMillingStrategy` 的开口槽粗加工取消基于 `openSide` 的槽外长距离引入，避免生成 `Y-5.xxx` 这类明显远离槽体的刀路。
- 粗加工现在从槽内安全边界起刀，到另一侧安全边界结束，所有横向清宽线都限制在槽矩形安全范围内。
- 开口外引入后续需要等开口方向稳定识别后再作为可选进刀策略恢复。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽多线粗加工侧向越界收紧
- `SlotMillingStrategy` 的开口槽多线粗加工增加刀心侧向安全边界，粗加工横向极限至少预留 `0.25D`，避免仿真中侧向刀路贴边/越过槽壁。
- 多线偏移生成改为从中心线向两侧扩展，并按刀具直径限制有效步距，减少一次性跳到槽边导致的越界风险。
- 该调整先保证粗加工安全，侧壁到尺寸后续应通过独立精加工/刀补线处理。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽按槽宽生成多条切削线
- `SlotMillingStrategy` 的开口槽粗加工不再只生成单条中心线；现在会根据槽宽、刀具直径和步距生成多条平行切削线，覆盖槽宽方向。
- 每条切削线仍从开口端外侧进刀，再沿槽长方向切到封闭端，保留开口槽从槽口进刀的加工意图。
- 当用户设置的步距大于槽宽时，会自动按刀具直径限制有效步距，避免又退化成一条中心线。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

## 2026-05-25

### 已完成：开口槽 XY 刀路按关联面投影重算
- 修正开口槽默认刀路仍对不上槽的问题：生成槽加工前不再直接信任识别阶段的槽中心/长宽估算，而是从槽特征 `faceIndices` 对应的网格三角面重新投影到当前 setup 的 XY 平面，按实际投影边界重算 `center`、`length`、`width` 和 `angle`。
- 右侧参数表的默认槽长/槽宽/角度不再覆盖这次重算结果；只有用户把参数改成不同于识别默认值时，才显式覆盖重算几何。
- 批量勾选多个槽时，每个槽都会用各自关联面重算几何，避免套用当前槽的旧中心线估算。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽加工顶面 Z 修正
- 修正识别槽进入加工策略时的 Z 基准：槽识别给出的 `center.z` 是槽几何中心，开槽策略需要的是槽口顶面 Z；现在槽特征转换为 `ContourFeature` 时会使用 `center.z + depth / 2` 作为加工顶面。
- 覆盖右侧策略面板直接生成、主窗口批量/加入工序转换、旧铣削弹窗预填三个入口，避免继续生成 `G0 Z-0.100` 这类贴近/进入工件的进给起始高度。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：设置加工面后按新 setup 重新识别特征
- `StepImporter` 新增带旋转的 STEP 重载入口，设置加工面时先把原始 STEP 形体变换到新 setup 坐标，再重新生成网格并重新跑特征识别，避免继续复用/旋转旧特征导致槽方向、面归属和加工侧判断出错。
- `ProjectManager` 开始保存原始 STEP 路径；`AppController` 新增 setup 重识别流程，重识别成功后会刷新项目网格和特征，并清空旧工序、旧刀路和旧 G 代码。
- `MainWindow` 的“设置正面”确认链路改为 OCC 主路径下重新导入识别，非 OCC 构建才保留旧的旋转网格/旋转特征 fallback。
- `FeatureListPanel` 的特征分组顺序改为正面优先，其次侧面、反面、未知，让设置加工面后的当前加工面特征优先显示和选择。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：开口槽按开口端进刀，不再按闭口矩形收刀
- `ContourFeature` 新增 `openSide`，`MainWindow` 会在把槽特征送入策略前，结合当前 setup 下的网格包围边界推断开口槽的槽口方向，保证“设置正面”后仍能识别哪一端是开口端。
- `SlotMillingStrategy` 的 `open_slot` 生成逻辑改为按槽口侧进刀：粗加工每刀都从槽口外侧切入，再沿槽长切到封闭端；精加工改为 `U` 形轮廓，不再像闭口槽那样补出整圈矩形把槽口封死。
- 修正识别为 `straight_slot` 但端部实际贴模型边界的槽：这类槽现在也会通过 `openSide` 进入开口槽刀路分支，避免继续按普通闭口槽生成螺旋下刀。
- `MainWindow` 的槽生成入口增加最近槽匹配兜底：即使左侧高亮行停在平面特征，右侧面板当前槽也会按中心点回找项目槽特征并重新补上 `openSide`。
- `mill_slot` 策略已收紧为固定开口槽刀路，不再保留普通闭口槽的螺旋下刀分支；闭口/盲槽后续统一走 `mill_blind_slot`。
- 开口槽粗加工进一步收敛为中心线开槽：每层只从槽口外侧进到槽中心线，再沿槽长切到封闭端；暂时取消开口槽侧壁轮廓精加工，避免继续生成偏到工件下方的矩形/折线路径。
- 修正 setup 切换后的槽方向角来源：不再用重分类时的面组 PCA 覆盖槽角度，而是旋转原始槽长方向向量，避免槽长方向被误算成 `-90°` 后导致刀路沿错误轴移动。
- 这一轮额外补入了槽策略的 `compensation` 参数，为后续刀具半径补偿继续落地保留接口；`SimulationController` 也预留了撞刀检测信号和刀具几何字段，但这两项还没有形成完整闭环。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽流程几何参数贯通与生成校验收口
- `ContourFeature` 新增 `width`、`length`、`angle` 字段，槽特征从识别结果进入铣削对话框、工序列表、工程保存/加载时不再只剩中心点和半径。
- `MillingOperationDialog` 生成槽工序时会把识别到的槽长、槽宽和方向角写入 `ContourFeature`，`ProjectManager` 同步补齐这些字段的序列化与反序列化。
- `MainWindow` 的普通铣削生成链路新增槽专用前置校验；“生成全部”遇到无效槽工序时会直接阻止继续生成，避免混入错误 G 代码。
- 已通过 `qmake untitled.pro -spec win32-g++ CONFIG+=debug CONFIG+=qml_debug` 与 `mingw32-make -f Makefile.Debug` 编译验证，`untitled/debug/CNEXT-CAM.exe` 成功生成。

### 已完成：开口槽/盲槽策略与工序显示收口
- 重写 `SlotMillingStrategy` 与 `BlindSlotMillingStrategy`，统一按槽长、槽宽、角度和层深生成刀路，槽策略不再依赖缺省的固定 50x10 尺寸。
- 开口槽与盲槽策略都补上了可读的英文显示名和错误提示，避免继续显示历史乱码文本。
- `OperationListPanel` 重写工序显示文案，槽工序改为显示 `L/W/Z` 关键尺寸，封闭/开放轮廓、槽、面铣等类型名称也改为可读文本。

### 已完成：槽批量建工序与槽相关界面文案清理
- `MainWindow` 的铣削生成链路新增“多槽批量”行为：当用户勾选多个槽特征并选择槽策略时，会按同一套刀具和参数为所有勾选槽一起生成 G 代码并批量加入工序列表。
- `FeatureListPanel` 与 `StrategyPanel` 相关槽界面文案改为可读中文/英文，当前选中槽会显示槽长、槽宽和深度，不再保留本轮路径上的乱码提示。
- 这次调整继续通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽专用右侧参数面板与参数覆盖贯通
- `StrategyPanel` 改为按当前特征上下文切换策略集：选中槽时，右侧直接显示开口槽/盲槽策略、刀具和参数表，不再强制走 `MillingOperationDialog` 弹窗。
- 槽右侧参数表补入 `depth`，并把 `depth/slotLength/slotWidth/angle` 真正覆盖到 `ContourFeature`，保证“右侧改参数 -> 生成 G 代码/加入工序”不是只改显示值。
- `MainWindow` 的槽批量生成链路同步套用这些覆盖参数，勾选多个槽时，批量生成、工序加入和后续“生成全部”看到的是同一份槽几何快照。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：槽点选误切回孔的选择链修正
- `MainWindow` 新增统一的特征面板切换辅助逻辑，避免把非孔特征继续送进 `StrategyPanel::setFeature(...)`，右侧策略栏不再把槽错误显示成钻孔。
- 视口点选命中槽/平面等非孔特征时，当前行为改为优先选中实际命中的特征，不再因为附近有孔就自动吸附回孔特征。
- 勾选多个特征时，若当前高亮特征仍在勾选集合内，右侧策略栏优先保持当前选中对象，不再无条件回退到“第一个孔优先”。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 已完成：以选中面作为当前 setup 的正面切换
- `MainWindow` 的“设置正面”不再只是按法向重分类孔/面/槽，而是把当前项目网格和特征整体旋转到新的 setup 坐标，让现有基于 `XY/Z` 的孔加工和 2.5D 槽/轮廓策略可以直接沿所选面法向加工。
- 切换 setup 时会同步重算特征 `region`、平面子类型和槽方向角，并清空旧工序、旧刀路和 G 代码缓存，避免把旧装夹下生成的数据继续用于新装夹。
- `onStepImported()` 现在会把项目网格同时同步给仿真控制器，保证导入模型和后续 setup 切换后，仿真基准网格与视口保持一致。
- 已通过 `mingw32-make -f Makefile.Debug` 增量编译验证，`untitled/debug/CNEXT-CAM.exe` 已重新生成。

### 当前残留
- `StrategyPanel.cpp`、`FeatureListPanel.cpp`、`MainWindow.cpp` 等文件仍有历史乱码文案，本次只处理了槽闭环直接相关路径，未做整库文本清洗。
- 槽流程目前已完成“识别 -> 右侧改参数 -> 批量建工序 -> 生成全部 -> 保存/加载”的闭环，但还没有对圆弧槽细分专用走刀，也没有按槽类型拆开的更细参数模板。

## 2026-05-22

### 已完成：槽加工流程 UI 闭环第一步
- `StrategyPanel` 新增 `clearFeature()`，切到槽、型腔等非孔特征时会清掉旧的孔状态，避免策略面板继续保留上一次孔加工的上下文。
- `MainWindow` 的特征选择/勾选联动已补齐槽分支：勾选集合里没有孔但有槽时，会优先把槽送入铣削入口，不再只对孔特征生效。
- `OperationListPanel` 的槽工序摘要改为按 `L/W/Z` 展示，`mill_slot` / `mill_blind_slot` 不再沿用通用轮廓的 `R/Z` 摘要，便于区分开槽与普通轮廓铣。
- 待验证：重新编译并用槽特征走一遍“识别 -> 添加工序 -> 生成 G 代码”的 UI 链路。

### 已完成：孔识别朝向阈值回调
- `FeatureRecognizer::findHoles()` 将孔圆柱面的朝向过滤从“必须明显朝内”放宽为“只拦截明显外侧且既不像孔也不像贯通孔的圆柱”，避免因法向/网格噪声把正常孔漏掉。
- 这次调整只影响孔识别的入口筛选，不改孔的孔口/盲孔/螺纹分类逻辑。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：外部槽误识别边界收紧
- `FeatureRecognizer::findSlots()` 新增模型外包络边界判定，槽候选如果贴着整体模型的投影极值，就不再当作槽底或开口槽墙组处理，避免把外表面、背面和侧面拼成一个“外部槽”。
- 该收紧只作用于槽候选的边界判定，不改动已识别的内槽/盲槽的主流程。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：槽底深度回退修正
- `FeatureRecognizer::findSlots()` 的槽底深度计算新增回退：当“平行邻面”法算不出有效深度时，改用侧壁沿槽轴的几何跨度兜底，避免像 `槽.stp` 这类真正的槽因为底面没有对应平行面而被漏识别。
- 这次修正只作用于槽候选的深度判定，不放宽底面/侧壁/长宽比的前置约束，避免把整张平面重新误识别成槽。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：开口槽第二套识别
- `FeatureRecognizer::findSlots()` 在原有“按槽底识别”的流程后，新增一套“按 4 个凹边连通内壁识别开口槽”的补充分支，用于识别没有独立槽底面的贯通/开口槽。
- 新增墙面组几何校验：必须能分成两组相对平行且彼此近似正交的内壁，长宽比满足槽特征，并且不存在可作为底面的共用平面邻面，避免把普通台阶侧壁误判成槽。
- `MillingOperationDialog` 对 `open_slot` 改为默认选择 `mill_slot`，不再一律按盲槽策略处理。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：孔外圆柱误判过滤与槽侧壁收敛
- `FeatureRecognizer::findHoles()` 增加圆柱法向朝向校验，只保留法向朝向轴心的内孔圆柱面，外圆角/外圆柱面不再直接进入孔识别。
- 孔识别不再把与孔轴垂直的大开口平面并入 `feature.faceIndices`，避免点选孔特征时把整张外表面一起高亮成孔。
- `FeatureRecognizer::findSlots()` 新增槽侧壁筛选，只保留贴近槽底两侧边的侧壁参与槽判定；同时把 `processedBottoms` 延后到槽真正识别成功后再消耗，避免候选过早被跳过。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：槽大平面误识别过滤与中文主界面恢复
- `FeatureRecognizer` 的平面包围盒改为按主方向重新计算长宽和方向，并新增槽侧壁贴边校验；只有侧壁真正贴近槽底两侧边时才保留槽候选，避免“大平面 + 局部凹边”被整面识别成槽。
- `MainWindow::retranslateUi()` 恢复按当前语言输出主菜单、工具栏、仿真按钮和停靠面板标题，中文模式不再统一回退成英文。
- `SlotMillingStrategy.cpp` 与 `BlindSlotMillingStrategy.cpp` 的槽工序错误提示恢复为中文可读文本。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：槽识别按共面底面组消耗
- `FeatureRecognizer::recognize()` 里槽特征不再只把 `slot.faceIndices.first()` 记入 `consumedBottomFaces`，改为按 `collectCoplanarFaces(...)` 一次消耗整组共面底面，避免同一槽被后续流程拆开。
- 清理了 `FeatureRecognizer.cpp` 中遗留的重复 `findSlots()` 声明，并修复了 `reclassifyRegions()` 里被乱码注释吞掉的控制流，保证槽识别模块可继续编译。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：反面孔生成禁用与批量按参数分批
- `MainWindow` 新增孔位装夹限制校验，当前单一正面 Z 向流程下，侧面孔和反面孔都会在生成前被阻止，避免直接输出错误刀路。
- `generateRequested` 和 `generateAllRequested` 统一复用同一套孔位校验逻辑，单孔生成和“生成全部”不再出现校验口径不一致。
- `generateAllRequested` 的孔工序分批条件从“同策略 + 同刀具”收紧为“同策略 + 同刀具 + 同参数”，避免不同参数的孔被错误合并为同一批次输出。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：工程状态检查与后续开发排期梳理
- 确认实际 Qt/C++ 工程位于 `untitled/`，根目录为文档与工作区总目录，当前根目录不是 Git 仓库。
- 确认项目进度规则：每次功能新增、功能删除、修复问题、行为调整或构建/部署规则变更，都必须在 `项目进度.md` 当日节追加记录；后续以根目录 `项目进度.md` 作为总账。
- 检查主要模块：STEP/OCC 导入、AAG 特征识别、孔/铣削策略、Siemens/Fanuc 后处理、仿真、刀具库、工序列表、CNC 串口接口均已有源码入口。
- 编译验证通过：已执行 `qmake` 与 `mingw32-make -f Makefile.Debug`，生成 `untitled/debug/CNEXT-CAM.exe`；当前警告为 OpenCAMLib vendored source 缺失，OCL 支持禁用。
- 风险记录：`untitled/设计方案.md` 和部分源码用户提示文本仍存在历史乱码，需要作为近期质量任务处理。

### 后续开发进度安排
- P0：进度记录规范化。收敛根目录与 `untitled/项目进度.md` 两份进度记录，明确唯一总账；新增/删除功能时同步写明改动文件、行为变化、验证命令和残留风险。
- P1：编码与中文提示清理。修复 `设计方案.md`、`CncCommInterface.cpp`、`SurfaceFinishStrategy.cpp` 等乱码文本，统一文档和源码编码为 UTF-8。
- P2：高频铣削闭环。优先完善封闭/开放轮廓、直槽/盲槽/圆弧槽、铣平面从“识别特征 -> 自动带入参数 -> 生成 G 代码 -> 仿真显示”的完整链路。
- P3：坐标系与多加工面。实现正面/反面/侧面装夹坐标变换或明确禁用生成逻辑，避免反面/侧面特征直接输出错误 Z 向刀路。
- P4：工程文件与刀具库持久化。补齐 `.cnext` 保存模型路径、特征、工序、策略参数、刀具、后处理器和 G 代码；刀具库独立 JSON 持久化。
- P5：后处理与仿真增强。完善 Fanuc 输出选择、G 代码高亮、工序手动排序、仿真圆弧/循环覆盖验证和基础碰撞/过切检查。
- P6：高级策略。按需求补齐铣螺纹、圆锥、清角、动态铣、3D 边线铣、3D 选面铣、毛坯/库存模型和加工边界。

### 已完成：圆插补铣孔改为连续螺旋下刀
- `HoleCircularMillingStrategy` 从“每层下到底后走整圆”的台阶式刀路改为连续螺旋刀路，`G2` 现在携带 `Z` 轴连续下降。
- 螺旋节距继续复用 `stepDown` 参数，含义调整为“每圈下刀量”；到底后仍保留底部精修圈 `springPasses`。
- `HoleCircularMillingStrategy` 的显示名和错误提示改为可读中文。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：槽开槽流程接入与编译收口
- `MainWindow.cpp` 接入槽工序校验，槽工序在生成前会检查槽长、槽宽、刀具直径和切深，批量生成时也会继续按槽工序类型拦截无效参数。
- `SlotMillingStrategy.cpp` 与 `BlindSlotMillingStrategy.cpp` 补齐槽参数校验，去掉了会导致编译失败的历史乱码提示文本。
- 删除了孔生成路径里误加的槽校验分支，圆插补孔流程不再误走槽限制判断。
- `MainWindow.cpp` 的 `retranslateUi()` 残留断行已清理，相关提示文本统一改为可编译的 ASCII 文本。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证，`debug/CNEXT-CAM.exe` 成功生成。

## 2026-05-20

### 已完成：圆插补铣孔改为连续螺旋下刀
- `HoleCircularMillingStrategy` 从“每层下到底后走整圆”的台阶式刀路改为连续螺旋刀路，`G2` 现在携带 `Z` 轴连续下降。
- 螺旋节距继续复用 `stepDown` 参数，含义调整为“每圈下刀量”；到底后仍保留底部精修圈 `springPasses`。
- `HoleCircularMillingStrategy` 的显示名和错误提示改为可读中文。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：孔倒角圆弧刀路与点钻刀具类型
- `ChamferStrategy` 改为孔口圆弧倒角刀路：倒角刀下到倒角深度后沿孔口半径 `G2` 绕孔一圈，不再只在孔中心下压。
- `ToolLibrary` 将默认中心钻归类为 `spot_drill`，避免孔定点默认选择麻花钻。
- `StrategyPanel` 的孔定点策略优先筛选中心钻，排屑钻/深孔钻避免选择大于目标孔径的钻头。
- `ToolLibraryPanel` 修复刀具类型和标题中文乱码。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：孔收尾倒角刀具约束与中文修复
- `ToolLibrary` 默认刀具库改为正常中文名称，并新增默认倒角刀 `D6/D10/D12 90°`。
- `StrategyPanel` 修复当前加工策略区域的中文乱码，孔倒角策略会自动过滤为倒角刀，不再默认显示麻花钻。
- `ChamferStrategy` 增加刀具类型校验，孔倒角/去毛刺必须使用 `chamfer_mill`，避免生成用麻花钻下压的错误收尾 G 代码。
- `StrategyBase` 默认错误提示改为中文可读文本。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：仿真圆弧插补与刀具模型
- `SimulationController` 的 G 代码解析补齐 `G2/G3` 圆弧插补离散，圆弧铣孔不再在仿真中退化为直线或漏画完整圆。
- 仿真解析会跟踪 `T` 刀具号，并从刀具库读取当前刀具直径，随每段运动传递给视口。
- `ViewportWidget` 新增简化刀具模型显示，按当前刀具直径绘制刀具圆柱和刀柄，替代单一黄色十字光标。
- `MainWindow` 接入仿真刀具直径信号，播放/停止时刀具模型与当前位置、刀具号保持同步。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

## 2026-05-18

### 已完成：孔加工最近邻排序
- `HoleStrategyUtils.h` 新增 `sortHolesByNearestNeighbor()`，贪心最近邻启发式在 XY 平面对孔位排序，减少空程移动距离。
- `MainWindow.cpp` 引入 `HoleStrategyUtils.h`，批量孔生成前调用排序，避免按特征列表索引顺序加工导致走刀迂回。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证（全部 .o 目标编译通过）。

### 已完成：孔定点需求替换与视频功能审查
- 审查 `D:\桌面\仰高科技` 目录下 26 个教学视频缩略画面，覆盖孔加工、铣削、特种加工、3D 加工和绘图流程。
- 重写 `项目需求说明书.md` 中 `3.3.1 孔定点加工`，将其明确为中心定位/点钻工序。
- 新增 `7. 视频审查补充需求`，补充默认参数、排屑钻、深孔钻、攻牙、铰孔、铣边、铣槽、铣平面、圆锥、铣螺纹、清角、动态铣、3D 边线铣、3D 选面铣、画图、中心线等需求。

### 已完成：项目进度文件编码清理
- 根目录 `项目进度.md` 清理为 UTF-8 中文文本。
- 保留当前关键进展记录，避免继续追加乱码。

### 已完成：视频功能与当前实现查漏补缺
- 对照视频、当前需求说明书和 `untitled/src` 实际实现，梳理出未实现或未闭环功能清单。
- 在 `项目需求说明书.md` 新增 `8. 视频功能与当前实现查漏补缺清单（2026-05-18）`。
- 清单覆盖孔加工批量闭环、封闭/开放轮廓铣、槽加工、铣平面、圆锥、铣螺纹、清角、动态铣、3D 边线铣、3D 选面铣、画图、中心线、默认参数、工程保存、后处理、仿真校验、毛坯库存模型、探测找正等功能缺口。

### 已完成：孔定点批量加工第一版
- `SpotDrillingStrategy` 新增批量孔生成接口，一次输出多个孔位的中心钻刀路，避免每个孔重复换刀和主轴启动块。
- `MainWindow` 生成 G 代码时会读取特征栏已勾选孔位；未勾选时回退到当前选中的孔特征。
- 排屑钻、深孔钻、攻牙、铰孔、孔倒角也接入同一勾选孔集合，先形成批量生成闭环。
- `HoleStrategyUtils` 修正孔口 Z 计算，孔定点深度从真实孔口向下计算，不再从孔圆柱中心直接下刀。
- `StrategyBase` 默认批量接口和默认错误提示改为中文，减少界面英文残留。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：铣削策略 UI 入口第一版
- 新增 `MillingOperationDialog`，可从界面选择铣平面、型腔开粗、轮廓精加工、曲面精加工 4 种已注册铣削策略。
- `StrategyPanel` 的铣削分组新增“添加铣削工序”入口，支持填写中心、顶面 Z、半径/半宽、加工深度和策略参数。
- `MainWindow` 接入铣削工序信号，生成铣削 G 代码并同步加入 `OperationListPanel`。
- `OperationListPanel` 的铣削工序标签改为显示策略名、尺寸和深度，开粗/精加工策略按工序类型归类。
- `ToolLibrary` 新增默认立铣刀/面铣刀，避免铣削入口只能选到钻孔刀具。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：开孔 G 代码固定循环化
- `SpotDrillingStrategy` 改为输出 `G81/G82` 孔定点固定循环，支持批量孔位排序后连续输出 XY。
- `PeckDrillingStrategy` 改为输出 `G83` 排屑钻固定循环，单孔和多孔不再展开为多段 `G0/G1` 手写下刀。
- `DeepHoleDrillingStrategy` 改为输出 `G83` 深孔钻固定循环，并移除 UI 中不再生效的局部退刀参数。
- `StrategyPanel` 增加策略参数缓存，切换开孔策略后保留用户改过的参数。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：Siemens 840D 孔循环后处理
- 开孔策略内部改为输出 `CNEXT_HOLE_CYCLE` 元数据，保留孔口参考平面、返回平面、安全距离、孔底、步距和进给。
- `SiemensPostProcessor` 读取孔循环元数据并转换为 `MCALL CYCLE81/CYCLE82/CYCLE83`，不再在 Siemens 840D 后处理下直接输出 Fanuc 风格 `G81/G83`。
- 多孔且循环参数一致时，后处理器只输出一次 `MCALL CYCLE...`，后续连续输出孔位 `X/Y`，最后用 `MCALL` 取消循环。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：攻丝/铰孔批量与仿真展开
- `TappingStrategy` 新增 `QVector<HoleFeature>` 批量接口，批量孔位会按最近邻排序并输出攻丝循环元数据。
- `ReamingStrategy` 新增 `QVector<HoleFeature>` 批量接口，批量孔位会按最近邻排序并输出铰孔循环元数据。
- `SiemensPostProcessor` 新增 `CYCLE84` 攻丝和 `CYCLE85` 铰孔输出，不再直接输出通用 `G84/G85`。
- `SimulationController` 的 Siemens 循环解析扩展到 `CYCLE84/CYCLE85`，攻丝/铰孔在仿真路径里会展开进刀和退刀动作。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：阶段一 UI 补齐
- `BottomBar` 增加仿真速度下拉框，支持 0.25x、0.5x、1x、2x、5x、10x。
- `MainWindow` 将速度下拉框接到 `SimulationController::setSpeed()`，仿真速度 UI 形成闭环。
- `StrategyPanel` 增加“编辑开孔参数”按钮，弹窗编辑当前孔策略参数并同步回参数表。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：型腔开粗行切路径
- `PocketRoughingStrategy` 从占位式方形同心框线改为分层往复行切清腔。
- 矩形型腔按半宽生成完整行切；圆形型腔按圆边界裁剪每条扫描线，刀路不会越过有效圆边界。
- 同步修复该策略文件内的中文显示乱码，错误提示和策略名称恢复为中文。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：轮廓精加工引入/引出
- `ContourFinishStrategy` 增加直线引入和引出，刀补 `G41/G42` 在引入线前开启，离开轮廓后再 `G40` 取消。
- 圆形轮廓支持每层切深后整圆精加工；点列轮廓支持开放/封闭点列加工。
- 新增 `leadLength` 参数用于控制引入长度，并将策略名称、错误提示恢复为中文。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：工序列表刀具同步修复
- `OperationListPanel` 新增“用当前刀具”按钮，可将右侧策略面板当前刀具应用到选中工序；未选中时应用到全部工序。
- 批量孔生成后，工序列表会把本次参与生成的每一个孔都加入列表，不再只记录当前单个孔。
- `StrategyPanel::currentTool()` 调整为公开只读接口，供工序列表同步当前刀具使用。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：孔径与刀具匹配校验
- 新增孔径/刀具直径辅助校验，排屑钻和深孔钻在刀具明显小于目标孔径时输出底孔加工提示注释。
- 铰孔策略增加硬校验：铰刀直径必须接近目标孔径，避免用错误刀具生成成品孔代码。
- 攻丝策略增加螺距和刀具直径匹配校验，避免错误丝锥加工非匹配螺纹孔。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：圆插补铣孔策略
- 新增 `HoleCircularMillingStrategy`，作为 `hole_circular_mill` 开孔策略注册到策略工厂。
- 可直接对识别到的孔特征生成圆插补铣孔 G 代码，自动按目标孔径和刀具直径计算刀心圆半径。
- 支持分层切深、进给/下刀速度、余量和光刀圈数参数，适合将钻后的底孔铣到成品孔径。
- `StrategyPanel` 选择该策略时自动筛选立铣刀。
- 已通过 `qmake` 和 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：圆插补铣孔下刀修正
- 圆插补铣孔不再在最终刀心圆位置垂直下刀，避免小立铣刀直接扎入未加工环形材料。
- 每层改为先回孔中心，在已钻底孔区域下刀，再径向进给到刀心圆并执行整圆插补。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：换刀与圆插补深度修正
- `SiemensPostProcessor` 在第二次及之后的 `T... M6` 换刀前自动插入 `M5`，避免主轴未停直接换刀。
- 圆插补铣孔不再默认沿用钻孔 1mm 穿透量，默认加工到识别孔深；新增 `bottomAllowance` 参数，需要穿透时再显式设置。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：五步工艺流程阶段化
- `MachiningOperation` 新增 `OperationStage`，覆盖对刀/准备、粗切、精修、深孔、收尾五个阶段。
- `OperationListPanel` 新增“阶段”列，并提供“按流程排序”按钮，可将工序按五步流程自动整理。
- 新增工序时会按策略自动推断阶段：点钻归入准备，型腔开粗归入粗切，圆插补/铰孔归入精修，排屑/深孔归入深孔，倒角归入收尾。
- `ProjectManager` 保存/加载工序时同步序列化阶段信息。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

### 已完成：加工策略提示与孔流程修正
- `StrategyPanel` 在加工策略选择区域新增动态工艺提示，说明当前策略所属阶段、用途和注意事项。
- 孔定点策略不再自动套用特征孔深，保持浅定位深度，避免生成 `CYCLE81` 钻穿孔。
- “按流程排序”顺序调整为准备、粗切、深孔/钻孔、精修、收尾，圆插补精修不再排到钻孔前。
- 生成全部 G 代码时，同一孔若同时存在排屑钻和深孔钻，会跳过排屑钻，保留深孔钻，避免重复钻孔。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。

## 2026-06-04

### 槽前视投影回归排查
- 排查确认最近槽刀路“越改越歪”的直接原因是 `MainWindow::refineSideTaggedSlotProjection()` 中新增的强制前视投影接管逻辑。
- 该逻辑把侧槽的全部特征面投影结果直接回写到正面槽的 `center/length/width/angle`，会把斜面与侧壁跨度混入正面轮廓，导致中心漂移和尺寸放大。
- 先移除 `all-feature-faces-front` 强制接管分支，回到仅接受稳定投影或拒绝改写的保守路径，作为本轮回归止损修复。

### 槽参数映射修正
- 继续排查发现偏移的上游错误在 `applySlotOverridesFromParams()`：此前把侧槽正面加工时的 `slotWidth` 默认映射成了 `feature.depth`，导致请求进入策略前就被改成 `W=5.0`。
- 现已移除这条宽度替换逻辑，只保留加工深度的前视限幅；槽宽继续使用识别到的真实宽度或用户显式输入值。
- 该修正用于消除“刀路严格跟随错误宽度几何而整体偏移”的问题。

### 槽参数面板同步修正
- 继续核查发现 `StrategyPanel::contourSlotWidth()` 仍保留同样的旧映射逻辑，会把侧槽/前视槽的槽宽显示和回填成 `feature.depth`。
- 已移除这条 UI 侧宽度替换逻辑，避免参数面板继续把 `槽宽=1.949` 错写成 `槽宽=5.000`，确保界面参数与后端槽几何一致。

### 槽中心偏移修正
- 在槽长宽恢复正确后，继续确认剩余偏移来自前视加工槽的中心点未同步到底部有效轮廓。
- 新增 `refineFrontSlotCenterFromMesh()`，仅针对“区域已是 Front、但轴仍为侧向”的槽，利用底部非垂直样本计算底部有效范围，并只修正 `center`，不再改写 `length/width/angle`。
- 该修正用于消除刀路整体落在槽外侧的问题，同时避免再次引入尺寸放大回归。

### 槽中心判定放宽
- 根据 2026-06-05 的日志，前视槽中心修正未触发的原因是对底部宽向跨度要求过严；实际该模型的底部样本已足够提供长向中心偏移信息。
- 已放宽 `refineFrontSlotCenterFromMesh()` 的应用条件：只要底部长向跨度足够覆盖识别槽长，就允许按底部样本中点修正 `center`，不再要求宽向跨度必须接近识别槽宽。

### 槽中心轴向修正
- 后续日志显示前视槽中心虽然已触发修正，但把 `U/V` 中点同时回写到世界坐标后，会把槽宽方向也一起带偏。
- 现改为仅沿底部主跨度对应的槽主方向修正 `center`，宽向维持原识别位置不动，并增加 `axis=` 调试日志标记当前采用 `U` 还是 `V` 作为长度方向。

### 槽中心投影角修正
- 新日志表明中心修正仍然沿错误世界方向偏移，原因是中心投影复用了前视加工后的 `contour.angle`，而该角度已在前面被额外旋转过 `+90°` 用于刀路方向。
- 现改为中心修正始终使用 `source.angle` 作为投影基准，只将 `contour.angle` 保留给后续刀路生成，避免几何定位和加工方向混用同一角度。

### 槽中心回写止损
- 结合连续日志确认，`refineFrontSlotCenterFromMesh()` 这条基于全特征 mesh 投影回写 `center` 的路线不稳定，会在错误坐标语义上反复放大偏移。
- 当前先停用这条中心回写，仅保留调试标记 `front-center-candidate skipped reason=disabled-unstable-projection`，把系统恢复到“槽宽正确、中心不再继续漂移”的稳定基线。
- 下一步将改为单独构造前视加工槽几何，而不是继续从 side-derived 投影结果反推通用 `center`。

### 前视加工槽几何构造
- 新增 `applyFrontReachableSlotMachiningGeometry()`，针对“区域已为 Front、但轴仍为侧向”的槽，直接从特征面集的世界坐标包围盒构造加工用中心和方向。
- 该逻辑不再回写识别特征，只在生成 `ContourFeature` 时单独修正加工几何，并输出 `front-geometry-applied` 调试日志。
- 当前先采用最小闭环：根据 `X/Y` 包围盒与识别槽长槽宽的匹配关系，判断主方向并设置加工中心与角度。

### 前视槽角度止损
- 根据 2026-06-05 14:13 的测试，前视加工几何构造在修正中心时把槽角度强制改成 `0/90`，导致原本应保留的斜面切削方向被改成直切。
- 当前先撤掉这条角度强制改写，只让前视几何构造修正加工中心，保留原识别/加工链路中的槽方向角，避免继续把斜面方向改反。

### 前视有效顶部投影
- 继续收敛前视加工槽几何来源：`applyFrontReachableSlotMachiningGeometry()` 不再直接使用整组特征面的 `X/Y` 包围盒，而是先筛选最接近前视开口顶面的样本点，再据此构造前视加工范围。
- 新增 `topZ/topTol/topCount` 调试信息，用于判断当前提取到的是否是前视开槽有效顶部窗口，减少由整段侧面外延导致的中心偏移、越界和铺不满问题。

### 前视顶部局部窗口过滤
- 根据 2026-06-05 15:01 的日志，单纯按 `topZ` 取样仍会把整段外延 `X[0,5]` 纳入顶部窗口，导致加工中心继续落在整段中点。
- 现增加基于当前识别中心的局部 `XY` 过滤窗口，仅保留槽口附近的顶部样本参与前视加工几何构造，并记录 `halfL/halfW` 调试信息用于继续收敛窗口范围。

### 前视局部种子中心
- 继续推进前视加工槽几何构造：顶部窗口不再直接围绕旧识别中心取样，而是先根据底部样本在原始角度坐标系下估算一个局部种子中心，再围绕该种子提取顶部有效窗口。
- 新增 `seed=(...)`、`bottomU/bottomV/bottomCount` 调试信息，用于确认局部种子是否已从旧识别中心移动到真实槽口附近。

### 前视窗口收紧
- 根据 2026-06-05 15:26 的日志，局部种子中心已经生效，但顶部窗口 `halfL=2.923 / halfW=2.436` 仍然过大，继续把整段 `X[0,5]` 外延纳入前视加工范围。
- 现将顶部局部窗口收紧到更接近识别槽长/槽宽本体的尺度，优先打破整段范围并为后续消除越界、铺满不足创造条件。

### 前视局部坐标过滤修正
- 根据 2026-06-05 之后的连续日志，前视槽口顶部窗口虽然已经引入局部种子中心，但筛样仍按世界 `X/Y` 比较，旋转槽在前视重建时会把整段侧壁外轮廓误认为同一局部窗口，导致中心被拉到整段中点。
- 现改为在 `applyFrontReachableSlotMachiningGeometry()` 中先为每个样本计算槽局部 `U/V` 坐标，并围绕局部种子中心按 `U/V` 窗口过滤顶部样本；同时新增 `spanU/spanV` 调试输出，用于继续核对前视重建几何是否真正收敛到槽本体范围。

### 前视局部轴语义修正
- 根据 2026-06-05 15:39 的日志，前视重建的 `seed=(2.500,0.625)` 明显被推到了外延区域，而 `bottomU[-0.000,1.150] / bottomV[-5.000,0.000]` 表明当前 side-derived 槽在前视下的 `U/V` 与“槽长/槽宽”语义并不固定一致。
- 现增加基于底部样本跨度的局部轴判定：先判断当前前视重建里是 `U` 还是 `V` 更像槽长，再用这个判定统一驱动 `seed`、顶部窗口过滤以及 `spanL/spanW` 比对，避免把整段外延轴误当成槽本体宽度轴。

### 前视几何改为按局部坐标回建中心
- 根据 2026-06-05 16:08 的日志，顶部有效样本已经进入局部窗口（`topCount=6`），但因为世界坐标 `X/Y` 上几乎退化成窄带，旧逻辑仍要求 `maxX > minX && maxY > minY`，从而把有效前视样本误判为“空窗口”。
- 现改为仅基于局部 `U/V` 样本是否成形来接受前视几何，并直接用 `U/V` 中点反算新的世界坐标中心，避免旋转槽或窄带顶面被世界 `X/Y` 包围盒错误拦截。

### 前视开口边内偏半槽宽
- 根据 2026-06-05 16:19 的日志，前视顶部样本虽已成功驱动几何重建，但 `U[1.150,1.150] / V[-5.000,0.000]` 说明当前拿到的并不是槽口面，而是开口上沿的一条边线；若直接取该边线中点回建中心，会把中心推到外缘。
- 现将前视中心重建改为：先用顶部边线确定开口边，再沿局部宽度方向向槽内偏移半个期望槽宽，从而把中心拉回槽本体内部，而不是停在外轮廓边线上。

### 暂停前视中心重写，保留识别中心
- 根据 2026-06-05 16:22 的日志，前视重建拿到的仍然主要是开口边线信息，继续基于该边线反推中心会导致 `newCenter` 在槽外缘左右跳变，问题比原始识别中心更不稳定。
- 现收口该分支：`applyFrontReachableSlotMachiningGeometry()` 保留前视边线/局部轴/范围诊断，但不再写入 `contour.center`，先以稳定的识别中心作为刀路原点，仅用 mesh 信息继续修正前视可加工深度和坡面范围，避免槽中心继续被错误漂移。

### 前视边线槽禁用宽度坡面修正
- 根据 2026-06-05 16:33 的日志，中心稳定后刀路仍然偏在外侧，说明当前问题已不在 `center`，而在宽度方向坡面参数 `slopeMinWidth/slopeMaxWidth` 把可切区域单边压缩成了边带。
- 现新增前视“边线槽”判定：若顶部样本只形成一条窄带而不是完整槽口面，则视为 front-edge-only 槽，并在 `toContourFeature()` 中禁用宽度方向坡面修正，仅保留长度方向坡面，避免 BlindSlot 被错误的单边宽度坡面推偏。

## 2026-05-15

### 阶段一：开孔加工最小闭环推进
- STEP 导入、孔识别、特征列表、策略面板、G 代码编辑器、刀路仿真已形成基础链路。
- 特征列表支持勾选确认，模型点选和列表选择可联动高亮。
- 生成 G 代码前增加孔特征、刀具、几何有效性校验。

### 阶段一：识别与显示修复
- 修复通孔被识别为凸台的问题。
- 修复孔口点选后策略面板记录为平面导致无法生成开孔 G 代码的问题。
- 修复孔特征高亮包含整张大平面的问题。
- 正面、反面、侧面分组和同类特征折叠已接入特征列表。

## 2026-05-14

### STEP 特征识别基础架构
- 将 `HoleFeature` 扩展为通用 `MachiningFeature`。
- 新增 `FeatureKind`，覆盖孔、槽、型腔、凸台、倒角、圆角、平面、螺纹孔。
- 拆出 `TopoAnalyzer`，构建 AAG 邻接属性图。
- `FeatureRecognizer` 从 `StepImporter` 中拆出，开始基于 AAG 做模式匹配。
- `FeatureClassifier` 负责孔类型细分。

### 运行环境与部署
- 补齐 Debug 目录运行所需的 OpenCascade / Qt / MSYS2 DLL。
- 修复双击 EXE 缺失 DLL 的问题。
- 启用软件 OpenGL 以规避部分 OpenGL 启动异常。
### 已完成：斜底槽刀路改为沿槽长横向往复分层切削
- 修复 `TaperedSlotMillingStrategy` 上一轮未完成的换轴修改，统一为“每个 Z 层先下刀，再沿槽长往复切削，分道沿槽宽推进”的路径组织。
- `roughReachForLayer()` 现按当前宽度道位返回该层可切的长度范围，粗加工与底面精加工共用同一层级边界逻辑，不再输出看起来像竖栅栏的固定 X 竖线组。
- 保留前面已经稳定的前视投影几何（中心/长度/宽度/深度），本次只修正 `mill_tapered_slot` 的扫描方向与分层组织。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。
### 已完成：前视投影直槽取消策略层二次换轴
- 针对 `front-local-slot-applied` 已稳定输出 `center=(2.500,0.050) L=5.000 W=1.949 A=180.000`，但 `mill_tapered_slot` 仍通过 `swapAxes=1` 把长度/宽度/角度再次重映射，导致刀路继续沿错误轴输出的问题，移除了该策略层二次换轴分支。
- `TaperedSlotMillingStrategy` 现在直接使用前序几何修正后的最终槽特征，按最终 `length/width/angle` 做分层往复切削，不再在策略内部交换长宽或追加 90 度角度补偿。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。
### 已完成：前视投影槽的单侧斜面回填与斜底策略语义统一
- 针对正面可达侧向槽日志中 `slope-candidate ... slopes=(0,0,0,0)` 的问题，新增前视投影槽专用的单侧宽度斜面回填：当底部长度已基本铺满、但底部只占槽宽一侧时，直接按底部窗口与全槽宽边界的差值回填 `slopeMinWidth/slopeMaxWidth`。
- 同时将该类槽在 `toContourFeature()` 中的几何适配语义从 `mill_blind_slot` 切换为 `mill_tapered_slot`，避免前视投影几何先按平底盲槽模型改写后，再进入斜底槽策略。
- 已通过 `mingw32-make -f Makefile.Debug` 编译验证。
## 2026-08-10：槽/型腔稳定分组与批处理保护

- 新增 `ContourFeatureGrouping`，槽和型腔按特征类型、长宽深、半径、角度及加工面形成稳定分组；特征树支持组级勾选。
- 批量槽工序增加同组硬校验，禁止把不同类型、尺寸、深度、角度或加工面的槽静默套用同一组几何参数。
- 新增 `contour_feature_grouping_test`，覆盖同组、角度差异、开口状态、尺寸、加工面、槽/型腔隔离及孔混入保护。
- 完整测试 21 项通过；`qmake` 与 Debug 全量编译通过。界面检测无布局告警，并完成真实 Qt 截图核验。
- 当前能力边界：矩形型腔已能正确识别和分组，但 `PocketRoughingStrategy` 仍以 `radius` 作为统一有效尺寸，尚未按真实 `length/width` 生成矩形边界刀路，不能作为最终验收能力。
## 2026-08-10：轮廓方向与 G40/G41/G42 人工确认

- 新增轮廓加工确认窗口：开放/封闭状态由识别结果只读显示，操作员必须人工选择正向/反向和 G41 左刀补、G42 右刀补或 G40 CAM 侧几何偏置。
- 反向选择会真正反转 `ContourFeature.points` 点序；仅有中心/半径而没有实际点列的回退轮廓不提供伪反向选项。
- 人工选择写入正式 `OperationProposal` 和工序参数，取消窗口不会创建工序；G40/G41/G42 继续经过现有引入/引出及 G40 取消安全校验。
- 窗口明确提示封闭轮廓的实际内外侧取决于点序方向，禁止软件未经人工判断自动代选刀具侧，并要求生成后继续进行最终 G 代码仿真。
- 新增 `contour_machining_choice_test`；完整测试增至 22 项并全部通过，Debug 全量编译、impeccable 布局检测和真实 Qt 截图核验通过。
## 2026-08-10：轮廓起点与方向/刀具侧模型预览

- 轮廓确认窗口新增加工起点：封闭点列可选择任意点作为起点并循环重排，保持所选正反方向；开放点列只允许首末两个端点互换，避免从中间起刀破坏路径连续性。
- `ContourMachiningChoice` 新增起点和开放/封闭语义；回退轮廓可以生成与当前策略一致的只读矩形预览，但没有真实点列时仍禁止伪造反向或任意起点。
- `ViewportWidget` 新增独立轮廓决策预览层：青色轮廓、黄色起点与方向箭头、橙色刀具侧标记；窗口选择变化时实时更新，关闭或取消后立即清除。
- 预览层不修改 STEP 模型、识别特征或仿真刀路；G40 显示当前点序左侧 CAM 偏置，G41/G42 分别显示编程路径左/右侧。
- 扩展 `contour_machining_choice_test` 覆盖封闭轮廓指定起点反向、开放轮廓端点反转和回退几何预览；完整 22 项测试、Debug 全量编译、impeccable 检测及 Qt/OpenGL 截图核验通过。
## 2026-08-10：刀具—工序适配差异预览与人工确认

- 新增 `ToolOperationCompatibility`，统一核对刀具类型、直径、有效刃长、总长、目标孔径/槽宽/型腔限制尺寸、铰刀直径和丝锥螺距。
- 工艺面板在刀具选择下方实时显示三种状态：绿色基础适配通过、黄色需要人工核对、红色禁止确认；红色状态同步禁用“确认并加入工序”。
- 几何不可能或明显危险的情况硬阻断，包括错误刀具类型、有效刃长不足、加工深度达到总长、钻头超目标孔径、圆插补刀具过大、铰刀/丝锥不匹配、槽或型腔刀具过大。
- 可继续但存在工艺差异的情况要求人工确认，包括刀具未填写刃长、钻头小于成品孔径以及槽刀过小导致走刀次数明显增加；风险窗口默认选择“不继续”。
- 软件不会自动换刀或修改参数；钻头欠径提示操作员确认后续扩孔、铰孔或铣孔工序。
- 当前尚无正式工件材料定义，因此没有对刀具材料和涂层进行无依据自动判断；该项留给材料—刀具—策略配速库。
- 新增 `tool_operation_compatibility_test`，完整测试增至 23 项并全部通过；Debug 全量编译、impeccable 检测和三状态 Qt 视觉核验通过。

## 2026-08-10：矩形/圆形型腔真实边界开粗

- `PocketRoughingStrategy` 的矩形型腔刀心边界改为分别使用真实 `length/width`，并扣除刀具半径与侧壁余量，不再错误地用单一 `radius` 生成正方形范围。
- 矩形型腔按识别方向角旋转行切路径；圆形型腔继续按有效半径裁剪扫描线，识别器同时补齐圆形型腔半径和型腔方向角。
- 工艺面板只向已验证的矩形、圆形型腔提供“型腔开粗/封闭轮廓”候选；异形型腔、孤岛及未验证类型明确阻止确认，不伪装成已支持能力。
- 新增 `pocket_roughing_strategy_test`，覆盖真实长宽、90° 旋转及刀具加余量无法容纳时的拒绝逻辑；完整 24 项测试全部通过。
- `qmake`、Debug 全量编译及 Impeccable 布局检测通过；最新 `debug/CNEXT-CAM.exe` 已生成。

## 2026-08-10：型腔下刀方式人工选择与安全闭环

- 型腔开粗新增“请选择、垂直下刀、螺旋下刀”显式选择框；每次切换型腔特征都会清除旧选择，未人工选择时禁止确认工序。
- 垂直下刀改为从型腔中心进入，不再从行切边角扎入；确认工序前提示必须已经预钻入口或确认刀具具备中心切削能力，风险窗口默认“不继续”。
- 螺旋下刀不再静默缩小半径或自动生成节距；半径、节距未填写、非正数或半径超出扣除刀具半径和余量后的有效边界时，直接禁止确认或拒绝生成。
- G 代码加入 `POCKET ENTRY: VERTICAL/HELICAL` 决策注释，便于最终代码复核和仿真追溯。
- 扩展 `pocket_roughing_strategy_test` 与 `operation_proposal_confirmation_test`；完整 24 项测试、Debug 全量构建和 Impeccable 布局检测通过。

## 2026-08-10：异形型腔外边界与孤岛拓扑数据闭环

- `MachiningFeature` 和 `ContourFeature` 增加外边界及多孤岛内环表达；OpenCascade 识别器按底面外线框和内线框采样直线、圆弧及其他曲线。
- 外边界和孤岛拓扑贯穿特征候选、工艺方案、正式工序、项目保存/加载和程序快照指纹；任一孤岛点变化都会使旧程序快照失效。
- 异形型腔批处理分组纳入完整拓扑坐标，尺寸相同但边界或孤岛不同的型腔不会再被静默归为同组。
- 特征列表和工艺面板显示外边界点数与孤岛数量；已识别拓扑但尚无安全刀路时，明确说明阻断原因。
- 使用最终验收模型 `WH250852-模板紅色面加工（零件3）.STEP` 进行只读烟雾测试：13 个型腔全部提取到有效外边界，其中 2 个复杂型腔各含 7 个内环，共识别 14 个孤岛。
- 当前边界：尚未实现经过验证的刀具半径偏置、分区连接和孤岛避让 G 代码，因此异形型腔继续禁止生成，不能宣称加工闭环。

## 2026-08-10：异形型腔开粗与孤岛避让最小安全闭环

- `PocketRoughingStrategy` 已读取真实外边界和多孤岛内环，按“刀具半径 + 用户余量”计算外边界内缩及孤岛外扩后的水平扫描安全区。
- 同一扫描行被孤岛切断时生成独立切削段；每段结束先退到安全高度，再进行下一段 XY 快速定位，禁止低位跨越孤岛。
- 自交、退化外边界以及位于外边界之外或与外边界相交的孤岛会被明确拒绝，不使用偶奇填充猜测加工区域。
- 工艺面板已向具有至少 3 个边界点的异形型腔开放“型腔开粗”；下刀方式仍由操作员选择，当前异形型腔仅允许人工确认的垂直下刀，选择螺旋下刀会阻止确认并提示恢复方式。
- 新增回归覆盖外边界刀具间隙、圆角孤岛扩张、分段避让、安全抬刀、螺旋下刀阻断、自交边界及外置孤岛拒绝；全套 24 项测试和 Debug 完整构建通过。
- 最终验收模型只读回归：13 个型腔中 2 个复杂型腔各含 7 个孤岛，两者均成功生成刀路；默认测试参数下分别约 15,345 行和 13,805 行，后续需由既定 CQ8 独立 TCP 旋转缓冲接口承接超过 1 万行程序的持续下发。
- 当前边界：仅完成开粗扫描，不包含异形侧壁/底面精加工、圆弧拟合或抬刀次数优化；正式实机加工前仍须完成最终 G 代码仿真、空运行和单段低倍率验证。

## 2026-08-10：异形型腔拓扑稳定区连续扫描优化

- 相邻扫描行只有在安全区间连续、区间端点一致且两行之间不存在外边界或孤岛顶点时才合并为同一切削链；任何拓扑变化仍强制抬刀后重新定位。
- 同一切削链只在链首垂直下刀、链尾退回安全高度，中间使用明确的 `G1 X/Y` 连续连接，仿真继续解析最终 G 代码，不引入查看器专用刀路。
- 最终验收模型回归中，两个各含 7 个孤岛的复杂型腔分别由约 15,345 行降至 13,811 行、由约 13,805 行降至 12,245 行，降幅约 10.0% 和 11.3%。
- 全套 24 项测试及 Debug 构建通过；外边界间隙、孤岛避让、低位快速移动禁令和不稳定拓扑拆链约束保持有效。
- 两个程序仍超过 1 万行。现有 Siemens SPF 构建器尚未接入正式生成服务，CQ8 函数调用方言也未确定；下一步应建立方言无关的参数化刀路函数中间表示，再由 Siemens/Fanuc/CQ8 后处理器分别输出，不能直接把 Siemens `PROC/SPF` 写死到 CQ8 路径。
- 完整 24 项测试和 Debug 全量构建通过。

## 2026-08-11：参数化刀路函数中间表示

- 新增控制器无关的参数化刀路中间表示：保存“例程模板 + 参数名 + 调用实参”，当前异形型腔按每层 `DEPTH_Z` 调用同一安全扫描模板。
- 每次生成仍先将中间表示完整展开为最终 `gcodeText`；安全校验、仿真、程序指纹和后续 CQ8 TCP 流式下发继续只使用这份实际 G 代码，避免函数形式和显示/执行路径不一致。
- 参数化元数据独立随项目快照保存，不混入可发送的程序包；用户手动修改最终 G 代码时会自动清除元数据，防止误认为模板仍与程序文本一致。
- 目前没有假定 CQ8 的函数语法或 Siemens SPF 格式。下一阶段待 CQ8 调用约定确认后，再由后处理器把该中间表示显式映射为控制器函数程序；原始展开 G 代码保留为可审核、可仿真的安全基准。

## 2026-08-11：固定循环 G80 模态安全校验

- G 代码安全校验器现在显式跟踪 `G81` 至 `G89` 的固定循环模态；手动定义的循环若未在换刀或 `M30` 前以 `G80` 取消，会阻止生成并指出违规行。
- 固定循环行不再错误继承前一条 `G0` 的快移模态，避免将合法的 `G81/G82/G83` 钻削坐标误报为低位快速移动。
- 回归测试覆盖“未取消必须拒绝”与“G80 取消后通过”；现有 Siemens `CYCLE83/MCALL` 孔批量生成服务回归及 Debug 完整构建通过。

## 2026-08-11：固定循环重复孔位校验

- 修复 Fanuc 风格批量孔循环的模态判定：`G83` 首孔之后只有 `X/Y` 的后续孔位属于当前固定循环调用，不再被误判为继承前序 `G0` 的低位快移。
- 显式 `G0` 在循环激活期间仍会进入原有快移安全校验；循环结束仍必须显式输出 `G80`，不放宽固定循环取消要求。
- 新增重复孔位固定循环回归，覆盖 `G83` 首孔、第二孔 `X/Y` 调用和 `G80` 取消的完整序列。

## 2026-08-11：固定循环显式快移取消校验

- 安全校验器拒绝固定循环尚未 `G80` 取消时的显式 `G0`，包括仅用于安全高度抬刀的 `G0 Z`；必须先取消循环再执行普通快移。
- 同循环仅 `X/Y` 孔位调用仍合法，避免破坏 Fanuc 风格批量孔的模态调用。
- 新增回归覆盖 `G81 → G0 Z → G80` 的即时拒绝；固定循环安全测试、程序生成服务测试和完整 Debug 构建均通过。

## 2026-08-11：毫米制 G 代码安全门

- 首期通用 ISO 输出固定为毫米制：安全校验现在要求 `G21`，并拒绝任何包含 `G20` 的程序，避免手工编辑后将尺寸按英制解释。
- 即使同时存在 `G21` 与 `G20` 也会阻断，不允许依赖末尾模态覆盖放行。
- 新增 `G20` 回归；安全校验、程序生成服务和完整 Debug 构建均通过。

## 2026-08-11：内部孔循环后处理安全门

- `ProgramGenerationService` 会在后处理前校验策略内部 `CNEXT_HOLE_CYCLE` 标记；缺失 `code` 或非首期 `G81-G85` 的循环会使整批程序原子失败。
- 修复了不支持循环可能被后处理器退化为单独 `X/Y` 行、从而出现“生成成功但孔未加工”的风险。
- 新增 `G86` 内部循环拒绝回归；程序生成服务、安全校验和完整 Debug 构建均通过。

## 2026-08-11：G83 啄钻参数安全门

- 手工或后处理后的 `G83` 循环定义必须显式给出正的啄钻量 `Q`；零值、负值或缺失 `Q` 都会在最终安全校验阶段被拒绝。
- 后续仅含 `X/Y` 的批量孔位调用不受此规则影响，仍由已定义的固定循环参数执行。
- 新增 `G83 Q0` 拒绝回归；安全校验、程序生成服务和完整 Debug 构建均通过。

## 2026-08-11：G83 返回平面安全门

- `G83` 循环定义现在要求明确的孔底 `Z` 与返回平面 `R`，且 `R` 必须高于 `Z`；缺失参数或返回平面落在孔底及以下都会阻断。
- 新增 `Z=-5、R=-6` 拒绝回归；安全校验、程序生成服务和完整 Debug 构建均通过。

## 2026-08-11：首期固定循环统一返回平面校验

- 将 `Z/R` 几何安全规则从 `G83` 扩展到首期全部 `G81-G85`：定义行必须明确孔底 `Z` 与返回平面 `R`，并满足 `R > Z`。
- `G83` 继续单独要求正的 `Q`；后续仅 `X/Y` 孔位调用不重复要求循环参数。
- 新增无效 `G81` 返回平面回归；安全校验、程序生成服务和完整 Debug 构建均通过。

## 2026-08-11：首期固定循环白名单

- 最终 G 代码安全校验现在明确拒绝手工写入的 `G86-G89`，与策略层和后处理器的首期 `G81-G85` 能力范围一致。
- 拒绝信息包含具体循环代码，方便操作员改回已验证工艺或等待后续能力验收。
- 新增完整 `G86` 程序拒绝回归；安全校验、程序生成服务和完整 Debug 构建均通过。

## 2026-08-11：首期进给模式安全门

- 最终 G 代码安全校验明确拒绝 `G95` 每转进给模式，首期仅允许 `G94` 每分钟进给，防止手工编辑后改变进给单位语义。
- 新增完整 `G95` 程序拒绝回归；安全校验、程序生成服务和完整 Debug 构建均通过。

## 2026-08-11：CQ8 模态压缩与首期宏程序包

- 最终程序现在省略连续重复的 `G0/G1/G2/G3`、`F` 与 `S` 词；安全复位、刀补、固定循环、换刀及 M 代码行不会被改写。
- 新增 CQ8（Macro B）后处理目标：重复层型腔可输出 `#100...` 参数、`O9001` 例程、`M98 P9001` 调用及 `M99` 返回，并以主程序与 `CQ8_MACROS.NC` 宏库分包导出。
- 仅当参数化模板能逐字复原原始展开刀轨时才允许压缩；否则生成失败。展开程序继续保存为仿真与安全校验基准，避免宏调用影响刀轨审查。
- 操作员手动修改最终主程序时，会同步清除宏库、宏例程元数据和展开仿真副本，避免导出或仿真使用已失效的派生产物。

## 2026-08-11：规则型腔重复层宏化

- 垂直下刀的矩形与圆形型腔粗加工现在也生成 `POCKET_LAYER(DEPTH_Z)` 参数例程；CQ8 输出可将每个重复深度层缩减为参数赋值与 `M98` 调用。
- 仅在模板展开与原始刀轨逐字一致时保留例程元数据；螺旋下刀因各层螺旋圈数与 Z 值不同，当前仍保留展开输出。

## 2026-08-11：CQ8 宏库操作员审查

- “最终 CQ8 程序”区域新增只读宏库视图，操作员可将主程序中的 `M98` 调用与对应 `Oxxxx/M99` 例程并列核对。
- 手动编辑主程序时立即清空宏库视图，避免将已失效宏库误认为当前可导出程序包。

## 2026-08-11：CQ8 宏参数注入防护

- 首期 CQ8 宏调用参数仅接受有限数值字面量；含换行、G/M 代码、变量表达式或非数值内容会在构建宏库时拒绝。
- Macro B 算术表达式、条件与循环属于后续独立验收能力，当前不会被 CAM 自动写入。

## 2026-08-11：CQ8 宏语法高亮

- 最终代码与宏库审查区现在独立高亮 `M98/M99`、`Oxxxx` 例程号及 `#变量`，便于操作员核对宏调用关系。

## 2026-08-11：CQ8 宏压缩行数审查

- 程序快照同时保存控制器主程序行数与展开仿真行数；当宏压缩生效时，快照提示显示“紧凑行数 / 展开行数”。

## 2026-08-12：CQ8 宏程序包静态审查

- 导出 CQ8 程序包前，`CQ8_MAIN.NC` 的每个 `M98 Pxxxx` 必须在 `CQ8_MACROS.NC` 中有唯一的 `Oxxxx/M99` 例程。
- 缺失主程序或宏库、重复例程号、或主程序调用未定义例程时，导出在写入任何文件前被阻断。
- 首期宏库同样拒绝例程内部 `M98` 嵌套调用，以及缺少 `M99` 返回的例程；仅允许扁平调用图。

## 2026-08-12：CQ8 宏变量范围审查

- 导出前同时扫描 `CQ8_MAIN.NC` 与 `CQ8_MACROS.NC`；首期仅允许使用 `#100-#199` 宏变量。
- 手工修改后出现 `#99`、`#200` 等未约定变量时，程序包会在写入任何文件前被拒绝；变量算术、条件与循环仍保持后续独立验收范围。

## 2026-08-12：CQ8 参数赋值字面量审查

- 主程序对 `#100-#199` 的赋值现在必须是一条完整的有限数值字面量；例如 `#100=-2.000` 可导出。
- `#100=#101+1` 等算术表达式或夹带其他代码的赋值会在导出前被拒绝，保证首期宏调用只传递经确认的参数值。

## 2026-08-12：CQ8 宏控制语法审查

- 导出审查拒绝宏库中的 `IF`、`GOTO`、`WHILE/DO/END` 控制流，以及方括号算术表达式。
- 首期保留扁平 `M98` 调用、`M99` 返回和参数变量直接引用；条件、循环与表达式须在后续 CQ8 能力验收后再开放。

## 2026-08-12：CQ8 宏参数只读审查

- 首期参数值只能在 `CQ8_MAIN.NC` 中以数值字面量传入；`CQ8_MACROS.NC` 例程内只允许引用，不允许重新赋值。
- 这使每次 `M98` 调用的参数状态由主程序唯一决定，避免例程内部状态修改影响后续刀轨。

## 2026-08-12：CQ8 宏返回终止审查

- `M99` 现在必须是每个 `Oxxxx` 例程最后一条有效指令；返回后的任何可执行代码都会拒绝导出。
- 例程结尾允许空行和分号注释，避免无关格式变化影响已验证程序包。

## 2026-08-12：规则型腔侧壁精加工

- 新增“型腔侧壁精加工”策略，当前仅面向矩形和圆形型腔；在策略面板与手动铣削工序中由操作员明确选择。
- 刀路使用 CAM 侧偏置（`G40`），不启用 `G41/G42` 机床刀补；按层沿最终壁面走刀，并复用型腔最小尺寸的刀具适配阻断规则。
- 异形型腔侧壁精修仍未自动开放，防止在尚未完成边界与孤岛精修验证前输出不可靠刀路。

## 2026-08-12：规则型腔底面精加工

- 新增“型腔底面精加工”策略，当前仅对矩形与圆形型腔开放；操作员可在策略面板或手动铣削工序中选择。
- 使用 CAM 刀心平行走刀，刀路限制在扣除刀具半径和底面余量后的有效边界内；矩形采用往复走刀，圆形采用弦线走刀。
- 底面精加工归类为 `FinishCut`，异形型腔仍保持禁用，待边界与孤岛精加工验收后再扩展。

## 2026-08-12：规则型腔精加工安全下刀

- 矩形、圆形型腔的侧壁与底面精加工不再从成品壁面刀心轨迹直接下刀；每层先在型腔中心下刀，再以进给移动到首条加工轨迹。
- 这两道精加工应安排在已清空中心区域的开粗之后；策略提示已明确要求操作员在确认工序前核对该前提。
