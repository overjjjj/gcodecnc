# 模块 1：全工艺通用设置验收记录

## 状态

- 已完成：通用 schema 参数在统一编辑器中的展示、单位、来源和跨字段校验。
- 已完成：工艺策略、G54-G59、刀具、进给/转速、高度/基准/深度、步距/分层、余量及进退刀参数沿用现有策略入口。
- 已完成：旧策略 `feedHeight`/`stepover` 与规范字段 `plungeHeight`/`stepOver` 双向兼容。
- 已完成：有效模板标识、版本、值和字段来源进入正式工序并可保存、重开、恢复。
- 未改动：冷却字段和行为。

## 证据

- `process_parameter_schema_test`：schema 可枚举、单位和公共安全校验通过。
- `operation_factory_test`：优先级、模板快照、人工覆盖和恢复模板值通过。
- `parameter_editor_schema_test`：四列表单、单位、来源和恢复模板值通过。
- `project_manager_serialization_test`：模板及字段来源保存重开通过。
- `standalone_ui_contract_test`：实际工序确认入口使用 `OperationFactory`。
- `CNEXT-CAM.exe`：Debug 工程完整链接成功。

## 基线与风险

- `scripts/run_tests.ps1` 已通过新增测试、孔选择、策略、后处理、程序生成和 CQ8 集成测试。
- `acceptance_step_model_test` 仍在既有侧面型腔 Setup 未确认场景失败；生成被安全阻止，未删除、跳过或放宽该检查。
- 当前模板标识为内置策略模板；材料模板和刀具类型模板的资料查找由模块 6 接入，优先级合同已由阶段 0 覆盖。
- 规范高度字段通过兼容层映射到现有策略；各策略迁移为独立 schema 将随对应工艺模块逐项完成。

## 下一个模块

- 模块 2：孔类与螺纹。先核对固定循环策略与最终后处理输出，再补循环参数和预览一致性失败测试。
